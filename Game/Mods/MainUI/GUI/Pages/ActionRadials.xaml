<ls:UIWidget x:Name="ActionRadials"
             ls:UIWidget.ContextName="HotBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors" 
             xmlns:ls="clr-namespace:ls;assembly=Code"
             xmlns:noesis="clr-namespace:NoesisGUIExtensions;assembly=Noesis.GUI.Extensions"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:System="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d"
             d:DesignHeight="2160" d:DesignWidth="3840" FocusLeft="UITabPrev" FocusRight="UITabNext"
             ls:TooltipExtender.Owner="{Binding CurrentPlayer.SelectedCharacter}" KeyboardNavigation.DirectionalNavigation="Continue"
             d:DataContext="{d:DesignInstance {x:Type ls:DCHotBar}, IsDesignTimeCreatable=True}" CanCacheFocusSurroundingElements="True" Template="{StaticResource ActionRadialWidgetTemplate}"
             ls:AttachedProperties.AutomationId ="{Binding (ls.UICanvas.CanvasLayout), RelativeSource={RelativeSource Mode=Self}, StringFormat='{}ActionRadials_{0}'}">

    <b:Interaction.Triggers>
        <b:EventTrigger EventName="GotKeyboardFocus">
            <b:ChangePropertyAction TargetName="ActionRadials" PropertyName="Tag" Value="{Binding Path=FocusedElement, ElementName=ActionRadials}"/>
            <b:InvokeCommandAction Command="{Binding ShowTooltipOnUIElementCommand, ElementName=ActionRadials}" CommandParameter="{Binding Path=FocusedElement, ElementName=ActionRadials}"/>
            <b:InvokeCommandAction Command="{Binding ClearResourceHighlightsCommand}"/>
        </b:EventTrigger>

        <b:EventTrigger EventName="Loaded">
            <b:InvokeCommandAction Command="{Binding EnableTooltipOnUIElementCommand, ElementName=ActionRadials}"/>
            <b:ChangePropertyAction TargetObject="{Binding CurrentPlayer.UIData}" PropertyName="AreRadialsOpen" Value="True"/>
            <!-- Tutorial UI Event - Action Radials Open -->
            <b:InvokeCommandAction Command="{Binding TutorialEvent}" CommandParameter="f651618e-d396-4d4a-ba5a-8f50c3697708"/>
            <ls:LSPlaySound Sound="UI_HUD_Controller_RadialMenu_Open" />
        </b:EventTrigger>
        <b:EventTrigger EventName="Loaded">
            <b:Interaction.Behaviors>
                <b:ConditionBehavior>
                    <b:ConditionalExpression>
                        <b:ComparisonCondition LeftOperand="{Binding Metadata, ElementName=ActionRadials}" Operator="Equal" RightOperand="CallAllies"/>
                    </b:ConditionalExpression>
                </b:ConditionBehavior>
            </b:Interaction.Behaviors>
            <b:InvokeCommandAction Command="{Binding CallAllies}"/>
        </b:EventTrigger>

        <!-- Focus the first one -->
        <b:EventTrigger EventName="Loaded">
            <b:Interaction.Behaviors>
                <b:ConditionBehavior>
                    <b:ConditionalExpression>
                        <b:ComparisonCondition LeftOperand="{Binding Metadata, ElementName=ActionRadials}" Operator="NotEqual" RightOperand="MoveToEnd"/>
                    </b:ConditionalExpression>
                </b:ConditionBehavior>
            </b:Interaction.Behaviors>
            <ls:SetMoveFocusAction TargetName="ActionRadials" DirectionFocus="Left" JumpFocus="First"/>
        </b:EventTrigger>

        <!-- Focus the last one -->
        <b:EventTrigger EventName="Loaded">
            <b:Interaction.Behaviors>
                <b:ConditionBehavior>
                    <b:ConditionalExpression>
                        <b:ComparisonCondition LeftOperand="{Binding Metadata, ElementName=ActionRadials}" Operator="Equal" RightOperand="MoveToEnd"/>
                    </b:ConditionalExpression>
                </b:ConditionBehavior>
            </b:Interaction.Behaviors>
            <ls:SetMoveFocusAction TargetName="ActionRadials" DirectionFocus="Right" JumpFocus="Last"/>
        </b:EventTrigger>

        <b:EventTrigger EventName="Unloaded">
            <b:ChangePropertyAction TargetName="ActionRadials" PropertyName="Tag" Value="{x:Null}"/>
        </b:EventTrigger>

        <b:EventTrigger EventName="WidgetClosing">
            <b:InvokeCommandAction Command="{Binding ClearResourceHighlightsCommand}"/>
            <b:ChangePropertyAction TargetObject="{Binding CurrentPlayer.UIData}" PropertyName="AreRadialsOpen" Value="False"/>
            <ls:LSPlaySound Sound="UI_HUD_Controller_RadialMenu_Close" />
        </b:EventTrigger>

        <b:EventTrigger EventName="MoveFocusRight">
            <ls:LSPlaySound Sound="UI_HUD_Controller_RadialMenu_MoveRight" />
        </b:EventTrigger>

        <b:EventTrigger EventName="MoveFocusLeft">
            <ls:LSPlaySound Sound="UI_HUD_Controller_RadialMenu_MoveLeft" />
        </b:EventTrigger>

        <b:PropertyChangedTrigger Binding="{Binding Layout}">
            <b:Interaction.Behaviors>
                <b:ConditionBehavior>
                    <b:ConditionalExpression>
                        <b:ComparisonCondition LeftOperand="{Binding Tag, ElementName=ActionRadials}" Operator="NotEqual" RightOperand="{x:Null}"/>
                    </b:ConditionalExpression>
                </b:ConditionBehavior>
            </b:Interaction.Behaviors>

            <!-- Force a re-focus of the current focused radial to make sure it will be placed properly -->
            <ls:SetMoveFocusAction TargetName="ActionRadials" Reset="True"/>
            <ls:SetMoveFocusAction TargetName="ActionRadials" FocusElement="{Binding Tag, ElementName=ActionRadials}" DeferFocusAction="True"/>
        </b:PropertyChangedTrigger>
    </b:Interaction.Triggers>


</ls:UIWidget>
