<ls:UIWidget
    x:Name="Book"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:noesis="clr-namespace:NoesisGUIExtensions;assembly=Noesis.GUI.Extensions"
    xmlns:ls="clr-namespace:ls;assembly=Code"
    xmlns:System="clr-namespace:System;assembly=mscorlib"
    mc:Ignorable="d"
    ls:UIWidget.UseCustomDC="True"
    ls:UIWidget.ContextName="Book">
    <ls:UIWidget.Resources>
        <ResourceDictionary Source="/Core;component/Library/BookTemplates.xaml"/>
    </ls:UIWidget.Resources>

    <ls:UIWidget.Template>
        <ControlTemplate>
            <ControlTemplate.Resources>

                <ControlTemplate x:Key="PageturnerTemplate" TargetType="ls:LSButton">
                    <Grid x:Name="hitbox" Width="300" Height="1300" Background="Transparent">
                        <Image x:Name="img" Stretch="None" VerticalAlignment="Center" HorizontalAlignment="Left" Source="{DynamicResource ButtonNormal}">
                        </Image>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Source" Value="{DynamicResource ButtonPressed}" TargetName="img"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Source" Value="{DynamicResource ButtonDisabled}" TargetName="img"/>
                        </Trigger>
                        <DataTrigger Binding="{Binding IsMouseOver, ElementName=img}" Value="True">
                            <Setter Property="Source" Value="{DynamicResource ButtonHighlight}" TargetName="img"/>
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
                
                <!--Bookviewer pageturner button -->
                <Style x:Key="PageTurnerButtonStyle" TargetType="{x:Type ls:LSButton}">
                    <Style.Resources>
                        <BitmapImage x:Key="ButtonNormal" UriSource="pack://application:,,,/Core;component/Assets/Book/browseBtn_d.png"/>
                        <BitmapImage x:Key="ButtonHighlight" UriSource="pack://application:,,,/Core;component/Assets/Book/browseBtn_h.png"/>
                        <BitmapImage x:Key="ButtonPressed" UriSource="pack://application:,,,/Core;component/Assets/Book/browseBtn_p.png"/>
                        <BitmapImage x:Key="ButtonDisabled" UriSource="pack://application:,,,/Core;component/Assets/Book/browseBtn_disabled.png"/>
                    </Style.Resources>
                    <Setter Property="Template" Value="{StaticResource PageturnerTemplate}"/>
                </Style>

                <ControlTemplate x:Key="ZeroEditionBookTemplate" TargetType="Control">
                    <Grid>
                        <Grid x:Name="Book" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="50,0,0,140">

                            <Image x:Name="BookBackground" Source="{StaticResource DefaultBookBackground}" Stretch="None"/>

                            <ls:LSButton x:Name="PageTurnerButtonLeft" Style="{StaticResource PageTurnerButtonStyle}" HorizontalAlignment="Left" VerticalAlignment="Center" Visibility="Collapsed"
                             Command="ls:LSBook.PrevPageCommand" CommandTarget="{Binding ElementName=BookMain}"
                             DataContext="{Binding CurrentPlayer.UIData.InputEvents, Converter={StaticResource FindInputEventConverter}, ConverterParameter='UILeft', RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}" 
                             IsEnabled="{Binding Path=HasPrevPage, ElementName=BookMain}"/>

                            <ls:LSButton x:Name="PageTurnerButtonRight" Style="{StaticResource PageTurnerButtonStyle}" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0 0 44 0" Visibility="Collapsed" 
                             Command="ls:LSBook.NextPageCommand" CommandTarget="{Binding ElementName=BookMain}"
                             DataContext="{Binding CurrentPlayer.UIData.InputEvents, Converter={StaticResource FindInputEventConverter}, ConverterParameter='UIRight', RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}"
                            IsEnabled="{Binding Path=HasNextPage, ElementName=BookMain}" RenderTransformOrigin="0.5,0.5">
                                <ls:LSButton.RenderTransform>
                                    <ScaleTransform ScaleX="-1"/>
                                </ls:LSButton.RenderTransform>
                            </ls:LSButton>

                            <ls:LSBook x:Name="BookMain" Margin="240,136,275,200" Text="{Binding BookFullText}" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"
                                       Template="{StaticResource DefaultBookTemplate}"/>

                            <Image x:Name="TabBackground" Source="{StaticResource PageTabBackground}" Stretch="None" Margin="0,0,1020,0" VerticalAlignment="Bottom"
                                   HorizontalAlignment="Right" Visibility="Collapsed"/>

                            <Grid x:Name="CloseButtonContainer" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,60,336,0" >
                                <Image x:Name="CloseButtonBackground" Source="pack://application:,,,/Core;component/Assets/Book/book_UI_close_bg.png" 
                                       Stretch="None" Margin="0,0,8,0" Visibility="Collapsed"/>

                                <ls:LSButton x:Name="BookCloseBtn" Template="{StaticResource IconButtonTemplate}" Command="{Binding CustomEvent}" CommandParameter="CloseWidget" SoundID="UI_Shared_Cancel" VerticalAlignment="Top" Margin="0,40,0,0" >
                                    <ls:LSButton.Resources>
                                        <BitmapImage x:Key="ButtonNormal" UriSource="pack://application:,,,/Core;component/Assets/Buttons/btn_hudNav_d.png"/>
                                        <BitmapImage x:Key="ButtonHighlight" UriSource="pack://application:,,,/Core;component/Assets/Buttons/btn_hudNav_h.png" />
                                        <BitmapImage x:Key="ButtonPressed" UriSource="pack://application:,,,/Core;component/Assets/Buttons/btn_hudNav_p.png" />
                                        <BitmapImage x:Key="ButtonDisabled" UriSource="pack://application:,,,/Core;component/Assets/Buttons/btn_hudNav_disabled.png" />
                                        <BitmapImage x:Key="IconNormal" UriSource="pack://application:,,,/Core;component/Assets/Buttons/ico_btn_cross_d.png" />
                                        <BitmapImage x:Key="IconHighlight" UriSource="pack://application:,,,/Core;component/Assets/Buttons/ico_btn_cross_h.png" />
                                        <BitmapImage x:Key="IconPressed" UriSource="pack://application:,,,/Core;component/Assets/Buttons/ico_btn_cross_d.png" />
                                        <BitmapImage x:Key="IconDisabled" UriSource="pack://application:,,,/Core;component/Assets/Buttons/ico_btn_cross_d.png" />
                                        <System:Double x:Key="IconDisabledOpacity">0.7</System:Double>
                                        <System:Double x:Key="IconWidth">64</System:Double>
                                        <System:Double x:Key="IconHeight">64</System:Double>
                                    </ls:LSButton.Resources>
                                </ls:LSButton>
                            </Grid>
                        </Grid>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <DataTrigger Binding="{Binding BookType}" Value="None">
                            <Setter TargetName="CloseButtonBackground" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="TabBackground" Property="Visibility" Value="{Binding ElementName=BookMain,Path=HasNextPage,Converter={StaticResource BoolToVisibleConverter}}"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding BookType}" Value="PaperSheet">
                            <Setter TargetName="BookBackground" Property="Source" Value="{StaticResource PaperSheetBackground}"/>
                            <Setter TargetName="BookMain" Property="Margin" Value="50,250,0,200"/>
                            <Setter TargetName="BookMain" Property="Template" Value="{StaticResource PaperSheetBookTemplate}"/>
                            <Setter TargetName="CloseButtonContainer" Property="Margin" Value="0,115,155,0"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding BookType}" Value="PaperStack">
                            <Setter TargetName="BookBackground" Property="Source" Value="{StaticResource PaperStackBackground}"/>
                            <Setter TargetName="BookMain" Property="Margin" Value="50,250,0,200"/>
                            <Setter TargetName="BookMain" Property="Template" Value="{StaticResource PaperSheetBookTemplate}"/>
                            <Setter TargetName="CloseButtonContainer" Property="Margin" Value="0,115,155,0"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding BookType}" Value="ScrollA">
                            <Setter TargetName="BookBackground" Property="Source" Value="{StaticResource ScrollABackground}"/>
                            <Setter TargetName="BookMain" Property="Margin" Value="45,250,0,200"/>
                            <Setter TargetName="BookMain" Property="Template" Value="{StaticResource ScrollBookTemplate}"/>
                            <Setter TargetName="CloseButtonContainer" Property="Margin" Value="0,290,295,0"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding BookType}" Value="ScrollB">
                            <Setter TargetName="BookBackground" Property="Source" Value="{StaticResource ScrollBBackground}"/>
                            <Setter TargetName="BookMain" Property="Margin" Value="45,250,0,200"/>
                            <Setter TargetName="BookMain" Property="Template" Value="{StaticResource ScrollBookTemplate}"/>
                            <Setter TargetName="CloseButtonContainer" Property="Margin" Value="0,290,295,0"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding BookType}" Value="TabletA">
                            <Setter TargetName="BookBackground" Property="Source" Value="{StaticResource TabletABackground}"/>
                            <Setter TargetName="BookMain" Property="Margin" Value="0,160,40,200"/>
                            <Setter TargetName="BookMain" Property="Template" Value="{StaticResource TabletBookTemplate}"/>
                            <Setter TargetName="CloseButtonContainer" Property="Margin" Value="0,185,190,0"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding BookType}" Value="TabletB">
                            <Setter TargetName="BookBackground" Property="Source" Value="{StaticResource TabletBBackground}"/>
                            <Setter TargetName="BookMain" Property="Margin" Value="0,160,40,200"/>
                            <Setter TargetName="BookMain" Property="Template" Value="{StaticResource TabletBookTemplate}"/>
                            <Setter TargetName="CloseButtonContainer" Property="Margin" Value="0,185,190,0"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding BookType}" Value="TabletC">
                            <Setter TargetName="BookBackground" Property="Source" Value="{StaticResource TabletCBackground}"/>
                            <Setter TargetName="BookMain" Property="Margin" Value="0,160,40,200"/>
                            <Setter TargetName="BookMain" Property="Template" Value="{StaticResource TabletBookTemplate}"/>
                            <Setter TargetName="CloseButtonContainer" Property="Margin" Value="0,185,190,0"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding BookType}" Value="GithDisc">
                            <Setter TargetName="BookBackground" Property="Source" Value="{StaticResource GithDiscBackground}"/>
                            <Setter TargetName="BookMain" Property="Margin" Value="0,250,150,200"/>
                            <Setter TargetName="BookMain" Property="Template" Value="{StaticResource GithDiscBookTemplate}"/>
                            <Setter TargetName="CloseButtonContainer" Property="Margin" Value="0,140,250,0"/>
                        </DataTrigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding BookType}" Value="None"/>
                                <Condition Binding="{Binding HasPrevPage, ElementName=BookMain}" Value="True"/>
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="PageTurnerButtonLeft" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="PageTurnerButtonRight" Property="Visibility" Value="Visible"/>
                        </MultiDataTrigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding BookType}" Value="None"/>
                                <Condition Binding="{Binding HasNextPage, ElementName=BookMain}" Value="True"/>
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="PageTurnerButtonRight" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="PageTurnerButtonLeft" Property="Visibility" Value="Visible"/>
                        </MultiDataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </ControlTemplate.Resources>

            <Grid Background="{StaticResource LS_PanelBackground}" x:Name="BookViewerHolder" Opacity="1">
                <ls:LSButton x:Name="ModalClose" Command="{Binding CustomEvent}" CommandParameter="CloseWidget" SoundID="UI_HUD_Options_Close" Margin="-8,-7">
                    <Image x:Name="Vignette" Source="{StaticResource MainPaneVignette}" Stretch="Fill"/>
                </ls:LSButton>
                <Border x:Name="BookContentHolder" Opacity="1">
                    <Control x:Name="Content" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                        <Control.Template>
                            <ControlTemplate TargetType="Control">
                                <ControlTemplate.Resources>
                                    <Style x:Key="BaseTextStyle" TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{StaticResource LS_tint20}"/>
                                        <Setter Property="TextWrapping" Value="Wrap"/>
                                        <Setter Property="FontWeight" Value="ExtraBold"/>
                                        <Setter Property="FontFamily" Value="{StaticResource DefaultFont}"/>
                                    </Style>

                                    <Style x:Key="ArticleStyle" TargetType="TextBlock" BasedOn="{StaticResource BaseTextStyle}">
                                        <Setter Property="FontSize" Value="{StaticResource IntermediateFontSize}"/>
                                        <Setter Property="LineStackingStrategy" Value="BlockLineHeight"/>
                                        <Setter Property="LineHeight" Value="{StaticResource MediumFontSizeLineHeight}"/>
                                        <Setter Property="Margin" Value="10"/>
                                        <Setter Property="Visibility" Value="Visible"/>
                                        <Style.Triggers>
                                            <Trigger Property="ls:TextBlockFormatter.SourceText" Value="">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </ControlTemplate.Resources>
                                <Control Template="{StaticResource GazetteTemplate}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </ControlTemplate>
                        </Control.Template>
                    </Control>
                    <Border.LayoutTransform>
                        <ScaleTransform x:Name="BookContentTransform" ScaleX="1" ScaleY="1"/>
                    </Border.LayoutTransform>
                </Border>
            </Grid>

            <ControlTemplate.Triggers>
                <DataTrigger Binding="{Binding Edition}" Value="0">
                    <Setter TargetName="Content" Property="Template" Value="{StaticResource ZeroEditionBookTemplate}"/>
                </DataTrigger>
                <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                    <BeginStoryboard x:Name="OpenBookAnimation" Storyboard="{StaticResource OpenBookViewerAnim}" />
                </EventTrigger>
                <EventTrigger RoutedEvent="FrameworkElement.Unloaded">
                    <StopStoryboard BeginStoryboardName="OpenBookAnimation" />
                </EventTrigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
    </ls:UIWidget.Template>

    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Loaded">
            <ls:LSPlaySound Sound="UI_Gameplay_Book_Open"/>
        </b:EventTrigger>
        <b:EventTrigger EventName="Unloaded">
            <ls:LSPlaySound Sound="UI_Gameplay_Book_Close"/>
        </b:EventTrigger>
    </b:Interaction.Triggers>
</ls:UIWidget>
