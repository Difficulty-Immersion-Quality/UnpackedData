<ls:UIWidget x:Name="CharacterPanel_c"
             ls:UIWidget.ContextName="CharacterPanels"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ls="clr-namespace:ls;assembly=Code"
             xmlns:noesis="clr-namespace:NoesisGUIExtensions;assembly=Noesis.GUI.Extensions"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:System="clr-namespace:System;assembly=mscorlib"
			 mc:Ignorable="d"
             d:DataContext="{d:DesignInstance {x:Type ls:DCCharacterPanels}, IsDesignTimeCreatable=True}"
             FocusDown="UIDown" FocusLeft="UILeft" FocusRight="UIRight" FocusUp="UIUp"
             d:DesignHeight="2160" d:DesignWidth="3840"
             CanCacheFocusSurroundingElements="False" ls:TooltipExtender.Owner="{Binding CurrentPlayer.SelectedCharacter}"
             Template="{StaticResource PreloadedCharacterPanel_c}">

    <ls:AttachedProperties.AutomationId>
        <MultiBinding StringFormat="{}CharacterPanel_c_{0}">
            <Binding Path="(ls.UICanvas.CanvasLayout)" RelativeSource="{RelativeSource Mode=Self}"/>
        </MultiBinding>
    </ls:AttachedProperties.AutomationId>

    <b:Interaction.Triggers>
        <b:EventTrigger EventName="WidgetClosing">
            <b:ChangePropertyAction TargetObject="{Binding CurrentPlayer.SelectedCharacter.Equipment}" PropertyName="UseProficiencyFilter" Value="False"/>
            <b:InvokeCommandAction Command="{Binding HideAvailableEquipmentCommand}" CommandParameter="{Binding CurrentPlayer.SelectedCharacter.Equipment}"/>
            <b:InvokeCommandAction Command="{Binding ClearFilterCommand}"/>
        </b:EventTrigger>
        <b:EventTrigger EventName="FocusChanged">
            <ls:LSPlaySound Sound="UI_Shared_Hover" />
        </b:EventTrigger>
        <b:EventTrigger EventName="Loaded">
            <ls:LSPlaySound Sound="UI_HUD_Inventory_Open"/>
        </b:EventTrigger>

        <b:EventTrigger EventName="Unloaded">
            <ls:LSPlaySound Sound="UI_HUD_Inventory_Close"/>
        </b:EventTrigger>
    </b:Interaction.Triggers>

</ls:UIWidget>