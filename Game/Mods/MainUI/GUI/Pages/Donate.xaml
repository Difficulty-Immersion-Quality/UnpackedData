<ls:UIWidget ls:UIWidget.ContextName="Donate"
             x:Name="Donate"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ls="clr-namespace:ls;assembly=Code"
             xmlns:System="clr-namespace:System;assembly=mscorlib"
    		 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:noesis="clr-namespace:NoesisGUIExtensions;assembly=Noesis.GUI.Extensions"
			 mc:Ignorable="d"
             d:DesignWidth="3840" d:DesignHeight="2160"
             d:DataContext="{d:DesignInstance Type=ls:DCDonate}"
             ls:UIWidget.Focusable="False" ls:TooltipExtender.Owner="{Binding CurrentPlayer.SelectedCharacter}">

    <ls:UIWidget.Style>
        <Style TargetType="ls:UIWidget" BasedOn="{StaticResource {x:Type ls:UIWidget}}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ls:UIWidget">
                        <ControlTemplate.Resources>
                            <System:Double x:Key="ScaledNewSlotSize">104</System:Double>
                        </ControlTemplate.Resources>
                        <Border Background="{StaticResource LS_PopUpBackgroundColor}" x:Name="RootDonatePanel">

                            <ls:LSNineSliceImage IconWidth="480" Slices="48 40 48 200" VerticalAlignment="Center" VerticalContentAlignment="Top" HorizontalAlignment="Center" Panel.ZIndex="1" ImageSource="pack://application:,,,/Core;component/Assets/Container/container_background.png" Padding="22" MinWidth="660">
                                <ls:LSNineSliceImage.Resources>
                                    <System:Double x:Key="InventoryWidth">555</System:Double>
                                </ls:LSNineSliceImage.Resources>

                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="800"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" Text="{Binding Source='h5a5655c8geaa1g4455g8534g7a7ac2f7815d', Converter={StaticResource TranslatedStringConverter}}" MaxWidth="580" TextWrapping="Wrap" TextAlignment="Center" FontSize="{StaticResource ScaledBigFontSize}"/>

                                    <ls:LSButton Style="{StaticResource SmallCloseButton}" x:Name="CloseBtn" Grid.Row="0" Command="{Binding CustomEvent}" CommandParameter="CloseWidget" VerticalAlignment="Top" HorizontalAlignment="Right" SoundID="UI_Shared_Cancel"/>

                                    <ListBox Grid.Row="1" Name="InventoryContainer" ItemsSource="{Binding DonateSourceInventories}" Margin="0,0,0,12" Tag="">
                                        <ListBox.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <VirtualizingStackPanel Background="Transparent"/>
                                            </ItemsPanelTemplate>
                                        </ListBox.ItemsPanel>
                                        <ListBox.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel x:Name="rootElement" Margin="0,0,0,24">
                                                    <b:Interaction.Behaviors>
                                                        <ls:InventoriesMergerBehavior x:Name="PlayerInventoriesMerged" Inventories="{Binding TradeInventories}"/>
                                                        <ls:CollectionFilterBehavior x:Name="PlayerInventoryFiltered" ItemsSource="{Binding InventoriesSlots, ElementName=PlayerInventoriesMerged}" Predicate="{Binding TradeInventories[0].TabFiltersPredicate}"/>
                                                        <ls:CollectionFilterBehavior x:Name="PlayerInventoryDonationsFiltered" ItemsSource="{Binding FilteredItems, ElementName=PlayerInventoryFiltered}" Predicate="{Binding DataContext.NonDonatedItemsPredicate, ElementName=Donate}"/>
                                                        <ls:CollectionSortBehavior x:Name="PlayerInventoryFilteredSorted" ItemsSource="{Binding FilteredItems, ElementName=PlayerInventoryDonationsFiltered}" Comparer="{Binding DataContext.CurrentPlayer.SelectedCharacter.TradeSortComparer, ElementName=Donate}"/>
                                                    </b:Interaction.Behaviors>
                                                    <TextBlock Text="{Binding OwnerObject.Name}" TextAlignment="Center" TextWrapping="Wrap" Width="{StaticResource InventoryWidth}" Margin="0,0,0,6"/>

                                                    <ListBox Name="Inventory" ItemsSource="{Binding Path=(b:Interaction.Behaviors)[3].SortedItems, ElementName=rootElement}" SelectionMode="Single" Background="Transparent" Template="{StaticResource MinimalItemsControlTemplate}">
                                                        <ListBox.ItemTemplate>
                                                            <DataTemplate DataType="ls:VMInventorySlot">
                                                                <Grid>
                                                                    <ContentPresenter x:Name="Presenter" Content="{Binding Object}" Style="{StaticResource Style.GameObject}"  />
                                                                    <Control Template="{StaticResource EquippedItemIconIndicator}" DataContext="{Binding Object}" Height="42" Width="54" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="0,0,0,8"/>
                                                                </Grid>
                                                                <DataTemplate.Triggers>
                                                                    <DataTrigger Binding="{Binding Object.IsEquipment}" Value="True">
                                                                        <Setter TargetName="Presenter" Property="ContentTemplate" Value="{StaticResource Template.ItemEquipment}"/>
                                                                    </DataTrigger>
                                                                </DataTemplate.Triggers>
                                                            </DataTemplate>
                                                        </ListBox.ItemTemplate>
                                                        <ListBox.ItemsPanel>
                                                            <ItemsPanelTemplate>

                                                                <ls:LSGrid x:Name="ContainerGrid" Columns="6" Rows="2" Width="{StaticResource InventoryWidth}" ContainerData="{Binding LootInventory}" Style="{StaticResource InventoryGridStyle}" DisableScrolling="True" AutoIndex="True"/>
                                                            
                                                            </ItemsPanelTemplate>
                                                        </ListBox.ItemsPanel>
                                                        <ListBox.ItemContainerStyle>
                                                            <Style TargetType="ListBoxItem" BasedOn="{StaticResource BaseInvContainerItemStyle}">
                                                                <Setter Property="ContextMenuService.IsEnabled" Value="False"/>
                                                                <Setter Property="ls:ContextMenuService.Context" Value=""/>
                                                            </Style>
                                                        </ListBox.ItemContainerStyle>
                                                        <b:Interaction.Triggers>
                                                            <b:PropertyChangedTrigger Binding="{Binding ElementName=InventoryContainer, Path=Tag}">
                                                                <b:Interaction.Behaviors>
                                                                    <b:ConditionBehavior>
                                                                        <b:ConditionalExpression>
                                                                            <b:ComparisonCondition LeftOperand="{Binding  IsMouseOver,RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Operator="NotEqual" RightOperand="True" />
                                                                        </b:ConditionalExpression>
                                                                    </b:ConditionBehavior>
                                                                </b:Interaction.Behaviors>
                                                                <b:ChangePropertyAction TargetName="Inventory" PropertyName="SelectedIndex" Value="-1"/>
                                                            </b:PropertyChangedTrigger>
                                                            <b:EventTrigger EventName="SelectionChanged">
                                                                <b:Interaction.Behaviors>
                                                                    <b:ConditionBehavior>
                                                                        <b:ConditionalExpression>
                                                                            <b:ComparisonCondition LeftOperand="{Binding SelectedIndex, ElementName=Inventory}" Operator="NotEqual" RightOperand="-1" />
                                                                        </b:ConditionalExpression>
                                                                    </b:ConditionBehavior>
                                                                </b:Interaction.Behaviors>
                                                                <b:ChangePropertyAction TargetName="InventoryContainer" PropertyName="Tag" Value="{Binding SelectedItems, ElementName=Inventory}"/>
                                                            </b:EventTrigger>
                                                        </b:Interaction.Triggers>
                                                    </ListBox>
                                                </StackPanel>
                                            </DataTemplate>
                                        </ListBox.ItemTemplate>
                                        <ListBox.ItemContainerStyle>
                                            <Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </Style>
                                        </ListBox.ItemContainerStyle>
                                    </ListBox>

                                    <ls:LSButton x:Name="DonateBtn" Grid.Row="2" FontFamily="{StaticResource DefaultFont}" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,24" Style="{StaticResource BigBlueButtonStyle}" Command="{Binding DonateItemsCommand}" CommandParameter="{Binding Tag, ElementName=InventoryContainer}" IsEnabled="{Binding Tag.Count, ElementName=InventoryContainer, Converter={StaticResource GreaterThanConverter}, ConverterParameter='0', FallbackValue=False}" Content="{Binding Source='h613a8245g0881g470bgb9a1ga060c50db2ed', Converter={StaticResource TranslatedStringConverter}}"/>
                                </Grid>
                            </ls:LSNineSliceImage>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </ls:UIWidget.Style>

    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Loaded">
            <ls:LSPlaySound Sound="UI_HUD_Trade_Open"/>
        </b:EventTrigger>
        <b:EventTrigger EventName="Unloaded">
            <ls:LSPlaySound Sound="UI_HUD_Trade_Close"/>
        </b:EventTrigger>
    </b:Interaction.Triggers>
</ls:UIWidget>