<ls:UIWidget x:Name="Options_c"
             ls:UIWidget.ContextName="GameOptions"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ls="clr-namespace:ls;assembly=Code"
             xmlns:noesis="clr-namespace:NoesisGUIExtensions;assembly=Noesis.GUI.Extensions"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="2160" d:DesignWidth="3840"
             ls:UIWidget.Focusable="False"
             FocusDown="UIDown" FocusUp="UIUp"
             ls:MoveFocus.FocusMovementMode="MainAxisTunnel"
             CanCacheFocusSurroundingElements="False"
             d:DataContext="{d:DesignInstance ls:DCOptionsBase}"
             ls:AttachedProperties.AutomationId ="{Binding (ls.UICanvas.CanvasLayout), RelativeSource={RelativeSource Mode=Self}, StringFormat='{}Options_c_{0}'}">

    <ls:UIWidget.Resources>
        <ResourceDictionary Source="/Core;component/Library/OptionTemplates_c.xaml"/>
    </ls:UIWidget.Resources>

    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Loaded">
            <ls:SetMoveFocusAction TargetName="Options_c"/>
        </b:EventTrigger>
    </b:Interaction.Triggers>

    <Control Template="{StaticResource PreviewOptionsTemplate}"/>
    
</ls:UIWidget>