<ls:UIWidget
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ls="clr-namespace:ls;assembly=Code"
             xmlns:noesis="clr-namespace:NoesisGUIExtensions;assembly=Noesis.GUI.Extensions"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:System="clr-namespace:System;assembly=mscorlib"
			 mc:Ignorable="d"
             d:DesignHeight="2160" d:DesignWidth="3840">

    <ls:UIWidget.Resources>
        <ResourceDictionary Source="/Core;component/Library/JournalTemplates.xaml"/>
    </ls:UIWidget.Resources>

    <!-- Outer full screen grid adding black borders left and right if in non 16:9 ratio -->
    <Grid Background="Black">
        <Grid HorizontalAlignment="Center" VerticalAlignment="Top">
            <ContentControl Template="{StaticResource JournalHeader}"/>
            <Grid x:Name="body" Background="Yellow" VerticalAlignment="Top" Height="Auto" Margin="0,132,0,0">
                <Border Height="800"/>
            </Grid>
        </Grid>
    </Grid>

</ls:UIWidget>