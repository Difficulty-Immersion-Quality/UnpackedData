<ls:UIWidget x:Name="DMSettingsMoreInfo"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ls="clr-namespace:ls;assembly=Code"
             xmlns:System="clr-namespace:System;assembly=mscorlib"
             xmlns:noesis="clr-namespace:NoesisGUIExtensions;assembly=Noesis.GUI.Extensions"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance {x:Type ls:Widget}, IsDesignTimeCreatable=True}"
             d:DesignHeight="2160" d:DesignWidth="3840">

    <ls:UIWidget.Template>
        <ControlTemplate>
            <ControlTemplate.Resources>
                <BitmapImage x:Key="FadeFooterImage" UriSource="pack://application:,,,/Core;component/Assets/DMSettings/fade_footer.png"/>

                <Style TargetType="TextBlock">
                    <Setter Property="TextWrapping" Value="Wrap"/>
                    <Setter Property="MaxWidth" Value="1400"/>
                    <Setter Property="FontSize" Value="{StaticResource ScaledIntermediateFontSize}"/>
                    <Setter Property="Foreground" Value="{StaticResource LS_accent75TxtColor}"/>
                </Style>
            </ControlTemplate.Resources>

            <Grid Background="{StaticResource LS_PopUpBackgroundColor}">
                <Image Source="{StaticResource FadeFooterImage}" VerticalAlignment="Bottom" HorizontalAlignment="Center" Stretch="None"/>

                <ls:LSNineSliceImage Style="{StaticResource DefaultTooltip9Slice}" HorizontalAlignment="Center" VerticalAlignment="Center" Width="1400" MinHeight="1000" Padding="80,60,80,48">
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="{Binding Source='hc84c257fg484bg490cgab0dg6026185bdebe', Converter={StaticResource TranslatedStringConverter}}" HorizontalAlignment="Center" FontSize="{StaticResource ScaledTitleFontSize}" Foreground="{StaticResource LS_tint100}" Margin="0,0,0,20"/>
                        <ls:LSScrollViewer MaxHeight="1400" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" Margin="0,0,0,20" BoundScrollUpEvent="UIScrollUp" BoundScrollDownEvent="UIScrollDown" GamepadScrollMultiplier="400" Template="{StaticResource MinimalisticScrollViewerTemplate}">
                            <TextBlock ls:TextBlockFormatter.SourceText="{Binding Source='h98f4af19g94b7g4821g9043g5e89b45bd8f6', Converter={StaticResource TranslatedStringConverter}}" HorizontalAlignment="Left" Width="1220"/>
                        </ls:LSScrollViewer>
                    </StackPanel>
                </ls:LSNineSliceImage>

                <StackPanel x:Name="ButtonPrompts" Style="{StaticResource ButtonHint.Container.Center}">
                    <ls:LSButton x:Name="CancelButton" 
                                 BoundEvent="UICancel" 
                                 Command="{Binding CustomEvent, Mode=OneTime}" 
                                 CommandParameter="CloseWidget" 
                                 Tag="{Binding Source='hfe928c0dgb014g4eb2g8c52g6ed43b026e20', Converter={StaticResource TranslatedStringConverter}}" 
                                 ContentTemplate="{StaticResource ControllerButtonHint}" 
                                 Content="{Binding CurrentPlayer.UIData.InputEvents, ConverterParameter=UICancel, Converter={StaticResource FindInputEventConverter}}"/>
                </StackPanel>
            </Grid>
        </ControlTemplate>
    </ls:UIWidget.Template>
</ls:UIWidget>