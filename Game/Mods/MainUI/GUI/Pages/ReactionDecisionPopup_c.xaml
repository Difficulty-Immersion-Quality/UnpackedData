<ls:UIWidget x:Name="ReactionDecisionPopup_c"
             ls:UIWidget.ContextName="ReactionDecision"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:System="clr-namespace:System;assembly=mscorlib"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:ls="clr-namespace:ls;assembly=Code"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"   
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance {x:Type ls:DCReactionDecision}, IsDesignTimeCreatable=True}"
             xmlns:noesis="clr-namespace:NoesisGUIExtensions;assembly=Noesis.GUI.Extensions"
             FocusDown="UIDown" FocusLeft="UILeft" FocusRight="UIRight" FocusUp="UIUp"
             d:DesignWidth="3840" d:DesignHeight="2160">
    <ls:UIWidget.Template>
        <ControlTemplate>
            <ControlTemplate.Resources>
                <ResourceDictionary>
                    <System:Double x:Key="MaxReactionWidth">800</System:Double>
                    <Thickness x:Key="ReactionMargin">20,0,60,8</Thickness>

                    <Storyboard x:Key="AnimateInPanelFromLeft" FillBehavior="Stop">
                        <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Opacity)" Storyboard.TargetName="SideDock">
                            <EasingDoubleKeyFrame KeyTime="0" Value="0"/>
                            <EasingDoubleKeyFrame KeyTime="0:0:0.30" Value="1">
                                <EasingDoubleKeyFrame.EasingFunction>
                                    <CubicEase EasingMode="EaseOut"/>
                                </EasingDoubleKeyFrame.EasingFunction>
                            </EasingDoubleKeyFrame>
                        </DoubleAnimationUsingKeyFrames>

                        <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)" Storyboard.TargetName="SideDock">
                            <EasingDoubleKeyFrame KeyTime="0:0:0.0" Value="-200"/>
                            <EasingDoubleKeyFrame KeyTime="0:0:0.30" Value="0">
                                <EasingDoubleKeyFrame.EasingFunction>
                                    <CubicEase EasingMode="EaseOut"/>
                                </EasingDoubleKeyFrame.EasingFunction>
                            </EasingDoubleKeyFrame>
                        </DoubleAnimationUsingKeyFrames>
                    </Storyboard>

                    <Storyboard x:Key="AnimateInPanelFromRight" FillBehavior="Stop">
                        <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Opacity)"  Storyboard.TargetName="SideDock">
                            <EasingDoubleKeyFrame KeyTime="0" Value="0"/>
                            <EasingDoubleKeyFrame KeyTime="0:0:0.30" Value="1">
                                <EasingDoubleKeyFrame.EasingFunction>
                                    <CubicEase EasingMode="EaseOut"/>
                                </EasingDoubleKeyFrame.EasingFunction>
                            </EasingDoubleKeyFrame>
                        </DoubleAnimationUsingKeyFrames>

                        <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)" Storyboard.TargetName="SideDock">
                            <EasingDoubleKeyFrame KeyTime="0:0:0.0" Value="200"/>
                            <EasingDoubleKeyFrame KeyTime="0:0:0.30" Value="0">
                                <EasingDoubleKeyFrame.EasingFunction>
                                    <CubicEase EasingMode="EaseOut"/>
                                </EasingDoubleKeyFrame.EasingFunction>
                            </EasingDoubleKeyFrame>
                        </DoubleAnimationUsingKeyFrames>
                    </Storyboard>

                    <Storyboard x:Key="GradientChaser" RepeatBehavior="Forever">
                        <PointAnimationUsingKeyFrames Storyboard.TargetProperty="(Shape.Stroke).(LinearGradientBrush.StartPoint)" Storyboard.TargetName="SpinnerIcon">
                            <EasingPointKeyFrame KeyTime="0:0:0.25" Value="0.855,0.148"/>
                            <EasingPointKeyFrame KeyTime="0:0:0.5" Value="0.852,0.855"/>
                            <EasingPointKeyFrame KeyTime="0:0:0.75" Value="0.148,0.855"/>
                            <EasingPointKeyFrame KeyTime="0:0:1" Value="0.144,0.149"/>
                            <EasingPointKeyFrame KeyTime="0:0:1.25" Value="0,0"/>
                        </PointAnimationUsingKeyFrames>
                        <PointAnimationUsingKeyFrames Storyboard.TargetProperty="(Shape.Stroke).(LinearGradientBrush.EndPoint)" Storyboard.TargetName="SpinnerIcon">
                            <EasingPointKeyFrame KeyTime="0:0:0.25" Value="0.145,0.852"/>
                            <EasingPointKeyFrame KeyTime="0:0:0.5" Value="0.148,0.145"/>
                            <EasingPointKeyFrame KeyTime="0:0:0.75" Value="0.852,0.145"/>
                            <EasingPointKeyFrame KeyTime="0:0:1" Value="0.856,0.851"/>
                            <EasingPointKeyFrame KeyTime="0:0:1.25" Value="0,1"/>
                        </PointAnimationUsingKeyFrames>
                    </Storyboard>

                    <DataTemplate x:Key="SinglePlayerEntry" DataType="{x:Type ls:Player}">
                        <ItemsControl x:Name="InterruptorsList" ItemsSource="{Binding FilteredItems, ElementName=charactersFiltered}" HorizontalAlignment="Center" ls:AttachedProperties.InheritedTag="{Binding .}"
                                        AlternationCount="{Binding FilteredItems.Count, ElementName=charactersFiltered}">
                        <b:Interaction.Behaviors>
                            <ls:CollectionFilterBehavior x:Name="charactersFiltered" ItemsSource="{Binding DataContext.Interruptors, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ScrollViewer}}"
                                                                Predicate="{Binding DataContext.Data.PredicateInterruptorOwnerUser, RelativeSource={RelativeSource AncestorType=ls:UIWidget}}"
                                                                CompareParam="{Binding .}"/>
                        </b:Interaction.Behaviors>
                        <b:Interaction.Triggers>
                                <b:EventTrigger EventName="Loaded">
                                    <ls:SetMoveFocusAction TargetName="ReactionDecisionPopup_c"/>
                                </b:EventTrigger>
                            </b:Interaction.Triggers>
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.Template>
                                <ControlTemplate TargetType="ItemsControl">
                                    <ItemsPresenter/>
                                </ControlTemplate>
                            </ItemsControl.Template>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="ls:VMInterruptor">
                                    <StackPanel x:Name="InterruptorRoot" MinHeight="10" MinWidth="10" Orientation="Horizontal" ls:TooltipExtender.Owner="{Binding Character}">
                                        <Grid x:Name="HealthbarPortrait" Style="{StaticResource LargePortrait}" IsHitTestVisible="False" VerticalAlignment="Top">
                                            <ContentPresenter x:Name="portrait" ContentTemplate="{StaticResource CharacterPortraitTemplate}" Content="{Binding Character}"/>
                                            <Image x:Name="Frame" Source="{StaticResource FrameImg_d}" Stretch="Uniform" IsHitTestVisible="False"/>
                                        </Grid>
                                        <StackPanel x:Name="InterruptorList" Width="{StaticResource MaxReactionWidth}" Margin="{StaticResource ReactionMargin}">
                                            <b:Interaction.Behaviors>
                                                    <ls:CollectionSortBehavior x:Name="SortedInterruptDecisions" ItemsSource="{Binding Path=InterruptDecisions}" Comparer="{Binding DecisionComparer}"/>
                                            </b:Interaction.Behaviors>
                                            <ItemsControl ItemsSource="{Binding SortedItems, ElementName=SortedInterruptDecisions}" HorizontalAlignment="Stretch" ItemTemplate="{StaticResource InterruptDecisionItemTemplate}">
                                                <ItemsControl.Template>
                                                    <ControlTemplate TargetType="ItemsControl">
                                                        <ItemsPresenter/>
                                                    </ControlTemplate>
                                                </ItemsControl.Template>
                                            </ItemsControl>
                                         </StackPanel>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding Path=FilteredItems.Count,ElementName=charactersFiltered}" Value="0">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>

                    <DataTemplate x:Key="PlayerEntry" DataType="{x:Type ls:Player}">
                        <DataTemplate.Resources>
                            <System:Double x:Key="LineThickness">4</System:Double>
                            <System:Double x:Key="LineRadius">32</System:Double>
                        </DataTemplate.Resources>
                        <Grid Margin="0,0,0,30">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Rectangle Grid.RowSpan="2" Margin="82,44,0,0" StrokeThickness="{StaticResource LineThickness}" StrokeDashArray="6 3" Stroke="{StaticResource LS_guestTxtColor}" RadiusX="{StaticResource LineRadius}" RadiusY="{StaticResource LineRadius}">
                                <Rectangle.OpacityMask>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                        <GradientStop Offset="0" Color="#FF000000" />
                                        <GradientStop Offset="1" Color="#00000000" />
                                    </LinearGradientBrush>
                                </Rectangle.OpacityMask>
                                <Rectangle.Fill>
                                    <SolidColorBrush Color="{StaticResource guestColor}" Opacity="0.65"/>
                                </Rectangle.Fill>
                            </Rectangle>
                            <StackPanel x:Name="PlayerHeader" Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center">
                                <Border Background="{Binding CommunityIcon}" Height="90" Width="90" BorderThickness="4" CornerRadius="8"  BorderBrush="{StaticResource LS_guestTxtColor}"/>
                                <TextBlock Margin="20,0,0,0" FontSize="{StaticResource ScaledMediumFontSize}" Foreground="{StaticResource LS_guestTxtColor}"
                                           Text="{Binding Name}" VerticalAlignment="Center"/>
                            </StackPanel>
                            <ContentPresenter x:Name="ListControl" Grid.Row="1" Content="{Binding .}" ContentTemplate="{StaticResource SinglePlayerEntry}" IsEnabled="False" Margin="0,0,0,30"/>
                        </Grid>
                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding Visibility, ElementName=ListControl}" Value="Collapsed">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>

                    <Style x:Key="TitleParamRun" TargetType="ls:LSRun" BasedOn="{StaticResource CombatLog.Tooltip.ParamRun}">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Type}" Value="DiceValue">
                                <Setter Property="Foreground" Value="{StaticResource LS_specialTxtColor}"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>

                    <Thickness x:Key="SinglePlayerMargin">200,0,0,0</Thickness>

                    <BitmapImage x:Key="CastBarBackground" UriSource="pack://application:,,,/Core;component/Assets/TargetInfo/target_bar_cast.png"/>
                </ResourceDictionary>
            </ControlTemplate.Resources>

            <Grid x:Name="Root" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Visibility="{Binding FilteredItems.Count, ElementName=CustomEventsCollection, Converter={StaticResource CountToVisibilityConverter}}"
                  IsEnabled="{Binding IsVisible, RelativeSource={RelativeSource Self}}">

                <b:Interaction.Behaviors>
                    <ls:CollectionFilterBehavior 
                        x:Name="OwnerInterruptors" 
                        ItemsSource="{Binding Data.InterruptEvents[0].Interruptors}"
                        Predicate="{Binding Data.PredicateInterruptorOwnerUser}"
                        CompareParam="{Binding CurrentPlayer}"
                    />
                    
                    <ls:CollectionFilterBehavior
                        x:Name="OtherLocalInterruptors"    
                        ItemsSource="{Binding Data.InterruptEvents[0].Interruptors}"
                        Predicate="{Binding OtherInterruptorsOnSamePeerPredicate}"
                        CompareParam="{Binding CurrentPlayer}"
                    />

                    <ls:CollectionFilterBehavior
                        x:Name="OtherRemoteInterruptors"    
                        ItemsSource="{Binding Data.InterruptEvents[0].Interruptors}"
                        Predicate="{Binding OtherInterruptorsOnDifferentPeerPredicate}"
                        CompareParam="{Binding CurrentPlayer}"
                    />
                </b:Interaction.Behaviors>

                <StackPanel
                    x:Name="WaitingSpinner"
                    Orientation="Horizontal"
                    VerticalAlignment="Bottom" 
                    HorizontalAlignment="Center"
                    Margin="0,0,0,200"
                    Visibility="{Binding ElementName=ReactionPanel, Path=Visibility, Converter={StaticResource InvertVisibilityConverter}}"
                >
                    <ls:LSNineSliceImage 
                        x:Name="SourceSpell" 
                        Slices="180,40"
                        Padding="120,40"
                        MaxWidth="1200" 
                        ImageSource="{StaticResource CastBarBackground}"
                        DataContext="{Binding Data.InterruptEvents[0].SourceSpell}"
                     >
                        <Grid>
                            <DockPanel LastChildFill="False">

                                <Ellipse 
                                    x:Name="SpinnerIcon" 
                                    DockPanel.Dock="Left" 
                                    VerticalAlignment="Center" 
                                    Width="106" 
                                    Height="106"
                                    StrokeThickness="4"
                                    Fill="{Binding Icon}"
                                    Visibility="{Binding ., Converter={StaticResource NullToCollapsedConverter}}"
                                >

                                    <Ellipse.Stroke>
                                        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                            <GradientStop Color="White" Offset="0"/>
                                            <GradientStop Color="Transparent" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Ellipse.Stroke>
                                    <Ellipse.Triggers>
                                        <EventTrigger RoutedEvent="FrameworkElement.Unloaded">
                                            <StopStoryboard BeginStoryboardName="BeginStoryboard_Spinner"/>
                                        </EventTrigger>
                                    </Ellipse.Triggers>
                                </Ellipse>

                                <TextBlock 
                                    x:Name="WaitPlayerLabel" 
                                    DockPanel.Dock="Top" 
                                    HorizontalAlignment="Stretch" 
                                    VerticalAlignment="Center"
                                    TextAlignment="Center" 
                                    Margin="20,20,20,0" 
                                    FontSize="{StaticResource ScaledDefaultFontSize}" 
                                    Foreground="{StaticResource LS_extraspecialTxtColor}" 
                                    TextWrapping="Wrap"
                                />
                            </DockPanel>
                        </Grid>

                    </ls:LSNineSliceImage>
                </StackPanel>

                <Grid x:Name="ReactionPanel">
                    <b:Interaction.Behaviors>
                        <ls:CollectionFilterBehavior x:Name="CustomEventsCollection" ItemsSource="{Binding Data.InterruptEvents}" Predicate="{Binding HasSelectedCharacterPredicate}"/>
                        <ls:CollectionContainsItemBehavior x:Name="HasAnybodyMadeASelection" ItemSource="{Binding DataContext.CurrentPlayer, RelativeSource={RelativeSource AncestorType=ls:UIWidget}}" CollectionSource="{Binding Data.InterruptEvents[0].PlayersWhoHaveDecision}"/>
                    </b:Interaction.Behaviors>
                    
                    <Rectangle x:Name="SideShading" HorizontalAlignment="Left" VerticalAlignment="Stretch" Fill="{StaticResource LeftSideShadingBrush}" Width="1920"/>

                    <DockPanel x:Name="SideDock" HorizontalAlignment="Left" VerticalAlignment="Stretch" LastChildFill="False" MaxWidth="1280" Margin="60,0" Background="{StaticResource CenterShadingBrush}">

                        <DockPanel.RenderTransform>
                            <TranslateTransform/>
                        </DockPanel.RenderTransform>

                        <b:Interaction.Triggers>
                            <b:EventTrigger EventName="Loaded">
                                <ls:SetMoveFocusAction TargetName="ReactionDecisionPopup_c"/>
                            </b:EventTrigger>
                            <b:EventTrigger EventName="Loaded">
                                <b:Interaction.Behaviors>
                                    <b:ConditionBehavior>
                                        <b:ConditionalExpression>
                                            <b:ComparisonCondition LeftOperand="{Binding Layout}" Operator="Equal" RightOperand="Common"/>
                                        </b:ConditionalExpression>
                                    </b:ConditionBehavior>
                                </b:Interaction.Behaviors>
                                <b:ControlStoryboardAction ControlStoryboardOption="Play" Storyboard="{StaticResource AnimateInPanelFromLeft}"/>
                            </b:EventTrigger>
                            <b:EventTrigger EventName="Loaded">
                                <b:Interaction.Behaviors>
                                    <b:ConditionBehavior>
                                        <b:ConditionalExpression>
                                            <b:ComparisonCondition LeftOperand="{Binding Layout}" Operator="Equal" RightOperand="Left"/>
                                        </b:ConditionalExpression>
                                    </b:ConditionBehavior>
                                </b:Interaction.Behaviors>
                                <b:ControlStoryboardAction ControlStoryboardOption="Play" Storyboard="{StaticResource AnimateInPanelFromLeft}"/>
                            </b:EventTrigger>
                            <b:EventTrigger EventName="Loaded">
                                <b:Interaction.Behaviors>
                                    <b:ConditionBehavior>
                                        <b:ConditionalExpression>
                                            <b:ComparisonCondition LeftOperand="{Binding Layout}" Operator="Equal" RightOperand="Right"/>
                                        </b:ConditionalExpression>
                                    </b:ConditionBehavior>
                                </b:Interaction.Behaviors>
                                <b:ControlStoryboardAction ControlStoryboardOption="Play" Storyboard="{StaticResource AnimateInPanelFromRight}"/>
                            </b:EventTrigger>
                        </b:Interaction.Triggers>

                        <ItemsControl x:Name="EventsList" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" ItemsSource="{Binding FilteredItems, ElementName=CustomEventsCollection}"
                                      AlternationCount="{Binding FilteredItems.Count, ElementName=CustomEventsCollection}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <Grid IsItemsHost="True"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.Template>
                                <ControlTemplate TargetType="ItemsControl">
                                    <ItemsPresenter/>
                                </ControlTemplate>
                            </ItemsControl.Template>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type ls:VMInterruptEvent}">
                                    <StackPanel x:Name="EventRoot" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                                        <TextBlock FontSize="{StaticResource ScaledTitleFontSize}" Foreground="{StaticResource LS_extraspecialTxtColor}" HorizontalAlignment="Center" TextAlignment="Center" Margin="0,180,0,0"
                                                   Text="{Binding Source='h43f4dbfeg0d62g49eegbecagad62630e032d', Converter={StaticResource TranslatedStringConverter}}"/>
                                        <TextBlock Name="ReactionTypeText" HorizontalAlignment="Stretch" TextWrapping="Wrap" TextAlignment="Center" Margin="60,0,60,20">
                                            <b:Interaction.Behaviors>
                                                <ls:CtxTransStringRunGeneratorBehavior Source="{Binding Description}" ParamRunStyle="{StaticResource TitleParamRun}"/>
                                            </b:Interaction.Behaviors>
                                        </TextBlock>

                                        <ls:LSNineSliceImage x:Name="SourceSpell" Width="{StaticResource MaxReactionWidth}" MinHeight="110" HorizontalAlignment="Center" Margin="120,0,0,0"
                                                             ImageSource="{StaticResource ButtonBg}" Slices="24,40,24,24" 
                                                             DataContext="{Binding SourceSpell}" Focusable="True" ls:MoveFocus.Focusable="True"
                                                             FocusVisualStyle="{StaticResource Style.FocusVisualStyle}"
                                                             ToolTipService.Placement="Right" ToolTipService.HorizontalOffset="20"
                                                             Visibility="{Binding ., Converter={StaticResource NullToCollapsedConverter}}">
                                            <ls:LSNineSliceImage.ToolTip>
                                                <ls:LSTooltip Content="{Binding DataContext.SourceSpell, RelativeSource={RelativeSource Mode=TemplatedParent}}"
                                                              ls:TooltipExtender.Owner="{Binding DataContext.SourceCharacter, RelativeSource={RelativeSource Mode=TemplatedParent}}"/>
                                            </ls:LSNineSliceImage.ToolTip>
                                            <Grid>

                                                <DockPanel Margin="20,20" LastChildFill="False">
                                                    <Rectangle x:Name="Icon" DockPanel.Dock="Left" VerticalAlignment="Center" Width="106" Height="106" Fill="{Binding Icon}"/>
                                                    <TextBlock x:Name="Name" DockPanel.Dock="Top" HorizontalAlignment="Stretch" TextAlignment="Left" Margin="20,0,20,0" 
                                                               FontSize="{StaticResource ScaledDefaultFontSize}" Text="{Binding Name}"
                                                               Foreground="{StaticResource LS_accent75TxtColor}" TextWrapping="Wrap"/>
                                                </DockPanel>
                                            </Grid>
                                        </ls:LSNineSliceImage>

                                        <TextBlock Name="ReactionCTAText" FontSize="{StaticResource ScaledMediumFontSize}" Foreground="{StaticResource LS_accent25TxtColor}" HorizontalAlignment="Center" TextAlignment="Center" Margin="0,40"
                                                   Text="{Binding Source='hb49a2b0ag17d5g4171g9060gffc9684f2189', Converter={StaticResource TranslatedStringConverter}}"/>
                                        <ScrollViewer  HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" Height="1300">
                                            <StackPanel>
                                                <ContentPresenter x:Name="MainPlayer" ls:MoveFocus.FocusMovementMode="Closest" Margin="0,0,0,40" Content="{Binding DataContext.CurrentPlayer, ElementName=ReactionDecisionPopup_c}"
                                                                  ContentTemplate="{StaticResource SinglePlayerEntry}"/>
                                                <ItemsControl x:Name="MultiPlayerList" ItemsSource="{Binding DataContext.Data.Players, ElementName=ReactionDecisionPopup_c}" ItemTemplate="{StaticResource PlayerEntry}">
                                                    <ItemsControl.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                            <StackPanel Opacity="0.7"/>
                                                        </ItemsPanelTemplate>
                                                    </ItemsControl.ItemsPanel>
                                                    <ItemsControl.Template>
                                                        <ControlTemplate TargetType="ItemsControl">
                                                            <ItemsPresenter/>
                                                        </ControlTemplate>
                                                    </ItemsControl.Template>
                                                    <ItemsControl.ItemContainerStyle>
                                                        <Style TargetType="ContentPresenter">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Value="True">
                                                                    <DataTrigger.Binding>
                                                                        <MultiBinding Converter="{StaticResource EqualConverter}">
                                                                            <Binding Path="." />
                                                                            <Binding Path="DataContext.CurrentPlayer" ElementName="ReactionDecisionPopup_c"/>
                                                                        </MultiBinding>
                                                                    </DataTrigger.Binding>
                                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </ItemsControl.ItemContainerStyle>
                                                </ItemsControl>
                                            </StackPanel>
                                        </ScrollViewer>
                                    </StackPanel>
                                    <DataTemplate.Triggers>
                                        <DataTrigger Binding="{Binding ElementName=MainPlayer, Path=ActualHeight}" Value="0">
                                            <Setter TargetName="ReactionCTAText" Property="Text" Value="{Binding Source='h8c0332e0gc656g43aegafcegc7c66c6e3516', Converter={StaticResource TranslatedStringConverter}}"/>
                                            <Setter TargetName="ReactionCTAText" Property="Foreground" Value="{StaticResource LS_guestTxtColor}"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding (ls:MoveFocus.IsFocused), ElementName=SourceSpell}" Value="True">
                                            <Setter TargetName="Name" Property="Foreground" Value="{StaticResource LS_extraspecialTxtColor}"/>
                                            <Setter TargetName="SourceSpell" Property="ImageSource" Value="{StaticResource ButtonBg_h}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding DataContext.Layout, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ls:UIWidget}}" Value="Right">
                                            <Setter TargetName="Icon" Property="ToolTipService.Placement" Value="Left"/>
                                            <Setter TargetName="Icon" Property="ToolTipService.HorizontalOffset" Value="-20"/>
                                        </DataTrigger>
                                    </DataTemplate.Triggers>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                            <ItemsControl.ItemContainerStyle>
                                <Style TargetType="ContentPresenter">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <Trigger Property="ItemsControl.AlternationIndex" Value="0">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </ItemsControl.ItemContainerStyle>

                        </ItemsControl>
                    </DockPanel>

                    <!--Button prompts-->

                    <StackPanel x:Name="ButtonPrompts" Orientation="Horizontal" HorizontalAlignment="Left" Margin="60,0" DockPanel.Dock="Bottom">

                        <ls:LSButton x:Name="SelectBtn" Focusable="False" BoundEvent="UIAccept" Margin="{StaticResource ButtonHint.Margin}" VerticalAlignment="Bottom"
                                         IsEnabled="False"
                                         Content="{Binding CurrentPlayer.UIData.InputEvents, Converter={StaticResource FindInputEventConverter}, ConverterParameter='UIAccept'}"
                                         Tag="{Binding Source='hd0c08193ga7f2g4a7eg8225g63748c7b3096',Converter={StaticResource TranslatedStringConverter}}"
                                         ContentTemplate="{StaticResource ControllerButtonHint}"
                                         Command="{Binding FocusedElement.Command, ElementName=ReactionDecisionPopup_c}"
                                         CommandParameter="{Binding FocusedElement.CommandParameter, ElementName=ReactionDecisionPopup_c}"/>
                        <ls:LSButton x:Name="ConfirmReactionsBtn" Focusable="False" BoundEvent="UITakeAll" Margin="{StaticResource ButtonHint.Margin}" VerticalAlignment="Bottom" 
                                         IsEnabled="False"
                                         Content="{Binding CurrentPlayer.UIData.InputEvents, ConverterParameter=UITakeAll, Converter={StaticResource FindInputEventConverter}}"
                                         Tag="{Binding Source='h65579315gbc72g4628gb4e1gc765606c63f0', Converter={StaticResource TranslatedStringConverter}}"
                                         ContentTemplate="{StaticResource ControllerButtonHint}"
                                         Command="{Binding InterruptAcceptPlayerCommand}"
                                         CommandParameter="{Binding Data.InterruptEvents[0]}"/>
                        <ls:LSButton x:Name="ToggleTooltips" Focusable="False" BoundEvent="UIShowInfo" Margin="{StaticResource ButtonHint.Margin}" VerticalAlignment="Bottom"
                                         Content="{Binding CurrentPlayer.UIData.InputEvents, ConverterParameter=UIShowInfo, Converter={StaticResource FindInputEventConverter}}"
                                         Tag="{Binding Source='hf8dd4c2fg97e0g4ccdgac1ag3cd1fe5060ef', Converter={StaticResource TranslatedStringConverter}}"
                                         ContentTemplate="{StaticResource ControllerButtonHint}"
                                         Command="ls:UIWidget.ToggleTooltipOnUIElementCommand"
                                         CommandParameter="{Binding FocusedElement, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}"/>
                        <ls:LSButton x:Name="SkipReactionsBtn" Focusable="False" BoundEvent="UICancel" Margin="{StaticResource ButtonHint.Margin}" VerticalAlignment="Bottom"
                                         Content="{Binding CurrentPlayer.UIData.InputEvents, ConverterParameter=UICancel, Converter={StaticResource FindInputEventConverter}}"
                                         Tag="{Binding Source='h6e84ebd2g4056g48b9ga760g3805aaadf1a8', Converter={StaticResource TranslatedStringConverter}}"
                                         ContentTemplate="{StaticResource ControllerButtonHint}"
                                         Command="{Binding InterruptDontReactPlayerCommand}"
                                         CommandParameter="{Binding Data.InterruptEvents[0]}"/>

                    </StackPanel>

                    <!--These input bindings are used to eat some inputs while the reactions are visible-->
                    <ls:LSInputBinding x:Name="ToggleSpellsInputBinding" BoundEvent="ToggleSpells" EatInput="True" PlayerId="{Binding CurrentPlayer.PlayerId}"/>
                    <ls:LSInputBinding x:Name="ToggleManualTurnBasedModeInputBinding" BoundEvent="ToggleManualTurnBasedMode" EatInput="True" PlayerId="{Binding CurrentPlayer.PlayerId}"/>
                    <ls:LSInputBinding x:Name="ToggleRestPanelInputBinding" BoundEvent="ToggleRestPanel" EatInput="True" PlayerId="{Binding CurrentPlayer.PlayerId}"/>
                    <ls:LSInputBinding x:Name="PanelSelectInputBinding" BoundEvent="PanelSelect" EatInput="True" PlayerId="{Binding CurrentPlayer.PlayerId}"/>
                    <ls:LSInputBinding x:Name="PartyManagementInputBinding" BoundEvent="PartyManagement" EatInput="True" PlayerId="{Binding CurrentPlayer.PlayerId}"/>

                    <b:Interaction.Triggers>
                        <!-- Play a UI looping sound while the reaction decision pop up is active -->
                        <b:DataTrigger Binding="{Binding Path=IsVisible, ElementName=Root}" Value="True">
                            <b:Interaction.Behaviors>
                                <b:ConditionBehavior>
                                    <b:ConditionalExpression>
                                        <b:ComparisonCondition LeftOperand="{Binding Data.InterruptEvents.Count}" Operator="GreaterThan" RightOperand="0"/>
                                    </b:ConditionalExpression>
                                </b:ConditionBehavior>
                            </b:Interaction.Behaviors>
                            <ls:LSPlaySound  Sound="UI_HUD_Reaction_LoopStart"/>
                            <!-- Tutorial UI Event - Reactions Decision Opened-->
                            <b:InvokeCommandAction Command="{Binding TutorialEvent}" CommandParameter="1a3c4bbd-4db8-4c58-9620-f1f78d57c964"/>
                        </b:DataTrigger>
                        <b:DataTrigger Binding="{Binding Path=IsVisible, ElementName=Root}" Value="False">
                            <b:Interaction.Behaviors>
                                <b:ConditionBehavior>
                                    <b:ConditionalExpression>
                                        <b:ComparisonCondition LeftOperand="{Binding Data.InterruptEvents.Count}" Operator="Equal" RightOperand="0"/>
                                    </b:ConditionalExpression>
                                </b:ConditionBehavior>
                            </b:Interaction.Behaviors>
                            <ls:LSPlaySound  Sound="UI_HUD_Reaction_LoopStop"/>
                        </b:DataTrigger>
                        <b:EventTrigger EventName="Unloaded">
                            <ls:LSPlaySound Sound="UI_HUD_Reaction_LoopStop"/>
                        </b:EventTrigger>
                    </b:Interaction.Triggers>
                </Grid>
            </Grid>
            <ControlTemplate.Triggers>

                <DataTrigger Binding="{Binding Path=FocusedElement.DataContext, ElementName=ReactionDecisionPopup_c, Converter={StaticResource TypeConverter}, FallbackValue=''}" Value="ls.VMInterruptDecision">
                    <Setter TargetName="SelectBtn" Property="IsEnabled" Value="True"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Path=FocusedElement.DataContext, ElementName=ReactionDecisionPopup_c, Converter={StaticResource TypeConverter}, FallbackValue=''}" Value="ls.VMInterruptor">
                    <Setter TargetName="SelectBtn" Property="IsEnabled" Value="True"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Result, ElementName=HasAnybodyMadeASelection}" Value="True">
                    <Setter TargetName="ConfirmReactionsBtn" Property="IsEnabled" Value="True"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Layout}" Value="Common">
                    <Setter TargetName="SideDock" Property="Margin" Value="{StaticResource SinglePlayerMargin}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Layout}" Value="Right">
                    <Setter TargetName="SideDock" Property="HorizontalAlignment" Value="Right"/>
                    <Setter TargetName="SideShading" Property="HorizontalAlignment" Value="Right"/>
                    <Setter TargetName="SideShading" Property="Fill" Value="{StaticResource RightSideShadingBrush}"/>
                    <Setter TargetName="ButtonPrompts" Property="HorizontalAlignment" Value="Right"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding IsVisible, ElementName=Root}" Value="False">
                    <Setter TargetName="ToggleSpellsInputBinding" Property="IsEnabled" Value="False"/>
                    <Setter TargetName="ToggleManualTurnBasedModeInputBinding" Property="IsEnabled" Value="False"/>
                    <Setter TargetName="ToggleRestPanelInputBinding" Property="IsEnabled" Value="False"/>
                    <Setter TargetName="PanelSelectInputBinding" Property="IsEnabled" Value="False"/>
                    <Setter TargetName="PartyManagementInputBinding" Property="IsEnabled" Value="False"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding ShowUIElementTooltip, ElementName=ReactionDecisionPopup_c}" Value="True">
                    <Setter TargetName="ToggleTooltips" Property="Tag" Value="{Binding Source='hb33ec471gd611g4ee3gbbffg699af8f79167', Converter={StaticResource TranslatedStringConverter}}"/>
                </DataTrigger>
                
                <!-- If the player has reactions, show the reaction panel... -->
                <DataTrigger Binding="{Binding ElementName=OwnerInterruptors, Path=FilteredItems.Count, Converter={StaticResource GreaterThanConverter}, ConverterParameter=0}" Value="True">
                    <Setter TargetName="ReactionPanel" Property="Visibility" Value="Visible"/>
                </DataTrigger>

                <!-- ...otherwise if the other splitscreen player has reactions, show the spinner... -->
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding ElementName=OwnerInterruptors, Path=FilteredItems.Count}" Value="0"/>
                        <Condition Binding="{Binding ElementName=OtherLocalInterruptors, Path=FilteredItems.Count, Converter={StaticResource GreaterThanConverter}, ConverterParameter=0}" Value="True"/>
                    </MultiDataTrigger.Conditions>
                    <Setter TargetName="ReactionPanel" Property="Visibility" Value="Collapsed"/>
                </MultiDataTrigger>

                <!-- ...or, if no local player has reactions, show remote reactions on the left screen, and the spinner on the right screen -->
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding ElementName=OwnerInterruptors, Path=FilteredItems.Count}" Value="0"/>
                        <Condition Binding="{Binding ElementName=OtherLocalInterruptors, Path=FilteredItems.Count}" Value="0"/>
                        <Condition Binding="{Binding Layout}" Value="Left"/>
                    </MultiDataTrigger.Conditions>
                    <Setter TargetName="ReactionPanel" Property="Visibility" Value="Visible"/>
                </MultiDataTrigger>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding ElementName=OwnerInterruptors, Path=FilteredItems.Count}" Value="0"/>
                        <Condition Binding="{Binding ElementName=OtherLocalInterruptors, Path=FilteredItems.Count}" Value="0"/>
                        <Condition Binding="{Binding Layout}" Value="Right"/>
                    </MultiDataTrigger.Conditions>
                    <Setter TargetName="ReactionPanel" Property="Visibility" Value="Collapsed"/>
                </MultiDataTrigger>

                <!-- If the other splitscreen player has reactions, show that you're waiting for them. -->
                <DataTrigger Binding="{Binding ElementName=OtherLocalInterruptors, Path=FilteredItems.Count, Converter={StaticResource GreaterThanConverter}, ConverterParameter=0}" Value="True">
                    <Setter TargetName="WaitPlayerLabel" Property="Text">
                        <Setter.Value>
                            <MultiBinding Converter="{StaticResource ParameterizedTranslatedStringConverter}">
                                <Binding Source="h00398504ga339g4b5fgb813g36ea49456487"/>
                                <Binding ElementName="OtherLocalInterruptors" Path="FilteredItems[0].Character.PlayerCharacterProperties.OwnerUserID"/>
                            </MultiBinding>
                        </Setter.Value>
                    </Setter>
                </DataTrigger>
                
                <!-- Otherwise, you're waiting for a remote player. -->
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding ElementName=OtherLocalInterruptors, Path=FilteredItems.Count}" Value="0"/>
                        <Condition Binding="{Binding ElementName=OtherRemoteInterruptors, Path=FilteredItems.Count, Converter={StaticResource GreaterThanConverter}, ConverterParameter=0}" Value="True"/>
                    </MultiDataTrigger.Conditions>
                    <Setter TargetName="WaitPlayerLabel" Property="Text">
                        <Setter.Value>
                            <MultiBinding Converter="{StaticResource ParameterizedTranslatedStringConverter}">
                                <Binding Source="h00398504ga339g4b5fgb813g36ea49456487"/>
                                <Binding ElementName="OtherLocalInterruptors" Path="FilteredItems[0].Character.PlayerCharacterProperties.OwnerPlayerName"/>
                            </MultiBinding>
                        </Setter.Value>
                    </Setter>
                </MultiDataTrigger>
                
                <DataTrigger Binding="{Binding Path=Visibility, ElementName=SpinnerIcon}" Value="Collapsed">
                    <Setter TargetName="WaitPlayerLabel" Property="Margin" Value="0,0,0,0"/>
                </DataTrigger>

                <Trigger Property="Visibility" Value="Visible" SourceName="SpinnerIcon">
                    <Trigger.EnterActions>
                        <BeginStoryboard x:Name="BeginStoryboard_Spinner" Storyboard="{StaticResource GradientChaser}"/>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <StopStoryboard BeginStoryboardName="BeginStoryboard_Spinner"/>
                    </Trigger.ExitActions>
                </Trigger>

            </ControlTemplate.Triggers>
        </ControlTemplate>
    </ls:UIWidget.Template>
    <b:Interaction.Triggers>
        <b:EventTrigger EventName="FocusChanged">
            <b:InvokeCommandAction Command="{Binding ShowTooltipOnUIElementCommand, ElementName=ReactionDecisionPopup_c}" CommandParameter="{Binding Path=FocusedElement, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}"/>
        </b:EventTrigger>
        <b:DataTrigger Binding="{Binding HasInputEnabled, ElementName=ReactionDecisionPopup_c}" Value="True">
            <b:InvokeCommandAction Command="{Binding ShowTooltipOnUIElementCommand, ElementName=ReactionDecisionPopup_c}" CommandParameter="{Binding Path=FocusedElement, ElementName=ReactionDecisionPopup_c}"/>
        </b:DataTrigger>

        <b:EventTrigger EventName="MoveFocusUp">
            <ls:LSPlaySound Sound="UI_Shared_Hover" />
        </b:EventTrigger>

        <b:EventTrigger EventName="MoveFocusDown">
            <ls:LSPlaySound Sound="UI_Shared_Hover" />
        </b:EventTrigger>
    </b:Interaction.Triggers>
</ls:UIWidget>