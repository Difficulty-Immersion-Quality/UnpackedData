<ls:UIWidget
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ls="clr-namespace:ls;assembly=Code"
             xmlns:System="clr-namespace:System;assembly=mscorlib"
             xmlns:noesis="clr-namespace:NoesisGUIExtensions;assembly=Noesis.GUI.Extensions"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             x:Name="RewardPanel"
             mc:Ignorable="d"
             ls:UIWidget.ContextName="RewardPanel"
             d:DataContext="{d:DesignInstance Type=ls:DCRewardPanel, IsDesignTimeCreatable=True}"
             d:DesignWidth="3840" d:DesignHeight="2160"
             ls:TooltipExtender.Owner="{Binding CurrentPlayer.SelectedCharacter}"
    >
    <ls:UIWidget.Resources>
        <!--overriding these to keep the old sizes since this ui doesn't support cell scaling yet-->
        <System:Double x:Key="ItemSlotSize">112</System:Double>

        <BitmapImage x:Key="SelectionLocal" UriSource="/Core;component/Assets/Options/ico_check_d.png"/>
        <BitmapImage x:Key="SelectionRemote" UriSource="/Core;component/Assets/Reward/ico_multiplayer_vote.png"/>

        <DataTemplate x:Key="ItemIcon">
            <DataTemplate.Resources>
                <ImageSource x:Key="RewardFrameImg" >/Core;component/Assets/Reward/reward_frame.png</ImageSource>
            </DataTemplate.Resources>
            <Grid ToolTipService.Placement="Top" DataContext="{Binding Object}">
                <Grid.ToolTip>
                    <ls:LSTooltip IsHitTestVisible="False" Content="{Binding DataContext.Object, RelativeSource={RelativeSource Mode=TemplatedParent}}"/>
                </Grid.ToolTip>
                <Image HorizontalAlignment="Center" VerticalAlignment="Center" Source="{StaticResource RewardFrameImg}" Stretch="None" Margin="-8"/>

                <ContentPresenter Content="{Binding .}" ContentTemplate="{StaticResource Template.GameObject}" Width="{StaticResource ItemSlotSize}" Height="{StaticResource ItemSlotSize}"/>
                <b:Interaction.Triggers>
                    <b:EventTrigger EventName="MouseEnter">
                        <ls:LSPlaySound Sound="UI_Shared_Hover" />
                        <b:InvokeCommandAction Command="{Binding DataContext.SetDebugUIEntityCommand, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}" CommandParameter="{Binding EntityHandle}"/>
                    </b:EventTrigger>
                    <b:EventTrigger EventName="MouseLeave">
                        <b:InvokeCommandAction Command="{Binding DataContext.ClearDebugUIEntityCommand, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}"/>
                    </b:EventTrigger>
                    <!-- Case unselecting an item, play unselected sound on click -->
                    <b:EventTrigger EventName="MouseLeftButtonDown">
                        <b:Interaction.Behaviors>
                            <b:ConditionBehavior>
                                <b:ConditionalExpression>
                                    <b:ComparisonCondition LeftOperand="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ListBoxItem}, AncestorLevel=1}, Path=IsSelected}" Operator="Equal" RightOperand="True"/>
                                </b:ConditionalExpression>
                            </b:ConditionBehavior>
                        </b:Interaction.Behaviors>
                        <ls:LSPlaySound Sound="UI_HUD_RewardSelect_Press_Unselected"/>
                    </b:EventTrigger>
                    <!-- Case not all are selected, play selected sound on click -->
                    <b:EventTrigger EventName="MouseLeftButtonDown">
                        <b:Interaction.Behaviors>
                            <b:ConditionBehavior>
                                <b:ConditionalExpression>
                                    <b:ComparisonCondition LeftOperand="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ListBoxItem}, AncestorLevel=1}, Path=IsSelected}" Operator="Equal" RightOperand="False"/>
                                    <b:ComparisonCondition LeftOperand="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ls:UIWidget}, AncestorLevel=1}, Path=DataContext.IsAllSelected}" Operator="Equal" RightOperand="False" />
                                </b:ConditionalExpression>
                            </b:ConditionBehavior>
                        </b:Interaction.Behaviors>
                        <ls:LSPlaySound Sound="UI_HUD_RewardSelect_Press_Selected"/>
                    </b:EventTrigger>
                    <!-- Case all are selected, play error sound on click -->
                    <b:EventTrigger EventName="MouseLeftButtonDown">
                        <b:Interaction.Behaviors>
                            <b:ConditionBehavior>
                                <b:ConditionalExpression>
                                    <b:ComparisonCondition LeftOperand="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ListBoxItem}, AncestorLevel=1}, Path=IsSelected}" Operator="Equal" RightOperand="False"/>
                                    <b:ComparisonCondition LeftOperand="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ls:UIWidget}, AncestorLevel=1}, Path=DataContext.IsAllSelected}" Operator="Equal" RightOperand="True" />
                                </b:ConditionalExpression>
                            </b:ConditionBehavior>
                        </b:Interaction.Behaviors>
                        <ls:LSPlaySound Sound="UI_HUD_RewardSelect_Press_Error"/>
                    </b:EventTrigger>
                </b:Interaction.Triggers>
            </Grid>

        </DataTemplate>
        <ControlTemplate x:Key="ListBoxItemItemIcon" TargetType="{x:Type ListBoxItem}">
            <Border x:Name="Border" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                <StackPanel Orientation="Vertical">
                    <ls:LSButton Command="{Binding DataContext.ToggleOptionalItemCommand, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}" CommandParameter="{Binding Object}">
                        <ls:LSButton.Template>
                            <ControlTemplate TargetType="{x:Type ls:LSButton}">
                                <Grid>
                                    <b:Interaction.Behaviors>
                                        <ls:CollectionContainsItemBehavior x:Name="IsSelectedBehaviour" ItemSource="{Binding Object}" CollectionSource="{Binding DataContext.SelectedItems, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}"/>
                                    </b:Interaction.Behaviors>
                                    <ContentPresenter ContentTemplate="{StaticResource ItemIcon}" Content="{Binding .}" DataContext="{Binding Object}"
                                                      HorizontalAlignment="Center" VerticalAlignment="Center"/>

                                    <Grid HorizontalAlignment="Center" VerticalAlignment="Bottom" IsHitTestVisible="False">
                                        <Image HorizontalAlignment="Center" VerticalAlignment="Bottom" Stretch="None" Source="{StaticResource BtnSmallRound}"/>
                                        <Image x:Name="LocalSelectionImage" HorizontalAlignment="Center" VerticalAlignment="Bottom" Stretch="None" Source="{StaticResource SelectionLocal}" Margin="4,0,0,0"
                                               Visibility="{Binding Result, ElementName=IsSelectedBehaviour, Converter={StaticResource BoolToVisibleConverter}}"/>
                                        <Image x:Name="RemoteSelectionImage" HorizontalAlignment="Center" VerticalAlignment="Bottom" Stretch="None" Source="{StaticResource SelectionRemote}" Width="40" Margin="0,0,12,0"
                                               Visibility="Collapsed"/>
                                    </Grid>
                                </Grid>
                                <ControlTemplate.Triggers>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding ElementName=LocalSelectionImage, Path=Visibility}" Value="Collapsed"/>
                                            <Condition Binding="{Binding SuggestedByPlayers.Count, Converter={StaticResource GreaterThanConverter}, ConverterParameter=0}" Value="True"/>
                                        </MultiDataTrigger.Conditions>
                                        <Setter TargetName="RemoteSelectionImage" Property="Visibility" Value="Visible"/>
                                    </MultiDataTrigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </ls:LSButton.Template>
                    </ls:LSButton>
                    <ItemsControl ItemsSource="{Binding SuggestedByPlayers}" HorizontalAlignment="Center" Margin="0,10">
                        <ItemsControl.ToolTip>
                            <ls:LSTooltip>
                                <ItemsControl ItemsSource="{Binding DataContext.SuggestedByPlayers, RelativeSource={RelativeSource Mode=TemplatedParent}}" HorizontalAlignment="Center">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Vertical"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate DataType="{x:Type ls:Player}">
                                            <TextBlock Text="{Binding Name}" Style="{StaticResource TooltipBaseTextStyle}"/>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ls:LSTooltip>
                        </ItemsControl.ToolTip>
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type ls:Player}">
                                <Ellipse Height="50" Width="50" Fill="{Binding CommunityIcon}" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="-10,0"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" TargetName="Border" Value="Transparent"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" TargetName="Border" Value="Transparent"/>
                </Trigger>
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsMouseOver" Value="True"/>
                        <Condition Property="IsSelected" Value="True"/>
                    </MultiTrigger.Conditions>
                    <Setter Property="Background" TargetName="Border" Value="Transparent"/>
                </MultiTrigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <Style x:Key="ItemIconStyle" TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}" >
            <Setter Property="ContentTemplate" Value="{StaticResource ItemIcon}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template" Value="{StaticResource ListBoxItemItemIcon}"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                </Trigger>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding Object.IsSelected, RelativeSource={RelativeSource Self}}" Value="False"/>
                        <Condition Binding="{Binding DataContext.IsAllSelected, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}" Value="True"/>
                    </MultiDataTrigger.Conditions>
                    <MultiDataTrigger.Setters>
                        <Setter Property="Opacity" Value="0.7"/>
                    </MultiDataTrigger.Setters>
                </MultiDataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="HeaderTextStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="{DynamicResource LS_accent75TxtColor}"/>
            <Setter Property="TextAlignment" Value="Center"/>
        </Style>

    </ls:UIWidget.Resources>
    <ls:UIWidget.ContentTemplate>
        <DataTemplate>
            <ls:LSNineSliceImage x:Name="Panel" Margin="0,0,0,208" HorizontalAlignment="Center" VerticalAlignment="Center" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" ImageSource="/Core;component/Assets/Reward/reward_bg_full.png" Slices="0,412,0,560" IconWidth="850" Padding="80,17,80,50" Width="1044">
                <Grid>
                    <Rectangle x:Name="DragObj" Fill="Transparent" Height="123" VerticalAlignment="Top" Margin="0,60,0,0"/>
                    <Image Source="pack://application:,,,/Core;component/Assets/Reward/ico_reward.png" Stretch="None" VerticalAlignment="Top"/>
                    <ls:LSButton x:Name="CloseButton" Style="{StaticResource BigBlueButtonStyle}" VerticalAlignment="Bottom"
                                 Content="{Binding Converter={StaticResource TranslatedStringConverter}, Source=h57c4b009g2843g4098gbf30gebb4819560ca}"
                                 Command="{Binding GetRewardsAndClose.ActionCommand}" ls:LSButton.SoundID="UI_Shared_Accept"
                                 ls:LSButton.OverwriteClickSound="UI_HUD_RewardSelect_Confirm_Click" HorizontalAlignment="Center">
                        <ls:LSButton.IsEnabled>
                            <MultiBinding Converter="{StaticResource BoolAndConverter}">
                                <Binding Path="IsAllSelected"/>
                                <Binding Path="CurrentPlayer.SelectedCharacter.IsMultiplayerCharacter" />
                            </MultiBinding>
                        </ls:LSButton.IsEnabled>
                    </ls:LSButton>
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,58,0,80" >
                        <b:Interaction.Behaviors>
                            <ls:LSMouseDragElementBehavior ls:LSMouseDragElementBehavior.HitArea="{Binding ElementName=DragObj, Mode=OneWay}"/>
                        </b:Interaction.Behaviors>
                        <b:Interaction.Triggers>
                            <b:EventTrigger EventName="Loaded">
                                <ls:LSPlaySound Sound="UI_HUD_Reward_Notification"/>
                            </b:EventTrigger>
                        </b:Interaction.Triggers>

                        <TextBlock Text="{Binding Converter={StaticResource TranslatedStringConverter}, Source=h41bffdc7g230bg47a5g80c0g23ea99df1a98}"
                                   Margin="0,44,0,5" Style="{StaticResource HeaderTextStyle}" FontSize="{StaticResource TitleFontSize}"/>

                        <ls:LSWrapPanel x:Name="QuestInfo" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Top" HorizontalContentAlignment="Center">
                            <TextBlock Text="{Binding QuestName, StringFormat='{}{0}:'}" Foreground="{DynamicResource LS_baseTxtColor}" FontSize="{StaticResource MediumFontSize}" HorizontalAlignment="Left" Margin="0 0 10 0" LineStackingStrategy="BlockLineHeight" LineHeight="{DynamicResource DefaultFontSizeLineHeight}"/>
                            <TextBlock Text="{Binding QuestDescription}" TextWrapping="Wrap" Foreground="{DynamicResource LS_accent100TxtColor}" FontSize="{StaticResource MediumFontSize}" HorizontalAlignment="Left" TextAlignment="Center" LineStackingStrategy="BlockLineHeight" LineHeight="{DynamicResource DefaultFontSizeLineHeight}"/>
                        </ls:LSWrapPanel>

                        <StackPanel VerticalAlignment="Top" Margin="0,20,0,0">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <Ellipse x:Name="ReceiverIcon" Height="50" Width="50" Fill="{Binding ReceiverCharacter.PlayerCharacterProperties.OwnerCommunityIcon}" HorizontalAlignment="Left" VerticalAlignment="Center" Visibility="Collapsed" Margin="20,0"/>
                                    <TextBlock x:Name="ReceiveText" Text="{Binding Converter={StaticResource TranslatedStringConverter}, Source=h24eeec24gae44g42edg81f2g6de76f80bd10}" Style="{DynamicResource HeaderTextStyle}" Margin="0"/>
                                </StackPanel>

                                <ls:LSNineSliceImage x:Name="MandatoryTreasureHolder" HorizontalAlignment="Center" VerticalAlignment="Center" ImageSource="/Core;component/Assets/Reward/itemList_full.png" Slices="0,50" IconWidth="108" Padding="40" Visibility="{Binding MandatoryItems.Slots.Count, Converter={StaticResource CountToVisibilityConverter}, Mode=Default}" Width="660" HorizontalContentAlignment="Center" Margin="0,-14,0,0">
                                    <ScrollViewer MaxHeight="700" VerticalScrollBarVisibility="Auto" Padding="30, 0">
                                        <StackPanel Orientation="Vertical">
                                            <ItemsControl x:Name="MandatoryTreasure" Tag="AllSelected" ItemsSource="{Binding MandatoryItems.Slots}" ItemTemplate="{StaticResource ItemIcon}" Background="Transparent" BorderThickness="0" MaxWidth="680" HorizontalAlignment="Center">
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <ls:LSWrapPanel HorizontalContentAlignment="Center" HorizontalAlignment="Center" />
                                                    </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                            </ItemsControl>
                                        </StackPanel>
                                    </ScrollViewer>
                                </ls:LSNineSliceImage>
                            </StackPanel>
                            <ls:LSNineSliceImage x:Name="OptionalTreasureHolder" VerticalAlignment="Top" ImageSource="/Core;component/Assets/Reward/itemBoxFull.png" Slices="236,136,236,141" IconWidth="140" Padding="40,30,40,42" Margin="0,22,0,0" Visibility="{Binding OptionalItems.Count, Converter={StaticResource CountToVisibilityConverter}, Mode=OneWay}" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" HorizontalAlignment="Stretch">
                                <StackPanel>
                                    <TextBlock x:Name="ChooseText" Text="{Binding Converter={StaticResource TranslatedStringConverter}, Source=ha8eafa85gec0eg4c43g9c5cg75c4d31ffddf}" Style="{DynamicResource HeaderTextStyle}" Margin="0,8,0,0" />

                                    <ListBox x:Name="OptionalTreasure"
                                             ItemsSource="{Binding OptionalItems}"
                                             ItemContainerStyle="{StaticResource ItemIconStyle}"
                                             HorizontalAlignment="Center" VerticalAlignment="Top"
                                             Background="Transparent"
                                             BorderThickness="0"
                                             SelectionMode="Multiple"
                                             Margin="0,10,0,0" HorizontalContentAlignment="Center">
                                        <ListBox.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <ls:LSWrapPanel HorizontalContentAlignment="Center" HorizontalAlignment="Center" MaxWidth="680"/>
                                            </ItemsPanelTemplate>
                                        </ListBox.ItemsPanel>
                                    </ListBox>
                                    <TextBlock Foreground="{StaticResource LS_PrimaryColor}" FontSize="{StaticResource DefaultFontSize}" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,15,0,0">
                                        <TextBlock.Text>
                                            <MultiBinding StringFormat="{}{0}/{1}">
                                                <Binding Path="SelectedItemsCount"/>
                                                <Binding Path="MaxSelectedItems"/>
                                            </MultiBinding>
                                        </TextBlock.Text>
                                    </TextBlock>
                                </StackPanel>
                            </ls:LSNineSliceImage>
                        </StackPanel>
                        <TextBlock x:Name="CharacterName" TextWrapping="Wrap" HorizontalAlignment="Center" Foreground="{DynamicResource LS_accent75TxtColor}" Margin="0,0,0,10">
                            <Run Text="{Binding ReceiverCharacter.Name}"/>
                            <Run x:Name="ClaimWarning" Text="" Foreground="{DynamicResource LS_alertTxtColor}"/>
                        </TextBlock>

                        <StackPanel x:Name="PanelButtons" HorizontalAlignment="Center" VerticalAlignment="Bottom" MaxWidth="900" Margin="0,0,0,40" Orientation="Horizontal">
                            <ls:LSButton x:Name="InventoryButton" Template="{StaticResource IconButtonTemplate}" Command="{Binding CustomEvent}" CommandParameter="ToggleCharacterPanel" ls:LSButton.SoundID="UI_HUD_RewardSelect_Inventory">
                                <ls:LSButton.Resources>
                                    <ImageSource x:Key="IconNormal">pack://application:,,,/Core;component/Assets/BottomBar/ico_inventory_d.png</ImageSource>
                                    <ImageSource x:Key="IconHighlight">pack://application:,,,/Core;component/Assets/BottomBar/ico_inventory_h.png</ImageSource>
                                    <ImageSource x:Key="IconPressed">pack://application:,,,/Core;component/Assets/BottomBar/ico_inventory_d.png</ImageSource>
                                    <ImageSource x:Key="IconDisabled">pack://application:,,,/Core;component/Assets/BottomBar/ico_inventory_disabled.png</ImageSource>
                                    <ImageSource x:Key="ButtonNormal">pack://application:,,,/Core;component/Assets/Buttons/btn_hudNav_d.png</ImageSource>
                                    <ImageSource x:Key="ButtonHighlight">pack://application:,,,/Core;component/Assets/Buttons/btn_hudNav_h.png</ImageSource>
                                    <ImageSource x:Key="ButtonPressed">pack://application:,,,/Core;component/Assets/Buttons/btn_hudNav_p.png</ImageSource>
                                    <ImageSource x:Key="ButtonDisabled">pack://application:,,,/Core;component/Assets/Buttons/btn_hudNav_disabled.png</ImageSource>
                                    <System:Double x:Key="IconWidth">52</System:Double>
                                    <System:Double x:Key="IconHeight">52</System:Double>
                                </ls:LSButton.Resources>
                            </ls:LSButton>
                            <TextBlock Text="{Binding Converter={StaticResource TranslatedStringConverter}, Source=ha6637339gc9adg4d0agb856g67b4662bbb8a}" Foreground="{StaticResource LS_baseTxtColor}" VerticalAlignment="Center" Margin="10,0,0,0"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </ls:LSNineSliceImage>
            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding CurrentPlayer.SelectedCharacter.IsMultiplayerCharacter}" Value="False">
                    <Setter TargetName="ClaimWarning" Property="Text"  Value="{Binding Converter={StaticResource TranslatedStringConverter}, Source=hdf342c97g8f0fg44bag8ffdg0e5b8b212b87, StringFormat=({0})}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding IsReceiver}" Value="False">
                    <Setter TargetName="CloseButton" Property="Content" Value="{Binding Converter={StaticResource TranslatedStringConverter}, Source=h411fb88eg5261g437aga291g2fb260e633c6}"/>
                    <Setter TargetName="CloseButton" Property="Command" Value="{Binding CustomEvent}"/>
                    <Setter TargetName="CloseButton" Property="CommandParameter" Value="CloseRewardPanel"/>
                    <Setter TargetName="CloseButton" Property="IsEnabled" Value="True"/>
                    <Setter TargetName="ChooseText" Property="Text" Value="{Binding Converter={StaticResource TranslatedStringConverter}, Source=h0357f010g635cg4c1cg9dfeg4f02a70bc0a8}"/>
                    <Setter TargetName="ReceiverIcon" Property="Visibility" Value="Visible"/>
                    <Setter TargetName="ReceiveText" Property="Text">
                        <Setter.Value>
                            <MultiBinding Converter="{StaticResource ParameterizedTranslatedStringConverter}">
                                <Binding Source="hb4ac2054ga4b8g43aag9db7g114fb41ee94b"/>
                                <Binding Path="ReceiverCharacter.PlayerCharacterProperties.OwnerPlayerName"/>
                            </MultiBinding>
                        </Setter.Value>
                    </Setter>
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>
    </ls:UIWidget.ContentTemplate>
</ls:UIWidget>