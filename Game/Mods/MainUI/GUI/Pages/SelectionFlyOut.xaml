<ls:UIWidget x:Name="SelectionFlyOut"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:System="clr-namespace:System;assembly=mscorlib"
			 xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:ls="clr-namespace:ls;assembly=Code"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 mc:Ignorable="d"
			 d:DataContext="{d:DesignInstance {x:Type ls:DCSelectionFlyOut}, IsDesignTimeCreatable=True}"
             xmlns:noesis="clr-namespace:NoesisGUIExtensions;assembly=Noesis.GUI.Extensions"
             d:DesignHeight="2160" d:DesignWidth="3840"
             ls:UIWidget.UseCustomDC="true"
             ls:UIWidget.ContextName="SelectionFlyOut">

    <ls:UIWidget.Resources>
        <ImageSource x:Key="SlotUpcastingBgImage">pack://application:,,,/Core;component/Assets/BottomBar/slot_upcasting_d.png</ImageSource>
    </ls:UIWidget.Resources>

    <Grid Width="3840" Height="2160" ls:TooltipExtender.Owner="{Binding OwnerCharacter}">
        <ls:LSSelectionFlyOut ParentControl="{Binding ParentControl}" Orientation="{Binding Orientation}" HorizontalAlignment="Left" VerticalAlignment="Top">
            <Grid>
                <ContentControl>
                    <ContentControl.Template>
                        <ControlTemplate>
                            <ls:LSNineSliceImage x:Name="Background" ImageSource="pack://application:,,,/Core;component/Assets/SelectionFlyOut/spellSubSelect_pane.png" IconWidth="92" Slices="56,96,56,96">
                                <Grid MinHeight="192" MinWidth="204">
                                    <Image x:Name="Icon" Stretch="None" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,24,0,0">
                                        <Image.Style>
                                            <Style TargetType="Image">
                                                <Setter Property="Source" Value="pack://application:,,,/Core;component/Assets/BottomBar/ico_spells_d.png"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding PanelContentType}" Value="Item">
                                                        <Setter Property="Source" Value="pack://application:,,,/Core;component/Assets/BottomBar/ico_inventory_d.png"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Image.Style>
                                    </Image>
                                    <ContentPresenter x:Name="FlyOutContent" Margin="56,96,56,56"/>
                                </Grid>
                            </ls:LSNineSliceImage>

                            <ControlTemplate.Triggers>
                                <DataTrigger Binding="{Binding ShowExtendedInfo}" Value="True">
                                    <Setter TargetName="Background" Property="ImageSource" Value="pack://application:,,,/Core;component/Assets/SelectionFlyOut/spellContainer.png" />
                                    <Setter TargetName="Background" Property="IconWidth" Value="0" />
                                    <Setter TargetName="Background" Property="Slices" Value="56" />
                                    <Setter TargetName="Icon" Property="Visibility" Value="Collapsed" />
                                    <Setter TargetName="FlyOutContent" Property="Margin" Value="50,30,50,40" />
                                </DataTrigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </ContentControl.Template>
                    <ItemsControl x:Name="OptionsContainer" ItemsSource="{Binding ObjectCollectionList}" >
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Vertical" MaxWidth="616" Visibility="{Binding ObjectCollection.Count, Converter={StaticResource CountToVisibilityConverter}, ConverterParameter=0}">
                                    <TextBlock ls:TextBlockFormatter.SourceText="{Binding Title}" Visibility="{Binding Title, Converter={StaticResource StringToVisibilityConverter}}" TextWrapping="Wrap" HorizontalAlignment="Center"/>
                                    <ScrollViewer VerticalAlignment="Top" HorizontalAlignment="Left" MaxHeight="470" Template="{StaticResource ScrollViewerTemplate}" VerticalScrollBarVisibility="Auto">
                                        <ScrollViewer.Resources>
                                            <GridLength x:Key="Top">0</GridLength>
                                            <GridLength x:Key="Bottom">0</GridLength>
                                        </ScrollViewer.Resources>
                                        <ItemsControl x:Name="Content" ItemsSource="{Binding ObjectCollection}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <ContentControl x:Name="MainContainer" Margin="4,4,6,6">
                                                        <b:Interaction.Behaviors>
                                                            <ls:CollectionFilterBehavior x:Name="FilteredActionResourcesCostPreview" ItemsSource="{Binding DataContext.OwnerCharacter.Stats.SpellSlots, ElementName=OptionsContainer}" Predicate="{Binding HotBarActionResourcesPredicate}"/>
                                                        </b:Interaction.Behaviors>

                                                        <StackPanel>
                                                            <Image x:Name="arrowImg" Source="pack://application:,,,/Core;component/Assets/ActiveRoll/addArrow.png" Stretch="None" VerticalAlignment="Bottom" Visibility="Collapsed" Margin="0,-100,0,0"/>

                                                            <ls:LSButton x:Name="SlotLevelBtn" Margin="2,2,0,0" Command="{Binding DataContext.UseSlotCommand, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}" CommandParameter="{Binding .}" SoundID="UI_HUD_HotbarSlot">
                                                                <Grid>
                                                                    <Image Stretch="None" Source="{StaticResource SlotUpcastingBgImage}"/>
                                                                    <ItemsControl ItemsSource="{Binding (b:Interaction.Behaviors)[0].FilteredItems, ElementName=MainContainer}" Visibility="{Binding Path=DataContext.ShowExtendedInfo, ElementName=OptionsContainer, Converter={StaticResource BoolToVisibleConverter}}" HorizontalAlignment="Center" VerticalAlignment="Top" ls:AttachedProperties.InheritedTag="minimalView">
                                                                        <ItemsControl.Style>
                                                                            <Style TargetType="ItemsControl">
                                                                                <Setter Property="ItemsPanel">
                                                                                    <Setter.Value>
                                                                                        <ItemsPanelTemplate>
                                                                                            <StackPanel Orientation="Horizontal" Height="100"/>
                                                                                        </ItemsPanelTemplate>
                                                                                    </Setter.Value>
                                                                                </Setter>
                                                                            </Style>
                                                                        </ItemsControl.Style>
                                                                        <ItemsControl.ItemContainerStyle>
                                                                            <Style TargetType="ContentPresenter">
                                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                                                <Setter Property="Margin" Value="0,0,-6,0"/>
                                                                                <Style.Triggers>
                                                                                    <DataTrigger Binding="{Binding ReplenishType}" Value="Rest">
                                                                                        <Setter Property="Visibility" Value="Visible"/>
                                                                                    </DataTrigger>
                                                                                    <DataTrigger Binding="{Binding ReplenishType}" Value="ShortRest">
                                                                                        <Setter Property="Visibility" Value="Visible"/>
                                                                                    </DataTrigger>
                                                                                </Style.Triggers>
                                                                            </Style>
                                                                        </ItemsControl.ItemContainerStyle>
                                                                        <ItemsControl.ItemTemplate>
                                                                            <DataTemplate DataType="ls:VMActionResource">
                                                                                <Grid Width="80" Height="80">
                                                                                    <Image x:Name="Background" Source="{StaticResource SlotUpcastingBgImage}" Stretch="None"/>
                                                                                    <TextBlock x:Name="SpellSlotLevel" Text="{Binding Level, Converter={StaticResource DecimalToRomanConverter}}" Foreground="{StaticResource LS_accent100TxtColor}" VerticalAlignment="Top" HorizontalAlignment="Center" Height="35" Margin="0,8,0,0" FontSize="{StaticResource MediumFontSize}"  noesis:Text.StrokeThickness="5"/>


                                                                                    <ls:LSActionPointResources x:Name="ResourcePoints" VerticalAlignment="Center" HorizontalAlignment="Center" Background="Transparent"  Margin="0,18,0,-18"
                                                                                            MaxActionPoints="1"
                                                                                            AvailableActionPoints="{Binding Value}"
                                                                                            HighlightedActionPoints="1"
                                                                                            MaxActionPointGroups="0"
                                                                                            SmallActionPointSize="{DynamicResource ActionResources.ActionPointSmallSize}"
                                                                                            ActionPointGroupSize="{DynamicResource ActionResources.ActionPointGroupSize}"
                                                                                            Style="{StaticResource ActionResourcesTemplateSelector}"/>
                                                                                </Grid>
                                                                            </DataTemplate>
                                                                        </ItemsControl.ItemTemplate>
                                                                    </ItemsControl>
                                                                </Grid>
                                                                <ContentControl.ToolTip>
                                                                    <ls:LSTooltip Content="{Binding DataContext.Spell,RelativeSource={RelativeSource Mode=TemplatedParent}}" ls:TooltipExtender.Context="Hotbar" Margin="60,0,-80,160" />
                                                                </ContentControl.ToolTip>

                                                                <b:Interaction.Triggers>
                                                                    <b:EventTrigger EventName="MouseEnter">
                                                                        <b:InvokeCommandAction Command="{Binding DataContext.HighlightResourcesCommand, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}" CommandParameter="{Binding}"/>
                                                                    </b:EventTrigger>
                                                                    <b:EventTrigger EventName="MouseLeave">
                                                                        <b:InvokeCommandAction Command="{Binding DataContext.ClearResourceHighlightsCommand, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}" CommandParameter="{Binding}"/>
                                                                    </b:EventTrigger>
                                                                </b:Interaction.Triggers>
                                                            </ls:LSButton>
                                                        </StackPanel>

                                                    </ContentControl>

                                                    <DataTemplate.Triggers>
                                                        <MultiDataTrigger>
                                                            <MultiDataTrigger.Conditions>
                                                                <Condition Binding="{Binding Path=DataContext.ShowExtendedInfo, ElementName=OptionsContainer}" Value="True" />
                                                                <Condition Binding="{Binding Path=IsMouseOver, ElementName=SlotLevelBtn}" Value="True"/>
                                                            </MultiDataTrigger.Conditions>
                                                            <MultiDataTrigger.Setters>
                                                                <Setter TargetName="arrowImg" Property="Visibility" Value="Visible"/>
                                                            </MultiDataTrigger.Setters>
                                                        </MultiDataTrigger>

                                                        <DataTrigger Binding="{Binding Path=DataContext.ShowExtendedInfo, ElementName=OptionsContainer}" Value="True">
                                                            <Setter TargetName="MainContainer" Property="Margin" Value="8"/>
                                                            <Setter TargetName="SlotLevelBtn" Property="Margin" Value="0"/>
                                                        </DataTrigger>
                                                    </DataTemplate.Triggers>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel Orientation="Horizontal"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                        </ItemsControl>
                                    </ScrollViewer>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ContentControl>
                <ls:LSButton HorizontalAlignment="Right" VerticalAlignment="Top" Template="{StaticResource ButtonTemplate}" Command="{Binding CloseFlyOutCommand}" Margin="0,24,24,0">
                    <ls:LSButton.Resources>
                        <ImageSource x:Key="ButtonNormal">pack://application:,,,/Core;component/Assets/SelectionFlyOut/spellSubSelect_close_d.png</ImageSource>
                        <ImageSource x:Key="ButtonHighlight">pack://application:,,,/Core;component/Assets/SelectionFlyOut/spellSubSelect_close_h.png</ImageSource>
                        <ImageSource x:Key="ButtonPressed">pack://application:,,,/Core;component/Assets/SelectionFlyOut/spellSubSelect_close_h.png</ImageSource>
                        <ImageSource x:Key="ButtonDisabled">pack://application:,,,/Core;component/Assets/SelectionFlyOut/spellSubSelect_close_d.png</ImageSource>
                    </ls:LSButton.Resources>
                </ls:LSButton>
            </Grid>

            <b:Interaction.Triggers>
                <b:EventTrigger EventName="RequestClose">
                    <b:InvokeCommandAction Command="{Binding CloseFlyOutCommand}" CommandParameter="Item"/>
                </b:EventTrigger>
            </b:Interaction.Triggers>
        </ls:LSSelectionFlyOut>
    </Grid>
</ls:UIWidget>
