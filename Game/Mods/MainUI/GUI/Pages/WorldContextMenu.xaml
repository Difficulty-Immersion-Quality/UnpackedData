<ls:UIWidget x:Name="WorldContextMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:b="http://schemas.microsoft.com/xaml/behaviors" 
             xmlns:ls="clr-namespace:ls;assembly=Code"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 mc:Ignorable="d"
             d:DesignHeight="2160" d:DesignWidth="3840" d:DataContext="{d:DesignInstance {x:Type ls:DCWidget}, IsDesignTimeCreatable=True}">

    <ls:LSEntityObject x:Name="WorldContextEntity" Visibility="{Binding CurrentPlayer.UIData.ShowHud, RelativeSource={RelativeSource AncestorType=ls:UIWidget}, Converter={StaticResource BoolToVisibleConverter}}"
                   EntityRef="{Binding EntityHandle}" Context="World" Background="{x:Null}" HorizontalAlignment="Left" VerticalAlignment="Top" Width="10" Height="10"
                   DataContext="{Binding CurrentPlayer.UIData.WorldContextMenu}">
        <ls:LSEntityObject.RenderTransform>
            <TranslateTransform X="{Binding Placement.X}" Y="{Binding Placement.Y}"/>
        </ls:LSEntityObject.RenderTransform>
        <b:Interaction.Triggers>
            <b:DataTrigger Binding="{Binding Visible}" Value="True">
                <b:InvokeCommandAction Command="{Binding DataContext.ShowContextMenu, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}" CommandParameter="{Binding ElementName=WorldContextEntity}"/>
            </b:DataTrigger>
        </b:Interaction.Triggers>
        <ls:LSEntityObject.ContextMenu>
            <ls:ContextMenu Style="{StaticResource BaseLSContextMenu}" Placement="Right"/>
        </ls:LSEntityObject.ContextMenu>
    </ls:LSEntityObject>
</ls:UIWidget>