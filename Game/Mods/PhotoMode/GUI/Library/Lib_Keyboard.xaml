<ResourceDictionary
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:ls="clr-namespace:ls;assembly=Code"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:System="clr-namespace:System;assembly=mscorlib"
    mc:Ignorable="d">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/PhotoMode;component/Library/Lib_Shared.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <Storyboard x:Key="RecallButtonFeedbackAnimation" FillBehavior="Stop">
        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="RecallCameraFeedback" Storyboard.TargetProperty="(UIElement.Opacity)">
            <EasingDoubleKeyFrame KeyTime="0:0:0" Value="1"/>
            <EasingDoubleKeyFrame KeyTime="0:0:1" Value="1"/>
            <EasingDoubleKeyFrame KeyTime="0:0:1.5" Value="0"/>
        </DoubleAnimationUsingKeyFrames>

        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RecallCameraFeedback" Storyboard.TargetProperty="(UIElement.Visibility)" >
            <DiscreteObjectKeyFrame KeyTime="0:0:0" Value="{x:Static Visibility.Visible}"/>
        </ObjectAnimationUsingKeyFrames>
    </Storyboard>

    <Storyboard x:Key="StoreButtonFeedbackAnimation" FillBehavior="Stop">
        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="StoreCameraFeedback" Storyboard.TargetProperty="(UIElement.Opacity)">
            <EasingDoubleKeyFrame KeyTime="0:0:0" Value="1"/>
            <EasingDoubleKeyFrame KeyTime="0:0:1" Value="1"/>
            <EasingDoubleKeyFrame KeyTime="0:0:1.5" Value="0"/>
        </DoubleAnimationUsingKeyFrames>

        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="StoreCameraFeedback" Storyboard.TargetProperty="(UIElement.Visibility)" >
            <DiscreteObjectKeyFrame KeyTime="0:0:0" Value="{x:Static Visibility.Visible}"/>
        </ObjectAnimationUsingKeyFrames>
    </Storyboard>

    <Style x:Key="OriginSliderStyle" TargetType="Path">
        <Setter Property="Data" Value="{StaticResource OriginSliderGeometry}"/>
        <Setter Property="Height" Value="10"/>
        <Setter Property="Width" Value="4"/>
        <Setter Property="StrokeThickness" Value="0"/>
        <Setter Property="Fill" Value="{StaticResource LS_accent100TxtColor}"/>
        <Setter Property="Stretch" Value="None"/>
    </Style>

    <Style x:Key="TreeNodeStyle" TargetType="Path">
        <Setter Property="Data" Value="{StaticResource DiamondPointGeometry}"/>
        <Setter Property="Height" Value="30"/>
        <Setter Property="Width" Value="30"/>
        <Setter Property="StrokeThickness" Value="0"/>
        <Setter Property="Fill" Value="{StaticResource LS_accent00TxtColor}"/>
        <Setter Property="Stretch" Value="None"/>
    </Style>

    <Style x:Key="TreeLineStyle" TargetType="Path">
        <Setter Property="Data" Value="M15,0 15,1"/>
        <Setter Property="Width" Value="30"/>
        <Setter Property="StrokeThickness" Value="2"/>
        <Setter Property="Stroke" Value="{StaticResource LS_accent00TxtColor}"/>
        <Setter Property="Stretch" Value="Fill"/>
    </Style>

    <Style x:Key="StripedBorderBG" TargetType="ls:LSNineSliceImage" BasedOn="{StaticResource {x:Type ls:LSNineSliceImage}}">
        <Setter Property="ImageSource" Value="{StaticResource StripedBoxBackground}"/>
        <Setter Property="Slices" Value="16,12"/>
    </Style>

    <Style x:Key="StickerPanelBG" TargetType="ls:LSNineSliceImage" BasedOn="{StaticResource {x:Type ls:LSNineSliceImage}}">
        <Setter Property="ImageSource" Value="{StaticResource StickerPanelBackground}"/>
        <Setter Property="Slices" Value="16"/>
    </Style>

    <ControlTemplate x:Key="IconButtonHelperTemplate" TargetType="ButtonBase">
        <Grid>
            <Image x:Name="img" Source="{TemplateBinding ls:ButtonHelper.ImageSource}" Stretch="None"/>
            <Image x:Name="icon" Margin="0,0,0,0"  HorizontalAlignment="Center" VerticalAlignment="Center" Source="{TemplateBinding ls:ButtonHelper.SecondaryImageSource}" Stretch="None" Width="{DynamicResource IconWidth}" Height="{DynamicResource IconHeight}"/>
        </Grid>
    </ControlTemplate>

    <Style x:Key="DeleteButtonStyle" TargetType="Button">
        <Setter Property="Template" Value="{StaticResource IconButtonHelperTemplate}"/>
        <Setter Property="ls:ButtonHelper.ImageSource" Value="{StaticResource MediumRoundButtonDefault}"/>
        <Setter Property="ls:ButtonHelper.SecondaryImageSource" Value="pack://application:,,,/Core;component/Assets/PhotoMode/ico_delete_d.png"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="ls:ButtonHelper.ImageSource" Value="{StaticResource MediumRoundButtonHighlight}"/>
                <Setter Property="ls:ButtonHelper.SecondaryImageSource" Value="pack://application:,,,/Core;component/Assets/PhotoMode/ico_delete_h.png"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="ls:ButtonHelper.ImageSource" Value="{StaticResource MediumRoundButtonPressed}"/>
                <Setter Property="ls:ButtonHelper.SecondaryImageSource" Value="pack://application:,,,/Core;component/Assets/PhotoMode/ico_delete_h.png"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="MoveUpButtonStyle" TargetType="Button">
        <Setter Property="Template" Value="{StaticResource IconButtonHelperTemplate}"/>
        <Setter Property="ls:ButtonHelper.ImageSource" Value="{StaticResource MediumRoundButtonDefault}"/>
        <Setter Property="ls:ButtonHelper.SecondaryImageSource" Value="pack://application:,,,/Core;component/Assets/Shared/ico_upArrow_h.png"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="ls:ButtonHelper.ImageSource" Value="{StaticResource MediumRoundButtonHighlight}"/>
                <Setter Property="ls:ButtonHelper.SecondaryImageSource" Value="pack://application:,,,/Core;component/Assets/Shared/ico_upArrow_h.png"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="ls:ButtonHelper.ImageSource" Value="{StaticResource MediumRoundButtonPressed}"/>
                <Setter Property="ls:ButtonHelper.SecondaryImageSource" Value="pack://application:,,,/Core;component/Assets/Shared/ico_upArrow_h.png"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="MoveDownButtonStyle" TargetType="Button">
        <Setter Property="Template" Value="{StaticResource IconButtonHelperTemplate}"/>
        <Setter Property="ls:ButtonHelper.ImageSource" Value="{StaticResource MediumRoundButtonDefault}"/>
        <Setter Property="ls:ButtonHelper.SecondaryImageSource" Value="pack://application:,,,/Core;component/Assets/Shared/ico_downArrow_d.png"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="ls:ButtonHelper.ImageSource" Value="{StaticResource MediumRoundButtonHighlight}"/>
                <Setter Property="ls:ButtonHelper.SecondaryImageSource" Value="pack://application:,,,/Core;component/Assets/Shared/ico_downArrow_h.png"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="ls:ButtonHelper.ImageSource" Value="{StaticResource MediumRoundButtonPressed}"/>
                <Setter Property="ls:ButtonHelper.SecondaryImageSource" Value="pack://application:,,,/Core;component/Assets/Shared/ico_downArrow_h.png"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="RestoreButtonStyle" TargetType="Button">
        <Setter Property="Margin" Value="-40,40,0,0"/>
        <Setter Property="Template" Value="{StaticResource IconButtonHelperTemplate}"/>
        <Setter Property="ls:ButtonHelper.ImageSource" Value="{StaticResource SmallRoundButtonDefault}"/>
        <Setter Property="ls:ButtonHelper.SecondaryImageSource" Value="pack://application:,,,/Core;component/Assets/Shared/ico_reset_d.png"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="ls:ButtonHelper.ImageSource" Value="{StaticResource SmallRoundButtonHighlight}"/>
                <Setter Property="ls:ButtonHelper.SecondaryImageSource" Value="pack://application:,,,/Core;component/Assets/Shared/ico_reset_h.png"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="ls:ButtonHelper.ImageSource" Value="{StaticResource SmallRoundButtonPressed}"/>
                <Setter Property="ls:ButtonHelper.SecondaryImageSource" Value="pack://application:,,,/Core;component/Assets/Shared/ico_reset_h.png"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="MinimizeButtonStyle" TargetType="ToggleButton">
        <Setter Property="Template" Value="{StaticResource IconButtonHelperTemplate}"/>
        <Setter Property="ls:ButtonHelper.ImageSource" Value="{StaticResource SmallRoundButtonDefault}"/>
        <Setter Property="ls:ButtonHelper.SecondaryImageSource" Value="{StaticResource DropdownOpenButtonDefault}"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="ls:ButtonHelper.ImageSource" Value="{StaticResource SmallRoundButtonHighlight}"/>
                <Setter Property="ls:ButtonHelper.SecondaryImageSource" Value="{StaticResource DropdownOpenButtonHighlight}"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="ls:ButtonHelper.ImageSource" Value="{StaticResource SmallRoundButtonPressed}"/>
                <Setter Property="ls:ButtonHelper.SecondaryImageSource" Value="{StaticResource DropdownOpenButtonHighlight}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <ControlTemplate x:Key="SimpleBackgroundButtonTemplate" TargetType="Button">
        <Grid>
            <Image x:Name="BtnBg" Source="{TemplateBinding ls:ButtonHelper.ImageSource}" Stretch="None"/>
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>
    </ControlTemplate>

    <Style x:Key="PhotoModeBrownButtonStyle" TargetType="Button">
        <Setter Property="Template" Value="{StaticResource SimpleBackgroundButtonTemplate}"/>
        <Setter Property="ls:ButtonHelper.ImageSource" Value="{StaticResource BrownButtonBackgroundDefault}"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="ls:ButtonHelper.ImageSource" Value="{StaticResource BrownButtonBackgroundHighlight}"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="ls:ButtonHelper.ImageSource" Value="{StaticResource BrownButtonBackgroundPressed}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="ls:ButtonHelper.ImageSource" Value="{StaticResource RedButtonBackgroundDisabled}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="AlertButtonStyle" TargetType="Button">
        <Setter Property="Template" Value="{StaticResource BaseButtonTemplate}"/>
        <Setter Property="ls:ButtonHelper.ImageSource" Value="{StaticResource AlertButtonBackgroundDefault}"/>
        <Setter Property="Padding" Value="18,15"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="ls:ButtonHelper.ImageSource" Value="{StaticResource AlertButtonBackgroundHighlight}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="ls:ButtonHelper.ImageSource" Value="{StaticResource AlertButtonBackgroundDisabled}" />
            </Trigger>
        </Style.Triggers>
        <Style.Resources>
            <Thickness x:Key="Slices">35</Thickness>
        </Style.Resources>
    </Style>
    
    <Style x:Key="ButtonPromptBG" TargetType="ls:LSNineSliceImage" BasedOn="{StaticResource {x:Type ls:LSNineSliceImage}}">
        <Setter Property="ImageSource" Value="{StaticResource PicturePromptBackground}"/>
        <Setter Property="Slices" Value="32,0"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="Margin" Value="-8,-7"/>
    </Style>

    <Style x:Key="SliderBG" TargetType="ls:LSNineSliceImage" BasedOn="{StaticResource {x:Type ls:LSNineSliceImage}}">
        <Setter Property="ImageSource" Value="{StaticResource PhotoModeSliderBGDefault}"/>
        <Setter Property="Slices" Value="58,0"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
    </Style>

    <ControlTemplate x:Key="ThumbTemplate" TargetType="Thumb">
        <Image x:Name="ico" Source="{TemplateBinding ls:ButtonHelper.ImageSource}" Stretch="None"/>
    </ControlTemplate>

    <Style x:Key="ThumbStyle" TargetType="ls:LSThumb">
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="SoundID" Value="UI_HUD_Shared_Slider_Thumb"/>
        <Setter Property="Template" Value="{StaticResource ThumbTemplate}"/>
        <Setter Property="ls:ButtonHelper.ImageSource" Value="{StaticResource PhotoModeSliderBGThumbDefault}"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="ls:ButtonHelper.ImageSource" Value="{StaticResource PhotoModeSliderBGThumbHighlight}"/>
            </Trigger>
            <Trigger Property="IsDragging" Value="True">
                <Setter Property="ls:ButtonHelper.ImageSource" Value="{StaticResource PhotoModeSliderBGThumbPressed}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="ls:ButtonHelper.ImageSource" Value="{StaticResource PhotoModeSliderBGThumbDisabled}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <ControlTemplate x:Key="SliderRepeatButtonTemplate" TargetType="{x:Type ls:LSRepeatButton}">
        <ControlTemplate.Resources>
            <BitmapImage x:Key="DefaultButtonImage" UriSource="pack://application:,,,/Core;component/Assets/CharacterCreation/input_arrow_d.png"/>
        </ControlTemplate.Resources>

        <Grid>
            <Ellipse x:Name="Hitbox" Fill="Transparent" Width="60" Height="60"/>
            <Image x:Name="img" Stretch="None" Source="{StaticResource DefaultButtonImage}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>

        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="img" Property="Source" Value="pack://application:,,,/Core;component/Assets/CharacterCreation/input_arrow_h.png"/>
            </Trigger>

            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="img" Property="Opacity" Value="{StaticResource DisabledOpacity}"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <DataTemplate x:Key="ActorSettingsNameTextBlockTemplate" DataType="{x:Type ls:VMActorSettings}">
        <TextBlock Name="ComboBoxListLabel" Text="{Binding Actor.Name}" Foreground="{StaticResource LS_accent75TxtColor}" Background="Transparent" FontSize="{StaticResource ScaledMediumFontSize}" Padding="16,4" TextTrimming="CharacterEllipsis"/>
        <DataTemplate.Triggers>
            <DataTrigger Binding="{Binding Path=IsMouseOver, RelativeSource={RelativeSource AncestorType={x:Type ComboBoxItem}}}" Value="True">
                <Setter TargetName="ComboBoxListLabel" Property="Foreground" Value="{StaticResource LS_extraspecialTxtColor}"/>
            </DataTrigger>
        </DataTemplate.Triggers>
    </DataTemplate>

    <ControlTemplate x:Key="SliderTemplatePM" TargetType="Slider">
        <Grid HorizontalAlignment="Center">
            <b:Interaction.Triggers>
                <b:PropertyChangedTrigger Binding="{Binding Value, RelativeSource={RelativeSource TemplatedParent}}">
                    <ls:LSPlaySound Sound="UI_HUD_SplitItem_Step"/>
                </b:PropertyChangedTrigger>
            </b:Interaction.Triggers>
            <ls:LSNineSliceImage x:Name="bg" Style="{StaticResource SliderBG}"/>

            <ls:LSRepeatButton x:Name="PART_DecreaseRepeatButton" HorizontalAlignment="Left" Template="{StaticResource SliderRepeatButtonTemplate}" SoundID="UI_HUD_Shared_Slider_LeftButton" Command="Slider.DecreaseLarge" />
            <ls:LSRepeatButton x:Name="PART_IncreaseRepeatButton" HorizontalAlignment="Right" Template="{StaticResource SliderRepeatButtonTemplate}" SoundID="UI_HUD_Shared_Slider_RightButton"  Command="Slider.IncreaseLarge" RenderTransformOrigin="0.5, 0.5">
                <ls:LSRepeatButton.RenderTransform>
                    <ScaleTransform ScaleX="-1"/>
                </ls:LSRepeatButton.RenderTransform>
            </ls:LSRepeatButton>

            <Track Name="PART_Track" HorizontalAlignment="Stretch" VerticalAlignment="Center" Margin="58,0" Height="{TemplateBinding Height}">
                <Track.DecreaseRepeatButton>
                    <ls:LSRepeatButton x:Name="PART_TrackBefore" Style="{StaticResource Style.Slider.RepeatButton}" Command="Slider.DecreaseLarge"/>
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                    <ls:LSThumb Style="{StaticResource ThumbStyle}" />
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                    <ls:LSRepeatButton x:Name="PART_TrackAfter" Style="{StaticResource Style.Slider.RepeatButton}" Command="Slider.IncreaseLarge"/>
                </Track.IncreaseRepeatButton>
            </Track>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="ImageSource" Value="{StaticResource PhotoModeSliderBGHighlight}" TargetName="bg"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="SliderTemplateLeftBarPM" TargetType="Slider">
        <Grid HorizontalAlignment="Center">
            <b:Interaction.Triggers>
                <b:PropertyChangedTrigger Binding="{Binding Value, RelativeSource={RelativeSource TemplatedParent}}">
                    <ls:LSPlaySound Sound="UI_HUD_SplitItem_Step"/>
                </b:PropertyChangedTrigger>
            </b:Interaction.Triggers>
            <ls:LSNineSliceImage x:Name="bg" Style="{StaticResource SliderBG}"/>

            <ls:LSRepeatButton x:Name="PART_DecreaseRepeatButton" HorizontalAlignment="Left" Template="{StaticResource SliderRepeatButtonTemplate}" SoundID="UI_HUD_Shared_Slider_LeftButton" Command="Slider.DecreaseLarge" />
            <ls:LSRepeatButton x:Name="PART_IncreaseRepeatButton" HorizontalAlignment="Right" Template="{StaticResource SliderRepeatButtonTemplate}" SoundID="UI_HUD_Shared_Slider_RightButton"  Command="Slider.IncreaseLarge" RenderTransformOrigin="0.5, 0.5">
                <ls:LSRepeatButton.RenderTransform>
                    <ScaleTransform ScaleX="-1"/>
                </ls:LSRepeatButton.RenderTransform>
            </ls:LSRepeatButton>

            <Path Margin="58,0" HorizontalAlignment="Left" VerticalAlignment="Center" Style="{StaticResource OriginSliderStyle}"/>
            
            <Track Name="PART_Track" HorizontalAlignment="Stretch" VerticalAlignment="Center" Margin="58,0" Height="{TemplateBinding Height}">
                <Track.DecreaseRepeatButton>
                    <ls:LSRepeatButton x:Name="PART_TrackBefore" Background="{StaticResource LS_baseTxtColor}" Style="{StaticResource Style.Slider.RepeatButton}" Padding="0,30" Command="Slider.DecreaseLarge"/>
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                    <ls:LSThumb Style="{StaticResource ThumbStyle}" />
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                    <ls:LSRepeatButton x:Name="PART_TrackAfter" Style="{StaticResource Style.Slider.RepeatButton}" Command="Slider.IncreaseLarge"/>
                </Track.IncreaseRepeatButton>
            </Track>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="ImageSource" Value="{StaticResource PhotoModeSliderBGHighlight}" TargetName="bg"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="SliderTemplateCenterBarPM" TargetType="Slider">
        <Grid HorizontalAlignment="Center">
            <b:Interaction.Triggers>
                <b:PropertyChangedTrigger Binding="{Binding Value, RelativeSource={RelativeSource TemplatedParent}}">
                    <ls:LSPlaySound Sound="UI_HUD_SplitItem_Step"/>
                </b:PropertyChangedTrigger>
            </b:Interaction.Triggers>
            <ls:LSNineSliceImage x:Name="bg" Style="{StaticResource SliderBG}"/>

            <ls:LSRepeatButton x:Name="PART_DecreaseRepeatButton" HorizontalAlignment="Left" Template="{StaticResource SliderRepeatButtonTemplate}" SoundID="UI_HUD_Shared_Slider_LeftButton" Command="Slider.DecreaseLarge" />
            <ls:LSRepeatButton x:Name="PART_IncreaseRepeatButton" HorizontalAlignment="Right" Template="{StaticResource SliderRepeatButtonTemplate}" SoundID="UI_HUD_Shared_Slider_RightButton"  Command="Slider.IncreaseLarge" RenderTransformOrigin="0.5, 0.5">
                <ls:LSRepeatButton.RenderTransform>
                    <ScaleTransform ScaleX="-1"/>
                </ls:LSRepeatButton.RenderTransform>
            </ls:LSRepeatButton>

            <Path HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource OriginSliderStyle}"/>
            
            <Track Name="PART_Track" HorizontalAlignment="Stretch" VerticalAlignment="Center" Margin="58,0" Height="{TemplateBinding Height}">
                <Track.DecreaseRepeatButton>
                    <ls:LSRepeatButton x:Name="PART_TrackBefore" Style="{StaticResource Style.Slider.RepeatButton}" Padding="0,30" Background="{StaticResource LS_baseTxtColor}" Command="Slider.DecreaseLarge">
                        <ls:LSRepeatButton.Template>
                            <ControlTemplate TargetType="RepeatButton">
                                <Grid Background="Transparent">
                                    <Border HorizontalAlignment="Right" Background="{TemplateBinding Background}" CornerRadius="{DynamicResource Corner.Border2}" Margin="{TemplateBinding Padding}">
                                        <Border.Width>
                                            <MultiBinding Converter="{StaticResource SubtractionMultipleConverter}">
                                                <Binding Path="ActualWidth" ElementName="PART_TrackBefore" Converter="{StaticResource DivideConverter}" ConverterParameter="2"/>
                                                <Binding Path="ActualWidth" ElementName="PART_TrackAfter" Converter="{StaticResource DivideConverter}" ConverterParameter="2"/>
                                                <Binding Path="ActualWidth" ElementName="Thumb" Converter="{StaticResource DivideConverter}" ConverterParameter="2"/>
                                            </MultiBinding>
                                        </Border.Width>
                                    </Border>
                                </Grid>
                            </ControlTemplate>
                        </ls:LSRepeatButton.Template>
                    </ls:LSRepeatButton>
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                    <ls:LSThumb x:Name="Thumb" Style="{StaticResource ThumbStyle}" />
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                    <ls:LSRepeatButton x:Name="PART_TrackAfter" Style="{StaticResource Style.Slider.RepeatButton}" Padding="0,30" Background="{StaticResource LS_baseTxtColor}" Command="Slider.IncreaseLarge">
                        <ls:LSRepeatButton.Template>
                            <ControlTemplate TargetType="RepeatButton">
                                <Grid Background="Transparent" >
                                    <Border x:Name="SliderFiller" HorizontalAlignment="Left" Background="{TemplateBinding Background}" CornerRadius="{DynamicResource Corner.Border2}" Margin="{TemplateBinding Padding}">
                                        <Border.Width>
                                            <MultiBinding Converter="{StaticResource SubtractionMultipleConverter}">
                                                <Binding Path="ActualWidth" ElementName="PART_TrackAfter" Converter="{StaticResource DivideConverter}" ConverterParameter="2"/>
                                                <Binding Path="ActualWidth" ElementName="PART_TrackBefore" Converter="{StaticResource DivideConverter}" ConverterParameter="2"/>
                                                <Binding Path="ActualWidth" ElementName="Thumb" Converter="{StaticResource DivideConverter}" ConverterParameter="2"/>
                                            </MultiBinding>
                                        </Border.Width>
                                    </Border>
                                </Grid>
                            </ControlTemplate>
                        </ls:LSRepeatButton.Template>
                    </ls:LSRepeatButton>
                </Track.IncreaseRepeatButton>
            </Track>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="ImageSource" Value="{StaticResource PhotoModeSliderBGHighlight}" TargetName="bg"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="PhotoModeSliderStyle" TargetType="Slider">
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="Width" Value="{StaticResource ControlWidth}"/>
        <Setter Property="Template" Value="{StaticResource SliderTemplatePM}" />
    </Style>

    <Style x:Key="PhotoModeSliderLeftBarStyle" TargetType="Slider">
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="Width" Value="{StaticResource ControlWidth}"/>
        <Setter Property="Template" Value="{StaticResource SliderTemplateLeftBarPM}" />
    </Style>

    <Style x:Key="PhotoModeSliderCenterBarStyle" TargetType="Slider">
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="Width" Value="{StaticResource ControlWidth}"/>
        <Setter Property="Template" Value="{StaticResource SliderTemplateCenterBarPM}" />
    </Style>

    <Style x:Key="ComboBoxBG" TargetType="ls:LSNineSliceImage" BasedOn="{StaticResource {x:Type ls:LSNineSliceImage}}">
        <Setter Property="ImageSource" Value="{StaticResource PhotoModeComboBoxBGDefault}"/>
        <Setter Property="Slices" Value="40,0"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
    </Style>

    <Style x:Key="PhotoModeTickBoxStyle" TargetType="CheckBox">
        <Setter Property="Template" Value="{StaticResource CheckBoxTemplate}" />
        <Setter Property="ls:ButtonHelper.ImageSource" Value="pack://application:,,,/Core;component/Assets/Options/checkBox_d.png" />
        <Setter Property="ls:ButtonHelper.SecondaryImageSource" Value="pack://application:,,,/Core;component/Assets/Options/ico_check_d.png" />
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="ls:ButtonHelper.ImageSource" Value="pack://application:,,,/Core;component/Assets/Options/checkBox_disabled.png"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="ls:ButtonHelper.ImageSource" Value="pack://application:,,,/Core;component/Assets/Options/checkBox_h.png"/>
                <Setter Property="ls:ButtonHelper.SecondaryImageSource" Value="pack://application:,,,/Core;component/Assets/Options/ico_check_h.png"/>
            </Trigger>
            <Trigger Property="IsPressed"  Value="True">
                <Setter Property="ls:ButtonHelper.ImageSource" Value="pack://application:,,,/Core;component/Assets/Options/checkBox_p.png"/>
                <Setter Property="ls:ButtonHelper.SecondaryImageSource" Value="pack://application:,,,/Core;component/Assets/Options/ico_check_h.png"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="PhotoModeComboBoxItem" TargetType="{x:Type ComboBoxItem}">
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="Width" Value="{StaticResource ControlWidth}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Grid Background="Transparent">
                        <Image x:Name="highlightBG" Source="pack://application:,,,/Core;component/Assets/CharacterPanel/selector_listitem_d.png" Stretch="None"/>
                        <ContentPresenter/>
                        <b:Interaction.Triggers>
                            <b:EventTrigger EventName="MouseEnter">
                                <ls:LSPlaySound Sound="UI_Shared_Hover"/>
                            </b:EventTrigger>
                        </b:Interaction.Triggers>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="highlightBG" Property="Source" Value="pack://application:,,,/Core;component/Assets/CharacterPanel/selector_listitem_h.png"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="CharacterComboBoxItem" BasedOn="{StaticResource PhotoModeComboBoxItem}" TargetType="{x:Type ComboBoxItem}">
        <Setter Property="ContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <TextBlock Name="ComboBoxListLabel" Text="{Binding Name}" Foreground="{StaticResource LS_accent75TxtColor}" Background="Transparent" FontSize="{StaticResource ScaledMediumFontSize}" Padding="16,4" TextTrimming="CharacterEllipsis"/>
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <ControlTemplate x:Key="ComboBoxToggleButtonTemplate" TargetType="ToggleButton">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ls:LSNineSliceImage Grid.Column="0" Grid.ColumnSpan="2" x:Name="Box" Width="{TemplateBinding Width}" Height="80" Style="{StaticResource ComboBoxBG}"/>
            <TextBlock Grid.Column="0" x:Name="txt" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="48,4,0,0" MaxWidth="{Binding Width, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource AddConverter}, ConverterParameter=-128}" Text="{Binding Text, RelativeSource={RelativeSource AncestorType={x:Type ComboBox}}}" Foreground="{StaticResource LS_accent100TxtColor}" IsHitTestVisible="False" FontSize="{StaticResource ScaledDefaultFontSize}"/>
            <Image Grid.Column="1" x:Name="bg" Source="{StaticResource SmallRoundButtonDefault}" Stretch="None" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="14,0"/>
            <Image Grid.Column="1" x:Name="img" Source="{StaticResource DropdownClosedButtonDefault}" Stretch="None" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Box" Property="ImageSource" Value="{StaticResource PhotoModeComboBoxBGHighlight}"/>
                <Setter TargetName="bg" Property="Source" Value="{StaticResource SmallRoundButtonDefault}" />
                <Setter TargetName="img" Property="Source" Value="{StaticResource DropdownClosedButtonHighlight}" />
                <Setter TargetName="txt" Property="Foreground" Value="{StaticResource LS_tint100}"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="bg" Property="Source" Value="{StaticResource SmallRoundButtonHighlight}" />
                <Setter TargetName="img" Property="Source" Value="{StaticResource DropdownClosedButtonHighlight}" />
            </Trigger>
            <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="img" Property="Source" Value="{StaticResource DropdownOpenButtonDefault}" />
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True"/>
                    <Condition Property="IsChecked" Value="True"/>
                </MultiTrigger.Conditions>
                <Setter TargetName="img" Property="Source" Value="{StaticResource DropdownOpenButtonHighlight}" />
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsPressed" Value="True"/>
                    <Condition Property="IsChecked" Value="True"/>
                </MultiTrigger.Conditions>
                <Setter TargetName="img" Property="Source" Value="{StaticResource DropdownOpenButtonHighlight}" />
            </MultiTrigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="ComboBoxTemplate" TargetType="{x:Type ComboBox}">
        <Grid>
            <ToggleButton HorizontalAlignment="Right" Foreground="{StaticResource LS_accent100TxtColor}" IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" Template="{StaticResource ComboBoxToggleButtonTemplate}"/>
            <Popup Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}">
                <ls:LSNineSliceImage Style="{StaticResource PopoutNineSliceStyle}" Slices="42" Width="{TemplateBinding Width}">
                    <ScrollViewer VerticalAlignment="Top" VerticalScrollBarVisibility="Auto" x:Name="content" MaxHeight="{TemplateBinding MaxDropDownHeight}" Template="{StaticResource ScrollViewerTemplate}">
                        <ScrollViewer.Resources>
                            <Style TargetType="{x:Type ScrollBar}" BasedOn="{StaticResource BaseScrollBar}"/>
                        </ScrollViewer.Resources>
                        <StackPanel VerticalAlignment="Center" IsItemsHost="True" Width="{Binding Width, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource AddConverter}, ConverterParameter=-100}" Background="Transparent" KeyboardNavigation.DirectionalNavigation="Contained"/>
                    </ScrollViewer>
                </ls:LSNineSliceImage>
            </Popup>
        </Grid>
    </ControlTemplate>

    <Style x:Key="PhotoModeComboBoxStyle" TargetType="{x:Type ComboBox}">
        <Setter Property="Width" Value="{StaticResource ControlWidth}"/>
        <Setter Property="MaxDropDownHeight" Value="480"/>
        <Setter Property="Template" Value="{StaticResource ComboBoxTemplate}">
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="{StaticResource DisabledOpacity}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="PhotoModePanelBG" TargetType="ls:LSNineSliceImage" BasedOn="{StaticResource {x:Type ls:LSNineSliceImage}}">
        <Setter Property="ImageSource" Value="{StaticResource PhotoModePanelBackground}"/>
        <Setter Property="Slices" Value="0,264,0,200"/>
        <Setter Property="VerticalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="-54,-50"/>
    </Style>

    <Style x:Key="PhotoModeMinimizedPanelBG" TargetType="ls:LSNineSliceImage" BasedOn="{StaticResource {x:Type ls:LSNineSliceImage}}">
        <Setter Property="ImageSource" Value="{StaticResource CompactPhotoModePanelBackground}"/>
        <Setter Property="Slices" Value="0,180,0,100"/>
        <Setter Property="VerticalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="-54,-30"/>
        <Setter Property="Visibility" Value="Collapsed"/>
    </Style>

    <Style x:Key="TabPanelSideDecor" TargetType="Image">
        <Setter Property="Source" Value="{StaticResource PhotoModePanelTabDecor}"/>
        <Setter Property="Stretch" Value="None"/>
        <Setter Property="Margin" Value="-7,0,-7,-7"/>
    </Style>

    <Style x:Key="PhotoModeTabItemStyle" TargetType="TabItem" BasedOn="{StaticResource {x:Type HeaderedContentControl}}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Grid x:Name="TabRoot" ToolTipService.Placement="Left" HorizontalAlignment="Center">
                        <Image x:Name="tabBG" Source="{StaticResource TabDecorBackgroundInactive}" Margin="-3,0" Stretch="None"/>
                        <ContentPresenter ContentSource="Header"/>
                        <b:Interaction.Triggers>
                            <b:EventTrigger EventName="MouseEnter">
                                <b:Interaction.Behaviors>
                                    <b:ConditionBehavior>
                                        <b:ConditionalExpression>
                                            <b:ComparisonCondition LeftOperand="{TemplateBinding IsSelected}" Operator="NotEqual" RightOperand="True"/>
                                        </b:ConditionalExpression>
                                    </b:ConditionBehavior>
                                </b:Interaction.Behaviors>
                                <ls:LSPlaySound Sound="UI_Shared_Hover"/>
                            </b:EventTrigger>
                            <b:EventTrigger EventName="MouseDown">
                                <b:Interaction.Behaviors>
                                    <b:ConditionBehavior>
                                        <b:ConditionalExpression>
                                            <b:ComparisonCondition LeftOperand="{TemplateBinding IsSelected}" Operator="NotEqual" RightOperand="True"/>
                                        </b:ConditionalExpression>
                                    </b:ConditionBehavior>
                                </b:Interaction.Behaviors>
                                <ls:LSPlaySound Sound="UI_Shared_Panel_Select"/>
                            </b:EventTrigger>
                        </b:Interaction.Triggers>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="tabBG" Property="Source" Value="{StaticResource TabDecorBackgroundInactiveHighlight}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="tabBG" Property="Source" Value="{StaticResource TabDecorBackgroundDefault}"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True"/>
                                <Condition Property="IsMouseOver" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="tabBG" Property="Source" Value="{StaticResource TabDecorBackgroundHighlight}"/>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="FocusVisualStyle" Value="{StaticResource Style.Focus.Inner.Uniform}"/>
    </Style>

    <Style x:Key="PhotoModeTabPanel" TargetType="TabControl" BasedOn="{StaticResource {x:Type ItemsControl}}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate>
                    <Grid x:Name="PhotoModePanelContainer">
                        <Grid.RenderTransform>
                            <TransformGroup>
                                <ScaleTransform/>
                                <SkewTransform/>
                                <RotateTransform/>
                                <TranslateTransform/>
                            </TransformGroup>
                        </Grid.RenderTransform>
                        <b:Interaction.Behaviors>
                            <ls:LSMouseDragElementBehavior HitArea="{Binding ElementName=DragHit,Mode=OneWay}" MoveArea="{Binding RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}"/>
                        </b:Interaction.Behaviors>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!--MAIN PANEL-->
                        <Grid x:Name="ContentContainer" Grid.Row="1" Width="1260">
                            <ls:LSNineSliceImage x:Name="PanelBackground" Style="{StaticResource PhotoModePanelBG}"/>
                            <ls:LSNineSliceImage x:Name="MinimizedBackground" Style="{StaticResource PhotoModeMinimizedPanelBG}"/>
                            <ls:LSToggleButton x:Name="MinimizeButton" IsChecked="False" Style="{StaticResource MinimizeButtonStyle}" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="40" />
                            <ContentPresenter x:Name="PanelContent" ContentSource="SelectedContent" Margin="80,0,80,80"/>
                        </Grid>

                        <!--DRAG AREA-->
                        <Border x:Name="DragHit" Grid.Row="0" Grid.RowSpan="2" Height="140" HorizontalAlignment="Stretch" Background="Transparent" VerticalAlignment="Top">
                            <b:Interaction.Triggers>
                                <b:EventTrigger EventName="MouseEnter">
                                    <b:InvokeCommandAction Command="{Binding DataContext.SetCursorCommand, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}" CommandParameter="Resize"/>
                                </b:EventTrigger>
                                <b:EventTrigger EventName="MouseLeave">
                                    <b:InvokeCommandAction Command="{Binding DataContext.SetCursorCommand, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}" CommandParameter=""/>
                                </b:EventTrigger>
                            </b:Interaction.Triggers>
                        </Border>

                        <!--HEADER-->
                        <StackPanel x:Name="HeaderContainer" Orientation="Horizontal" Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Bottom" >
                            <Image Style="{StaticResource TabPanelSideDecor}" IsHitTestVisible="False" VerticalAlignment="Bottom" Stretch="None"/>
                            <TabPanel x:Name="HeaderPanel" IsItemsHost="True" KeyboardNavigation.TabIndex="1" VerticalAlignment="Bottom"/>
                            <Image Style="{StaticResource TabPanelSideDecor}" IsHitTestVisible="False" RenderTransformOrigin="0.5,0.5" VerticalAlignment="Bottom" Stretch="None">
                                <Image.RenderTransform>
                                    <ScaleTransform ScaleX="-1"/>
                                </Image.RenderTransform>
                            </Image>
                        </StackPanel>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger SourceName="MinimizeButton" Property="IsChecked" Value="True" >
                            <Setter TargetName="MinimizedBackground" Property="Visibility" Value="Visible" />
                            <Setter TargetName="PanelBackground" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="PanelContent" Property="IsEnabled" Value="False" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ItemContainerStyle" Value="{StaticResource PhotoModeTabItemStyle}"/>
    </Style>
    
    <Style x:Key="LabelTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{StaticResource ScaledDefaultFontSize}"/>
        <Setter Property="Foreground" Value="{StaticResource LS_accent75TxtColor}"/>
    </Style>

    <Style x:Key="TitleTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{StaticResource ScaledDefaultFontSize}"/>
        <Setter Property="Foreground" Value="{StaticResource LS_extraspecialTxtColor}"/>
        <Setter Property="Margin" Value="0,40,0,20"/>
        <Setter Property="FontWeight" Value="DemiBold"/>
    </Style>

    <Style x:Key="ValueTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{StaticResource ScaledDefaultFontSize}"/>
        <Setter Property="Foreground" Value="{StaticResource LS_extraspecialTxtColor}"/>
    </Style>

    <Style x:Key="AlertTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{StaticResource ScaledTinyFontSize}"/>
        <Setter Property="Foreground" Value="{StaticResource LS_alertTxtColor}"/>
    </Style>

</ResourceDictionary>