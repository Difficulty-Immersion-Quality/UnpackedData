<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			        xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
					xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:System="clr-namespace:System;assembly=mscorlib"
                    xmlns:ls="clr-namespace:ls;assembly=Code"
                    xmlns:noesis="clr-namespace:NoesisGUIExtensions;assembly=Noesis.GUI.Extensions"
					mc:Ignorable="d">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Core;component/Library/CharacterSheetTemplates.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <!-- Icons -->

    <ControlTemplate x:Key="ScrolllessListBox" TargetType="ls:LSListBox">
        <ItemsPresenter/>
    </ControlTemplate>

    <!-- Spellbook -->
    <Style x:Key="SpellBookSlot" TargetType="ls:LSButton">
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ls:LSButton">
                    <Grid>
                        <Rectangle Fill="{Binding Icon}" Width="108" Height="108"/>
                        <TextBlock Text="{Binding SpellSlotLevel, Converter={StaticResource DecimalToRomanConverter}}" Margin="0,0,8,4" Foreground="{StaticResource LS_specialTxtColor}" VerticalAlignment="Bottom" HorizontalAlignment="Right" Height="35" FontSize="32" FontWeight="Bold" noesis:Text.Stroke="#FF000000" noesis:Text.StrokeThickness="5"/>
                        <Border x:Name="NotPreparedFrame" BorderBrush="#bcbcbc" Background="#aa000000" BorderThickness="0" Visibility="Hidden"/>
                        <Border x:Name="PreparedFrame" BorderBrush="#bcbcbc" BorderThickness="4" Visibility="Hidden"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding NeedsActivation}" Value="True"/>
                                <Condition Binding="{Binding Activated}" Value="True"/>
                            </MultiDataTrigger.Conditions>
                            <MultiDataTrigger.Setters>
                                <Setter Property="Visibility" Value="Visible" TargetName="PreparedFrame"/>
                            </MultiDataTrigger.Setters>
                        </MultiDataTrigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding NeedsActivation}" Value="True"/>
                                <Condition Binding="{Binding Activated}" Value="False"/>
                            </MultiDataTrigger.Conditions>
                            <MultiDataTrigger.Setters>
                                <Setter Property="Visibility" Value="Visible" TargetName="NotPreparedFrame"/>
                            </MultiDataTrigger.Setters>
                        </MultiDataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <DataTemplate x:Key="SpellTemplate">
        <ls:LSButton Style="{StaticResource SpellBookSlot}" Command="{Binding DataContext.ToggleCharacterAction, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}" CommandParameter="{Binding}" Focusable="False" ToolTip="{StaticResource ManagedTooltip}" ToolTipService.Placement="Right" ToolTipService.HorizontalOffset="88" ToolTipService.VerticalOffset="88" />
    </DataTemplate>

    <ControlTemplate x:Key="CarouselButtonTemplate" TargetType="ls:LSButton">
        <Grid>
            <Image x:Name="arrow" Source="{DynamicResource ButtonNormal}" Stretch="None"/>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="ls:MoveFocus.IsFocused" Value="True">
                <Setter Property="Source" Value="{DynamicResource ButtonHighlight}" TargetName="arrow"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Source" Value="{DynamicResource ButtonPressed}" TargetName="arrow"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Source" Value="{DynamicResource ButtonDisabled}" TargetName="arrow"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="CarouselTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{DynamicResource MediumFontSize}"/>
        <Setter Property="FontWeight" Value="Regular"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Foreground" Value="{DynamicResource LS_PrimaryColor}"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Border}}}" Value="True">
                <Setter Property="Foreground" Value="{DynamicResource LS_alertTxtColor}"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="ToggleMultiSelectButtonStyle" TargetType="ls:LSButton">
        <Setter Property="BoundEvent" Value="UIAccept"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ls:LSButton">
                    <StackPanel x:Name="MultiSelectRoot" Orientation="Horizontal" Tag="{Binding FocusedElement, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}">
                        <ContentPresenter ContentTemplate="{StaticResource ControllerButtonHint}" Content="{Binding CurrentPlayer.UIData.InputEvents, Converter={StaticResource FindInputEventConverter}, ConverterParameter='UIAccept'}" Tag="{Binding Source=hd0c08193ga7f2g4a7eg8225g63748c7b3096,Converter={StaticResource TranslatedStringConverter}}"/>
                        <b:Interaction.Triggers>
                            <b:EventTrigger EventName="Click" SourceObject="{Binding RelativeSource={RelativeSource TemplatedParent}}">
                                <ls:ToggleListBoxItemSelected TargetObject="{Binding Tag, ElementName=MultiSelectRoot}"/>
                            </b:EventTrigger>
                            <b:PropertyChangedTrigger Binding="{Binding Tag, ElementName=MultiSelectRoot}">
                                <b:Interaction.Behaviors>
                                    <b:ConditionBehavior>
                                        <b:ConditionalExpression>
                                            <b:ComparisonCondition LeftOperand="{Binding Path=IsPressed, RelativeSource={RelativeSource TemplatedParent}}" Operator="Equal" RightOperand="True"/>
                                        </b:ConditionalExpression>
                                    </b:ConditionBehavior>
                                </b:Interaction.Behaviors>
                                <ls:ToggleListBoxItemSelected TargetObject="{Binding Tag, ElementName=MultiSelectRoot}"/>
                            </b:PropertyChangedTrigger>
                        </b:Interaction.Triggers>
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="StartMultiSelectButtonStyle" TargetType="ls:LSButton" BasedOn="{StaticResource ControllerDelayedHoldButtonStyle}">
        <Setter Property="EatInput" Value="False"/>
        <Setter Property="HoldTime" Value="0:0:0.6"/>
        <Setter Property="HoldDelay" Value="{StaticResource DelayedButtonTapTime}"/>
        <Setter Property="noesis:StyleInteraction.Triggers">
            <Setter.Value>
                <noesis:StyleTriggerCollection>
                    <b:EventTrigger EventName="LSButtonReleased">
                        <ls:LSPlaySound Sound="UI_Controller_DefaultHoldButton_Release"/>
                    </b:EventTrigger>

                    <b:EventTrigger EventName="Click">
                        <ls:ToggleListBoxItemSelected TargetObject="{Binding FocusedElement, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}"/>
                    </b:EventTrigger>
                </noesis:StyleTriggerCollection>
            </Setter.Value>
        </Setter>
        <Setter Property="Tag" Value="{Binding Source='h1ca1b236g5282g4349gabc4g96639808464f', Converter={StaticResource TranslatedStringConverter}}"/>
        <Setter Property="BoundEvent" Value="ContextMenu"/>
        <Setter Property="Content" Value="{Binding CurrentPlayer.UIData.InputEvents, Converter={StaticResource FindInputEventConverter}, ConverterParameter='ContextMenu'}"/>
    </Style>

    <ControlTemplate x:Key="Template.InventoryContainer" TargetType="ListBoxItem">
        <ls:LSEntityObject x:Name="CellRoot" Context="{Binding (ls:ContextMenuService.Context) , RelativeSource={RelativeSource Mode=TemplatedParent}}" ls:ContextMenuService.GenerateEnabled="False" DataContext="{Binding Object}" EntityRef="{Binding EntityHandle}" Background="{TemplateBinding Background}" ToolTipService.Placement="Right" ToolTipService.HorizontalOffset="0" ToolTipService.VerticalOffset="110" Focusable="True" ls:MoveFocus.Focusable="True" Tag="" FocusVisualStyle="{StaticResource Style.FocusVisualStyle}">
            <ls:LSEntityObject.ToolTip>
                <ls:LSTooltip Content="{Binding DataContext.Object,RelativeSource={RelativeSource Mode=TemplatedParent}}"/>
            </ls:LSEntityObject.ToolTip>
            <b:Interaction.Behaviors>
                <ls:ContextMenuFillBehavior Context="{Binding (ls:ContextMenuService.Context) , RelativeSource={RelativeSource Mode=TemplatedParent} }" Player="{Binding DataContext.CurrentPlayer, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}" TargetObject="{Binding .}" Source="{Binding SelectedItems,RelativeSource={RelativeSource AncestorType=ListBox}}" Command="{Binding DataContext.ContextActionPressed, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}" MultiSelectThreshold="1" RequireTargetInList="False"/>
            </b:Interaction.Behaviors>
            <b:Interaction.Triggers>
                <b:DataTrigger Binding="{Binding Path=(ls:MoveFocus.IsFocused), ElementName=CellRoot}" Value="True">
                    <b:InvokeCommandAction Command="{Binding DataContext.ClearItemNewFlag, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}" CommandParameter="{Binding .}"/>
                    <ls:LSPlaySound Sound="UI_Shared_Hover"/>
                </b:DataTrigger>
            </b:Interaction.Triggers>
            <ContentPresenter  IsHitTestVisible="False"/>
            <Image x:Name="SelectOverlay" Source="{StaticResource InvSlot_SelectionOverlay}" IsHitTestVisible="False" Visibility="Hidden" />
            <Rectangle x:Name="InactiveOverlay" Visibility="{Binding Visibility, ElementName=SelectOverlay, Converter={StaticResource InvertVisibilityConverter}}" Style="{StaticResource MultiselectInactiveOverlay}"/>
            <Image x:Name="FocusOverlay" Source="{StaticResource InvSlot_Selector}" IsHitTestVisible="False" Visibility="Hidden" />
        </ls:LSEntityObject>
        <ControlTemplate.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter TargetName="SelectOverlay" Property="Visibility" Value="Visible" />
                <!--can be removed if we get the focusedElement on the ListboxItem-->
                <Setter TargetName="CellRoot" Property="Tag" Value="IsSelected"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="FocusOverlay" Property="Visibility" Value="Visible" />
            </Trigger>
            <DataTrigger Binding="{Binding (ls:MultiSelect.IsEnabled), RelativeSource={RelativeSource AncestorType={x:Type ListBox}}}" Value="False">
                <Setter TargetName="InactiveOverlay" Property="Visibility" Value="Collapsed" />
            </DataTrigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="ControllerContainerSelectedItemTemplate" TargetType="Control">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="inventorySelectionName" Grid.Column="0" HorizontalAlignment="Left" Text="{Binding Name}" Style="{StaticResource PanelSubTitleStyle}" MaxWidth="810" TextAlignment="Left" TextTrimming="CharacterEllipsis" TextWrapping="NoWrap" Margin="0,6,0,18" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Column="1" >
                <TextBlock x:Name="containerSelectionValue" Text="{Binding Gold}" Style="{StaticResource PanelSubTitleStyle}" Margin="0,2,0,0" TextAlignment="Right" TextTrimming="CharacterEllipsis"  VerticalAlignment="Center" MaxHeight="100"/>
                <Image x:Name="GoldIcon" Visibility="{Binding Name, Converter={StaticResource StringToVisibilityConverter}, FallbackValue=Collapsed}" Source="{StaticResource IconGold}" Stretch="None" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,6,0,0"  />
            </StackPanel>
        </Grid>
    </ControlTemplate>

    <ControlTemplate x:Key="ControllerWeightBarTemplate" TargetType="Control">
        <ControlTemplate.Resources>
            <BitmapImage x:Key="PointerWeight" UriSource="pack://application:,,,/Core;component/Assets/Shared/Weight_Encumbrance/pointer_weight.png"/>
        </ControlTemplate.Resources>
        <Grid ToolTipService.Placement="Top" ToolTipService.VerticalOffset="4" Background="Transparent">
            <Grid.Resources>
                <Style TargetType="TextBlock" BasedOn="{StaticResource BarBaseTextStyle}"/>
            </Grid.Resources>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Weight icon -->
            <StackPanel Grid.Row="0" HorizontalAlignment="Right" Orientation="Horizontal">
                <StackPanel.Resources>
                    <Style TargetType="TextBlock" >
                        <Setter Property="FontWeight" Value="DemiBold" />
                        <Setter Property="VerticalAlignment" Value="Top" />
                        <Setter Property="Foreground" Value="{StaticResource LS_ActivePanelFontColor}" />
                        <Setter Property="FontSize" Value="{DynamicResource DefaultFontSize}" />
                    </Style>
                </StackPanel.Resources>
                
                <!-- Carry limit exceeded text -->
                <TextBlock x:Name="carryLimitText" Margin="0,0,0,0" MaxWidth="380" ClipToBounds="False" TextAlignment="Center" TextWrapping="Wrap" Foreground="{StaticResource LS_bar100TxtColor}"
                           Text="{Binding Source='h5f15514bgc4fcg4386ga1c3g65fbf8bc2ef5', Converter={StaticResource TranslatedStringConverter}}"
                           Canvas.Left="{Binding ActualWidth, RelativeSource={RelativeSource Self}, Converter={StaticResource DivideConverter}, ConverterParameter=-2}"/>
                
                <TextBlock x:Name="currentWeightText" Text="{Binding EncumbranceStats.CurrentWeight, Converter={StaticResource UnitConverter}, ConverterParameter='Weight N2'}" Margin="0,0,0,0"
                                       Canvas.Left="{Binding ActualWidth, RelativeSource={RelativeSource Self}, Converter={StaticResource DivideConverter}, ConverterParameter=-2}"/>

                <TextBlock Text="/" Margin="8,0,2,0"/>

                <!-- Max weight -->
                <TextBlock x:Name="maxWeightText" Grid.Column="2" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="8,0,0,0" Text="{Binding EncumbranceStats.WeightMaximum, Converter={StaticResource UnitConverter}, ConverterParameter='Weight N2'}"/>

                <Image x:Name="currentWeightIcon" Grid.Row="0" HorizontalAlignment="Right" VerticalAlignment="Top" Source="{StaticResource NormalWeightIcon}" Stretch="None" Margin="0,0,-6,0"/>

            </StackPanel>
            <!-- Weight bar -->
            <Grid Grid.Row="1" Margin="0,12,0,0">

                <!-- Bar -->
                <Grid HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0,12,0,0">


                    <Grid x:Name="EncumberenceBar" HorizontalAlignment="Left" VerticalAlignment="Top" Width="Auto" Height="14">
                        <!-- Encumbrance limits -->
                        <Grid HorizontalAlignment="Left" Width="Auto">
                            <Image Source="{StaticResource BarWeightHeavilyEncumberedDisabled}" Stretch="Fill"/>
                        </Grid>
                        <Grid HorizontalAlignment="Left">
                            <Grid.Width>
                                <MultiBinding Converter="{StaticResource MultiplierMultiConverter}">
                                    <Binding Path="EncumbranceStats.WeightHeavyPercentage"/>
                                    <Binding Path="ActualWidth" ElementName="EncumberenceBar"/>
                                </MultiBinding>
                            </Grid.Width>

                            <Image Source="{StaticResource BarWeightEncumberedDisabled}" Stretch="Fill"/>
                        </Grid>
                        <Image HorizontalAlignment="Left"
                               Source="{StaticResource BarWeightNormalDisabled}" Stretch="Fill">
                            <Image.Width>
                                <MultiBinding Converter="{StaticResource MultiplierMultiConverter}">
                                    <Binding Path="EncumbranceStats.WeightLightPercentage"/>
                                    <Binding Path="ActualWidth" ElementName="EncumberenceBar"/>
                                </MultiBinding>
                            </Image.Width>
                        </Image>

                        <!-- Current weight bar -->
                        <ProgressBar x:Name="currentWeightBar" Value="{Binding EncumbranceStats.CurrentWeight, Mode=OneWay}" Minimum="0" Maximum="{Binding EncumbranceStats.WeightMaximum}" Background="Transparent" BorderBrush="Transparent" IsHitTestVisible="False">
                            <ProgressBar.Foreground>
                                <ImageBrush ImageSource="{StaticResource BarWeightNormal}"/>
                            </ProgressBar.Foreground>
                        </ProgressBar>
                    </Grid>

                    <!-- Current weight pointer -->
                    <Grid HorizontalAlignment="Left" IsHitTestVisible="False">
                        <Grid.Width>
                            <MultiBinding Converter="{StaticResource MultiplierMultiConverter}">
                                <Binding Path="EncumbranceStats.WeightCurrentPercentage"/>
                                <Binding Path="ActualWidth" ElementName="EncumberenceBar"/>
                            </MultiBinding>
                        </Grid.Width>
                        <Canvas HorizontalAlignment="Right">
                            <Image x:Name="currentWeightPointer" Margin="-14,-25,0,0" ClipToBounds="False" Source="{StaticResource PointerWeight}" Stretch="None"/>
                        </Canvas>
                    </Grid>

                    <!-- Dividers -->
                    <Image HorizontalAlignment="Left" IsHitTestVisible="False" Margin="-2,0,0,0" Source="{StaticResource BarDivider}" Stretch="None">
                        <Image.RenderTransform>
                            <TranslateTransform>
                                <TranslateTransform.X>
                                    <MultiBinding Converter="{StaticResource MultiplierMultiConverter}">
                                        <Binding Path="EncumbranceStats.WeightLightPercentage"/>
                                        <Binding Path="ActualWidth" ElementName="EncumberenceBar"/>
                                    </MultiBinding>
                                </TranslateTransform.X>
                            </TranslateTransform>
                        </Image.RenderTransform>
                    </Image>

                    <Image HorizontalAlignment="Left" IsHitTestVisible="False" Margin="-2,0,0,0" Source="{StaticResource BarDivider}" Stretch="None">
                        <Image.RenderTransform>
                            <TranslateTransform>
                                <TranslateTransform.X>
                                    <MultiBinding Converter="{StaticResource MultiplierMultiConverter}">
                                        <Binding Path="EncumbranceStats.WeightHeavyPercentage"/>
                                        <Binding Path="ActualWidth" ElementName="EncumberenceBar"/>
                                    </MultiBinding>
                                </TranslateTransform.X>
                            </TranslateTransform>
                        </Image.RenderTransform>
                    </Image>
                </Grid>
            </Grid>
        </Grid>
        <ControlTemplate.Triggers>
            
            <DataTrigger Binding="{Binding EncumbranceStats.EncumberanceState}" Value="Light">
                <Setter TargetName="currentWeightIcon" Property="Source" Value="{StaticResource IconWeightEncumbered}"/>
                <Setter TargetName="currentWeightBar" Property="Foreground">
                    <Setter.Value>
                        <ImageBrush ImageSource="{StaticResource BarWeightEncumbered}"/>
                    </Setter.Value>
                </Setter>
                <Setter TargetName="currentWeightPointer" Property="Source" Value="pack://application:,,,/Core;component/Assets/Shared/Weight_Encumbrance/pointer_encumbrance.png"/>
                <Setter TargetName="currentWeightText" Property="Foreground" Value="{StaticResource LS_bar50TxtColor}"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding EncumbranceStats.EncumberanceState}" Value="Heavy">
                <Setter TargetName="currentWeightIcon" Property="Source" Value="pack://application:,,,/Core;component/Assets/Shared/Weight_Encumbrance/ico_weight_heavilyEncumbered.png"/>
                <Setter TargetName="currentWeightBar" Property="Foreground">
                    <Setter.Value>
                        <ImageBrush ImageSource="pack://application:,,,/Core;component/Assets/Shared/Weight_Encumbrance/bar_weight_heavilyEncumbered_d.png"/>
                    </Setter.Value>
                </Setter>
                <Setter TargetName="currentWeightPointer" Property="Source" Value="pack://application:,,,/Core;component/Assets/Shared/Weight_Encumbrance/pointer_heavyEncumbrance.png"/>
                <Setter TargetName="currentWeightText" Property="Foreground" Value="{StaticResource LS_bar100TxtColor}"/>
            </DataTrigger>

            <DataTrigger Binding="{Binding EncumbranceStats.IsOverEncumbared}" Value="False">
                <Setter TargetName="maxWeightText" Property="Foreground" Value="{StaticResource LS_extraspecialTxtColor}"/>
                <Setter TargetName="carryLimitText" Property="Visibility" Value="Collapsed"/>
            </DataTrigger>

            <DataTrigger Binding="{Binding EncumbranceStats.IsOverEncumbared}" Value="True">
                <Setter TargetName="currentWeightIcon" Property="Source" Value="pack://application:,,,/Core;component/Assets/Shared/Weight_Encumbrance/ico_weight_weightLimit.png"/>
                <Setter TargetName="maxWeightText" Property="Foreground" Value="{StaticResource LS_bar100TxtColor}"/>
                <Setter TargetName="carryLimitText" Property="Visibility" Value="Visible"/>
            </DataTrigger>

        </ControlTemplate.Triggers>
    </ControlTemplate>

</ResourceDictionary>