<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			        xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
					xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:System="clr-namespace:System;assembly=mscorlib"
                    xmlns:ls="clr-namespace:ls;assembly=Code"
                    xmlns:noesis="clr-namespace:NoesisGUIExtensions;assembly=Noesis.GUI.Extensions"
					mc:Ignorable="d">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="OptionTemplates_Shared.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <DataTemplate x:Key="SliderRepeatButtonTemplate" DataType="{x:Type ls:LSRepeatButton}">
        <DataTemplate.Resources>
            <BitmapImage x:Key="DefaultButtonImage" UriSource="pack://application:,,,/Core;component/Assets/CharacterCreation/input_arrow_d.png"/>
        </DataTemplate.Resources>

        <Grid>
            <Ellipse x:Name="Hitbox" Fill="Transparent" Width="60" Height="60"/>
            <Image x:Name="img" Stretch="None" Source="{StaticResource DefaultButtonImage}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>

        <DataTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="img" Property="Source" Value="pack://application:,,,/Core;component/Assets/CharacterCreation/input_arrow_h.png"/>
            </Trigger>

            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="img" Property="Opacity" Value="{StaticResource DisabledOpacity}"/>
            </Trigger>
        </DataTemplate.Triggers>
    </DataTemplate>

    <Style x:Key="OptionSliderThumbStyle" TargetType="ls:LSThumb">
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="SoundID" Value="UI_HUD_Shared_Slider_Thumb"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Grid>
                        <Image x:Name="img" Source="pack://application:,,,/Core;component/Assets/Buttons/btn_hudNav_d.png" Stretch="None"/>
                        <Image x:Name="ico" Source="pack://application:,,,/Core;component/Assets/Options/ico_scrubber_d.png" Stretch="None"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Source" Value="pack://application:,,,/Core;component/Assets/Buttons/btn_hudNav_h.png" TargetName="img"/>
                            <Setter Property="Source" Value="pack://application:,,,/Core;component/Assets/Options/ico_scrubber_h.png" TargetName="ico"/>
                        </Trigger>
                        <Trigger Property="IsDragging" Value="True">
                            <Setter Property="Source" Value="pack://application:,,,/Core;component/Assets/Buttons/btn_hudNav_p.png" TargetName="img"/>
                            <Setter Property="Source" Value="pack://application:,,,/Core;component/Assets/Options/ico_scrubber_h.png" TargetName="ico"/>
                            <Setter Property="Margin" Value="0,2,0,-2" TargetName="ico"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Source" Value="pack://application:,,,/Core;component/Assets/Buttons/btn_hudNav_disabled.png" TargetName="img"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DefaultSliderValueStyle" TargetType="TextBlock">
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="TextAlignment" Value="Left"/>
        <Setter Property="Text" Value="{Binding Value, Converter={StaticResource RoundDownConverter}}"/>
        <Setter Property="Visibility" Value="{Binding ShowValue, Converter={StaticResource BoolToVisibleConverter}}"/>
    </Style>

    <Style x:Key="PointNClickSliderValueStyle" BasedOn="{StaticResource PointNClickSliderValueSharedStyle}" TargetType="TextBlock">
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="TextAlignment" Value="Left"/>
    </Style>

    <Style x:Key="CameraPanningSpeedSliderValueStyle" TargetType="TextBlock">
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="TextAlignment" Value="Left"/>
        <Setter Property="Visibility" Value="Visible"/>
        <!-- Default, 'Normal' case, less than 1, greater than -1 -->
        <Setter Property="Text" Value="{Binding Source='h6311cf93gc2fdg4dbbga861g2c7a44f394d8', Converter={StaticResource TranslatedStringConverter}}"/>

        <Style.Triggers>
            <!-- 'Slow' case: less than -1 -->
            <DataTrigger Binding="{Binding Value, Converter={StaticResource LessThanConverter}, ConverterParameter=-1}" Value="True">
                <Setter Property="Text" Value="{Binding Source='hc71f7bb4g5029g4e4bg9de5g637a4a3e34f7', Converter={StaticResource TranslatedStringConverter}}"/>
            </DataTrigger>
            <!-- 'Fast' case: more than 1 -->
            <DataTrigger Binding="{Binding Value, Converter={StaticResource GreaterThanConverter}, ConverterParameter=1}" Value="True">
                <Setter Property="Text" Value="{Binding Source='h51157197gd1b8g414bga9b8gd205000c0415', Converter={StaticResource TranslatedStringConverter}}"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="FontSizeSliderValueStyle" BasedOn="{StaticResource FontSizeSliderValueSharedStyle}" TargetType="TextBlock">
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="TextAlignment" Value="Left"/>
        <Setter Property="Tag" Value="{Binding Value, Converter={StaticResource RoundConverter}}"/>
    </Style>

    <ControlTemplate x:Key="OptionsSliderTemplate" TargetType="Slider">
        <ControlTemplate.Resources>
            <BitmapImage x:Key="SliderBackground" UriSource="pack://application:,,,/Core;component/Assets/Options/sliderBox_wide_d.png"/>
        </ControlTemplate.Resources>
        <StackPanel Orientation="Horizontal">
            <Grid HorizontalAlignment="Center">
                <ls:LSNineSliceImage x:Name="bg" HorizontalAlignment="Center" VerticalAlignment="Center" ImageSource="{StaticResource SliderBackground}" Width="{Binding Width, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource AddConverter}, ConverterParameter=-100}" Height="80" Slices="106,20"/>

                <ls:LSRepeatButton x:Name="PART_DecreaseRepeatButton" HorizontalAlignment="Left" ContentTemplate="{StaticResource SliderRepeatButtonTemplate}" SoundID="UI_HUD_Shared_Slider_LeftButton" Command="Slider.DecreaseLarge" />
                <ls:LSRepeatButton x:Name="PART_IncreaseRepeatButton" HorizontalAlignment="Right" ContentTemplate="{StaticResource SliderRepeatButtonTemplate}" SoundID="UI_HUD_Shared_Slider_RightButton"  Command="Slider.IncreaseLarge" RenderTransformOrigin="0.5, 0.5">
                    <ls:LSRepeatButton.RenderTransform>
                        <ScaleTransform ScaleX="-1"/>
                    </ls:LSRepeatButton.RenderTransform>
                </ls:LSRepeatButton>

                <Track Name="PART_Track" HorizontalAlignment="Stretch" VerticalAlignment="Center" Margin="56,0" Height="{TemplateBinding Height}">
                    <Track.DecreaseRepeatButton>
                        <ls:LSRepeatButton x:Name="PART_TrackBefore" Style="{StaticResource Style.Slider.RepeatButton}" Command="Slider.DecreaseLarge"/>
                    </Track.DecreaseRepeatButton>
                    <Track.Thumb>
                        <ls:LSThumb x:Name="HighlightTrack" Style="{StaticResource OptionSliderThumbStyle}" />
                    </Track.Thumb>
                    <Track.IncreaseRepeatButton>
                        <ls:LSRepeatButton x:Name="PART_TrackAfter" Style="{StaticResource Style.Slider.RepeatButton}" Command="Slider.IncreaseLarge"/>
                    </Track.IncreaseRepeatButton>
                </Track>
            </Grid>
            <Viewbox Width="100" StretchDirection="DownOnly" HorizontalAlignment="Left" Margin="20,0,-20,0">
                <TextBlock x:Name="PART_ValueLabel" Style="{StaticResource DefaultSliderValueStyle}" Foreground="{StaticResource LS_accent100TxtColor}" FontSize="{StaticResource ScaledMediumFontSize}"/>
            </Viewbox>
        </StackPanel>
        <ControlTemplate.Triggers>
            <DataTrigger Binding="{Binding Enabled}" Value="False">
                <Setter Property="Opacity" Value="{StaticResource DisabledOpacity}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding OptionType}" Value="POINT_AND_CLICK_FREQUENCY">
                <Setter Property="Style" Value="{StaticResource PointNClickSliderValueStyle}" TargetName="PART_ValueLabel"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding OptionType}" Value="DIALOG_FONT_SIZE">
                <Setter Property="Style" Value="{StaticResource FontSizeSliderValueStyle}" TargetName="PART_ValueLabel"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding OptionType}" Value="OVERHEAD_FONT_SIZE">
                <Setter Property="Style" Value="{StaticResource FontSizeSliderValueStyle}" TargetName="PART_ValueLabel"/>
            </DataTrigger>            
            <DataTrigger Binding="{Binding OptionType}" Value="BOOK_FONT_SIZE">
                <Setter Property="Style" Value="{StaticResource FontSizeSliderValueStyle}" TargetName="PART_ValueLabel"/>
                <Setter Property="Text" TargetName="PART_ValueLabel">
                    <Setter.Value>
                        <MultiBinding Converter="{StaticResource ParameterizedTranslatedStringConverter}">
                            <Binding Source="h1169e44bg06ddg4592g8016gfaa0943514c6"/>
                            <Binding Path="Tag" RelativeSource="{RelativeSource Mode=Self}" Converter="{StaticResource AddConverter}" ConverterParameter="{StaticResource IntermediateFontSize}"/>
                        </MultiBinding>
                    </Setter.Value>
                </Setter>
            </DataTrigger>
            <DataTrigger Binding="{Binding OptionType}" Value="PANNING_SPEED">
                <Setter Property="Style" Value="{StaticResource CameraPanningSpeedSliderValueStyle}" TargetName="PART_ValueLabel"/>
            </DataTrigger>
            
            <DataTrigger Binding="{Binding OptionType}" Value="UIScaling">
                <Setter Property="Style" Value="{StaticResource FontSizeSliderValueStyle}" TargetName="PART_ValueLabel"/>
                <Setter Property="Text" Value="{Binding Value, Converter={StaticResource EnumTranslatedStringConverter}, ConverterParameter='h2e12e1f7g7318g4704ga3efgf4121ab0027a'}" TargetName="PART_ValueLabel"/>
            </DataTrigger>

            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="bg" Property="ImageSource" Value="pack://application:,,,/Core;component/Assets/Options/sliderBox_wide_h.png"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="ComboBoxTemplate">
        <ls:LSComboBox x:Name="comboBox" ItemsSource="{Binding ComboItems, Mode=OneWay}" SelectedItem="{Binding SelectedItem}" Foreground="{StaticResource LS_specialTxtColor}" Style="{StaticResource ComboBoxStyle}" Text="{Binding SelectedItem.Name, Mode=OneWay}" IsEnabled="{Binding Enabled, Mode=OneWay}" SoundID="UI_HUD_Options_ComboBox"/>

        <ControlTemplate.Triggers>
            <DataTrigger Binding="{Binding ComboItems.Count, Converter={StaticResource GreaterThanConverter}, ConverterParameter=1}" Value="False">
                <Setter TargetName="comboBox" Property="IsEnabled" Value="False"/>
            </DataTrigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="TickBoxTemplate">
        <ls:LSToggleButton x:Name="TickBoxButton" Style="{StaticResource TickBox}" IsChecked="{Binding Value}" Command="{Binding DataContext.OnChangeToggleButton, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}" IsEnabled="{Binding Enabled}" SoundID="UI_HUD_Options_ToggleButton"/>
    </ControlTemplate>

    <ControlTemplate x:Key="SliderTemplate">
        <Slider x:Name="element" DataContext="{Binding}" Template="{StaticResource OptionsSliderTemplate}" Value="{Binding Value}" Minimum="{Binding Min}" Maximum="{Binding Max}" IsSnapToTickEnabled="True" Width="{Binding Width, RelativeSource={RelativeSource TemplatedParent}, FallbackValue=868}"
                SmallChange="{Binding Max, Converter={StaticResource DivideAndRoundConverter}, ConverterParameter='40'}" LargeChange="{Binding Max, Converter={StaticResource DivideAndRoundConverter}, ConverterParameter='10'}" IsEnabled="{Binding Enabled}" TickFrequency="1" >
            <b:Interaction.Triggers>
                <b:EventTrigger EventName="ValueChanged">
                    <b:InvokeCommandAction Command="{Binding DataContext.ValueChanged, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}" CommandParameter="{Binding .}"/>
                </b:EventTrigger>
            </b:Interaction.Triggers>
        </Slider>
        <ControlTemplate.Triggers>
            <DataTrigger Binding="{Binding OptionType}" Value="PANNING_SPEED">
                <Setter TargetName="element" Property="IsSnapToTickEnabled" Value="False"/>
            </DataTrigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="EditBoxTemplate">
        <ls:LSTextBox Text="{Binding Value}" MaxLength="{Binding MaxLength}" TextImmediateSourceUpdate="True" Foreground="{DynamicResource LS_ActivePanelFontColor}" FontSize="{DynamicResource BigFontSize}" TextAlignment="Center" Width="540"/>
    </ControlTemplate>

    <ControlTemplate x:Key="PasswordBoxTemplate">
        <ls:LSPasswordBox x:Name="PassBox" Text="{Binding Value}" IsEnabled="True" MaxLength="{Binding MaxLength}" Foreground="{DynamicResource LS_ActivePanelFontColor}" FontSize="{DynamicResource BigFontSize}" Width="540"/>
    </ControlTemplate>

    <DataTemplate x:Key="KeybindingTemplate">
        <Grid Margin="0,4,0,4" Width="2000">
            <Grid.Resources>
	            <Style TargetType="{x:Type ls:LSButton}" BasedOn="{StaticResource BaseButtonStyle}">
                    <Setter Property="FontSize" Value="{StaticResource BigFontSize}"/>
		            <Setter Property="Foreground" Value="{DynamicResource LS_accent100TxtColor}"/>
		            <Setter Property="ls:ButtonHelper.ImageSource" Value="pack://application:,,,/Core;component/Assets/Options/inputBox_keybinds_1.png"/>
		            <Style.Triggers>
			            <Trigger Property="IsMouseOver" Value="True">
				            <Setter Property="ls:ButtonHelper.ImageSource" Value="pack://application:,,,/Core;component/Assets/Options/inputBox_keybinds_1_active.png" />
			            </Trigger>
			            <Trigger Property="IsPressed" Value="True">
				            <Setter Property="ls:ButtonHelper.ImageSource" Value="pack://application:,,,/Core;component/Assets/Options/inputBox_keybinds_1_active.png"/>
			            </Trigger>
			            <Trigger Property="IsEnabled" Value="False">
				            <Setter Property="ls:ButtonHelper.ImageSource" Value="pack://application:,,,/Core;component/Assets/Options/inputBox_keybinds_1_active.png" />
			            </Trigger>
		            </Style.Triggers>
	            </Style>
            </Grid.Resources>
            <Image x:Name="highlight" Stretch="None" Source="pack://application:,,,/Core;component/Assets/Options/listItem_bg_fullscreen.png"/>
            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Left" Text="{Binding Text}" FontSize="{DynamicResource DefaultFontSize}" Foreground="{StaticResource LS_accent75TxtColor}" Margin="60,0,0,0"/>
            <ItemsControl VerticalAlignment="Center" HorizontalAlignment="Right" ItemsSource="{Binding Bindings}" Background="Transparent" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Margin="0,0,60,0">

                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel IsItemsHost="True" Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ls:LSButton Content="{Binding BindingString}" Command="{Binding DataContext.RequestKeybindingChange, RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}" CommandParameter="{Binding}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>

            </ItemsControl>

            <b:Interaction.Triggers>
                <b:EventTrigger EventName="MouseEnter">
                    <ls:LSPlaySound Sound="UI_HUD_Options_OptionHover"/>
                </b:EventTrigger>
            </b:Interaction.Triggers>
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="OptionTemplate">
        <DataTemplate.Resources>
            <ImageSource x:Key="ItemBG">pack://application:,,,/Core;component/Assets/Options/li_bg.png</ImageSource>
            <ImageSource x:Key="ItemBGHL">pack://application:,,,/Core;component/Assets/Options/li_bg_active.png</ImageSource>
			<Thickness x:Key="SubOptionMargin">120,0,0,0</Thickness>
        </DataTemplate.Resources>
        <Grid x:Name="Container" Margin="0,4,0,4" MaxWidth="2000">
            
            <ls:AttachedProperties.AutomationId>
                <MultiBinding StringFormat="{}{0}_{1}">
                    <Binding Path="ListItemType"/>
                    <Binding Path="Text"/>
                </MultiBinding>
            </ls:AttachedProperties.AutomationId>
            
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="col0" Width="*"/>
                <ColumnDefinition x:Name="col1" Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Image x:Name="highlight" Grid.Column="0" Grid.ColumnSpan="2" Stretch="None" Source="{StaticResource ItemBG}"/>
            <TextBlock x:Name="label" Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="60,0,0,0" TextWrapping="Wrap" FontWeight="Medium" Text="{Binding Text}" FontSize="{StaticResource ScaledDefaultFontSize}" Foreground="{StaticResource LS_accent75TxtColor}"/>
            <Control x:Name="content" Grid.Column="1" VerticalAlignment="Center"  HorizontalAlignment="Right" Template="{StaticResource ComboBoxTemplate}" Margin="20,0,160,0"/>

            <b:Interaction.Triggers>
                <b:EventTrigger EventName="MouseEnter">
                    <ls:LSPlaySound Sound="UI_HUD_Options_OptionHover"/>
                </b:EventTrigger>
            </b:Interaction.Triggers>
        </Grid>
        <DataTemplate.Triggers>
            <DataTrigger Binding="{Binding ListItemType}" Value="ComboBox">
                <Setter Property="Template" Value="{StaticResource ComboBoxTemplate}" TargetName="content"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding ListItemType}" Value="TickBox">
                <Setter Property="Template" Value="{StaticResource TickBoxTemplate}" TargetName="content"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding ListItemType}" Value="Slider">
                <Setter Property="Template" Value="{StaticResource SliderTemplate}" TargetName="content"/>
                <Setter Property="Margin" Value="20,0,60,0" TargetName="content"/>
            </DataTrigger>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding ListItemType}" Value="EditBox" />
                    <Condition Binding="{Binding HideValue}" Value="False"/>
                </MultiDataTrigger.Conditions>
                <Setter Property="Template" Value="{StaticResource EditBoxTemplate}" TargetName="content"/>
            </MultiDataTrigger>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding ListItemType}" Value="EditBox" />
                    <Condition Binding="{Binding HideValue}" Value="True"/>
                </MultiDataTrigger.Conditions>
                <Setter Property="Template" Value="{StaticResource PasswordBoxTemplate}" TargetName="content"/>
            </MultiDataTrigger>

            <!-- Sub elements list -->
            <DataTrigger Binding="{Binding OptionType}" Value="AUTO_SAVES">
                <Setter Property="Margin" Value="{StaticResource SubOptionMargin}" TargetName="label"/>
            </DataTrigger>

            <DataTrigger Binding="{Binding OptionType}" Value="PANNING_SPEED">
                <Setter Property="Margin" Value="{StaticResource SubOptionMargin}" TargetName="label"/>
            </DataTrigger>

            <DataTrigger Binding="{Binding OptionType}" Value="FRAME_CAP">
                <Setter Property="Margin" Value="{StaticResource SubOptionMargin}" TargetName="label"/>
            </DataTrigger>

            <DataTrigger Binding="{Binding OptionType}" Value="SHARPNESS_VALUE">
                <Setter Property="Margin" Value="{StaticResource SubOptionMargin}" TargetName="label"/>
            </DataTrigger>

            <DataTrigger Binding="{Binding OptionType}" Value="SHOW_SPEAKER">
                <Setter Property="Margin" Value="{StaticResource SubOptionMargin}" TargetName="label"/>
            </DataTrigger>

            <DataTrigger Binding="{Binding OptionType}" Value="TEXT_BACKGROUND_OPACITY">
                <Setter Property="Margin" Value="{StaticResource SubOptionMargin}" TargetName="label"/>
            </DataTrigger>

            <DataTrigger Binding="{Binding OptionType}" Value="GAME_CAMERA_AUTO_ROTATION_SPEED">
                <Setter Property="Margin" Value="{StaticResource SubOptionMargin}" TargetName="label"/>
            </DataTrigger>

            <DataTrigger Binding="{Binding OptionType}" Value="SENSITIVE_CONTENT">
                <Setter Property="Margin" Value="20,0" TargetName="content"/>
            </DataTrigger>

            <!-- UI where the options have less room -->
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ls:UIWidget}}, Path=ContextName}" Value="Lobby">
                <Setter TargetName="Container" Property="Tag" Value="Lobby"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ls:UIWidget}}, Path=ContextName}" Value="LobbyBrowser">
                <Setter TargetName="Container" Property="Tag" Value="Lobby"/>
            </DataTrigger>

            <DataTrigger Binding="{Binding Path=Tag, ElementName=Container}" Value="Lobby">
                <Setter Property="Width" Value="1200" TargetName="Container"/>
                <Setter Property="Margin" TargetName="label" Value="40,0,0,0"/>
                <Setter Property="Margin" TargetName="content" Value="20,0,40,0"/>
            </DataTrigger>

            <DataTrigger Binding="{Binding Enabled}" Value="False">
                <Setter TargetName="label" Property="Foreground" Value="{StaticResource LS_tint20}"/>
                <Setter TargetName="highlight" Property="Opacity" Value="{StaticResource DisabledOpacity}"/>
            </DataTrigger>

            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="highlight" Property="Source" Value="{StaticResource ItemBGHL}"/>
                <Setter TargetName="label" Property="Foreground" Value="{StaticResource LS_tint100}"/>
            </Trigger>

            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding Enabled}" Value="False"/>
                    <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="True"/>
                </MultiDataTrigger.Conditions>
                <Setter TargetName="label" Property="Foreground" Value="{StaticResource LS_tint60}"/>
            </MultiDataTrigger>
        </DataTemplate.Triggers>
    </DataTemplate>

    <!-- Options -->
    <Style x:Key="OptionStyle" TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
        <Setter Property="ContentTemplate" Value="{StaticResource OptionTemplate}" />
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Top"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="IsSelected" Value="False"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding ListItemType}" Value="Keybinding">
                <Setter Property="ContentTemplate" Value="{StaticResource KeybindingTemplate}"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding ListItemType}" Value="Button">
                <Setter Property="ContentTemplate">
                    <Setter.Value>
                        <DataTemplate>
                            <ls:LSButton Margin="0,8,0,8" Style="{StaticResource BrownButtonStyle}" Content="{Binding Text}" Command="{Binding ButtonCommand.ActionCommand}" IsEnabled="{Binding Enabled, Mode=OneWay}"/>
                        </DataTemplate>
                    </Setter.Value>
                </Setter>
            </DataTrigger>
            <DataTrigger Binding="{Binding ListItemType}" Value="String">
                <Setter Property="ContentTemplate">
                    <Setter.Value>
                        <DataTemplate>
                            <DataTemplate.Resources>
                                <ImageSource x:Key="SideDecorations">pack://application:,,,/Core;component/Assets/Options/decors_header.png</ImageSource>
                            </DataTemplate.Resources>

                            <StackPanel Margin="0,40,0,0" Orientation="Horizontal">
                                <Image Source="{StaticResource SideDecorations}" Stretch="None" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding Text}" FontWeight="Bold" Foreground="{StaticResource LS_tint100}" Margin="16,0"/>
                                <Image Source="{StaticResource SideDecorations}" Stretch="None" VerticalAlignment="Center">
                                    <Image.RenderTransform>
                                        <ScaleTransform ScaleX="-1" CenterX="42"/>
                                    </Image.RenderTransform>
                                </Image>
                            </StackPanel>
                        </DataTemplate>
                    </Setter.Value>
                </Setter>
            </DataTrigger>

            <!-- Option is hidden until we focus on task -->
            <DataTrigger Binding="{Binding OptionType}" Value="UIScaling">
                <Setter Property="Visibility" Value="Collapsed"/>
            </DataTrigger>
            
            <!-- Controller only option -->
            <DataTrigger Binding="{Binding OptionType}" Value="HOLD_TO_END_TURN">
                <Setter Property="Visibility" Value="Collapsed"/>
            </DataTrigger>

            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="IsSelected" Value="True"/>
                <Setter Property="BorderBrush" Value="Transparent" />
                <Setter Property="Background" Value="Transparent" />
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="BorderBrush" Value="Transparent" />
                <Setter Property="Background" Value="Transparent" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="PreviewTextStyle" TargetType="TextBlock">
        <Setter Property="TextAlignment" Value="Center" />
        <Setter Property="Foreground" Value="{StaticResource LS_accent75TxtColor}" />
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>

    <Style x:Key="OptionPreviewGridMarginStyle" TargetType="Grid">
        <Setter Property="Margin" Value="120,24,0,0"/>
    </Style>

    <DataTemplate x:Key="NavigationButtonTemplate" DataType="ls:LSButton">
        <Grid VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="27"/>
                <ColumnDefinition Width="58"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="19"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="92"/>
            </Grid.RowDefinitions>
            <Image x:Name="iconEnabled" Grid.Column="1" Source="pack://application:,,,/Core;component/Assets/Shared/ico_inventory_active.png" Stretch="Uniform"/>
            <Image x:Name="iconDisabled" Grid.Column="1" Source="pack://application:,,,/Core;component/Assets/Shared/ico_inventory_active.png" Stretch="Uniform" Visibility="Collapsed"/>
            <TextBlock x:Name="label" Grid.Column="2" Text="{Binding Label ,Converter={StaticResource TranslatedStringConverter}}" MaxWidth="280" FontSize="{StaticResource SmallFontSize}" FontWeight="Bold" Foreground="{StaticResource LS_accent75TxtColor}" HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center" noesis:Text.StrokeThickness="4" noesis:Text.Stroke="{StaticResource LS_tint00}"/>
        </Grid>
        <DataTemplate.Triggers>
            <DataTrigger Binding="{Binding Path=IsEnabled, RelativeSource={RelativeSource Mode=Self}}" Value="False">
                <Setter Property="Foreground" Value="{DynamicResource LS_extraspecialTxtColor}" TargetName="label"/>
                <Setter Property="Visibility" Value="Collapsed" TargetName="iconEnabled"/>
                <Setter Property="Visibility" Value="Visible" TargetName="iconDisabled"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding Path=IsMouseOver, RelativeSource={RelativeSource AncestorType={x:Type ls:LSButton}}}" Value="True">
                <Setter Property="Foreground" Value="{DynamicResource LS_accent100TxtColor}" TargetName="label"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding IconId}" Value="5">
                <Setter Property="Source" Value="pack://application:,,,/Core;component/Assets/Options/ico_video_d.png" TargetName="iconEnabled"/>
                <Setter Property="Source" Value="pack://application:,,,/Core;component/Assets/Options/ico_video_h.png" TargetName="iconDisabled"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding IconId}" Value="6">
                <Setter Property="Source" Value="pack://application:,,,/Core;component/Assets/Options/ico_keybinds_d.png" TargetName="iconEnabled"/>
                <Setter Property="Source" Value="pack://application:,,,/Core;component/Assets/Options/ico_keybinds_h.png" TargetName="iconDisabled"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding IconId}" Value="7">
                <Setter Property="Source" Value="pack://application:,,,/Core;component/Assets/Options/ico_gameplay_d.png" TargetName="iconEnabled"/>
                <Setter Property="Source" Value="pack://application:,,,/Core;component/Assets/Options/ico_gameplay_h.png" TargetName="iconDisabled"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding IconId}" Value="8">
                <Setter Property="Source" Value="pack://application:,,,/Core;component/Assets/Options/ico_controller_d.png" TargetName="iconEnabled"/>
                <Setter Property="Source" Value="pack://application:,,,/Core;component/Assets/Options/ico_controller_h.png" TargetName="iconDisabled"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding IconId}" Value="9">
                <Setter Property="Source" Value="pack://application:,,,/Core;component/Assets/Options/ico_audio_d.png" TargetName="iconEnabled"/>
                <Setter Property="Source" Value="pack://application:,,,/Core;component/Assets/Options/ico_audio_h.png" TargetName="iconDisabled"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding IconId}" Value="10">
                <Setter Property="Source" Value="pack://application:,,,/Core;component/Assets/Options/ico_video_d.png" TargetName="iconEnabled"/>
                <Setter Property="Source" Value="pack://application:,,,/Core;component/Assets/Options/ico_video_h.png" TargetName="iconDisabled"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding IconId}" Value="11">
                <Setter Property="Source" Value="pack://application:,,,/Core;component/Assets/Options/ico_video_d.png" TargetName="iconEnabled"/>
                <Setter Property="Source" Value="pack://application:,,,/Core;component/Assets/Options/ico_video_h.png" TargetName="iconDisabled"/>
            </DataTrigger>
        </DataTemplate.Triggers>
    </DataTemplate>

    <ControlTemplate x:Key="OptionTopButtons">
        <Grid MaxWidth="3840">
            <ls:LSButton x:Name="CloseButtonTop" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="{StaticResource SmallCloseButtonDefaultMargin}" Style="{StaticResource SmallCloseButton}" SoundID="UI_HUD_Options_Close" Command="{Binding CancelCommand}" BoundEvent="ToggleInGameMenu"/>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,44,0,0">
                <StackPanel.Resources>
                    <Style TargetType="ls:LSButton" >
                        <Setter Property="SoundID" Value="UI_HUD_Options_Panel"/>
                        <Setter Property="ContentTemplate" Value="{StaticResource NavigationButtonTemplate}"/>

                        <Setter Property="Command" Value="{Binding CustomEvent}"/>
                        <Setter Property="CommandParameter" Value="{Binding Content.EventString, RelativeSource={RelativeSource Mode=Self}}"/>

                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ls:LSButton">
                                    <Grid>
                                        <Image x:Name="img" Source="pack://application:,,,/Core;component/Assets/Buttons/topNavBtn_inactive_d.png" Stretch="None" VerticalAlignment="Top" Margin="-6,0,-8,0"/>
                                        <ContentPresenter/>
                                    </Grid>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Source" Value="pack://application:,,,/Core;component/Assets/Buttons/topNavBtn_inactive_h.png" TargetName="img"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter Property="Source" Value="pack://application:,,,/Core;component/Assets/Buttons/topNavBtn_inactive_p.png" TargetName="img"/>
                                        </Trigger>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter Property="Source" Value="pack://application:,,,/Core;component/Assets/Buttons/topNavBtn_active_d.png" TargetName="img"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </StackPanel.Resources>

                <ls:LSButton x:Name="gameplayButton" Content="{Binding Source={StaticResource gameplayOptionInfo}}"/>
                <ls:LSButton x:Name="keybindingButton" Content="{Binding Source={StaticResource keybindingOptionInfo}}"/>
                <ls:LSButton x:Name="controllerButton" Content="{Binding Source={StaticResource controllerOptionInfo}}" Visibility="Collapsed"/>
                <ls:LSButton x:Name="videoButton" Content="{Binding Source={StaticResource videoOptionInfo}}"/>
                <ls:LSButton x:Name="audioButton" Content="{Binding Source={StaticResource audioOptionInfo}}"/>
                <ls:LSButton x:Name="interfaceButton" Content="{Binding Source={StaticResource interfaceOptionInfo}}"/>
                <ls:LSButton x:Name="accessibilityButton" Content="{Binding Source={StaticResource accessibilityOptionInfo}}"/>
            </StackPanel>
        </Grid>

        <ControlTemplate.Triggers>
            <DataTrigger Binding="{Binding ActiveOptionsID}" Value="AudioOptions">
                <Setter TargetName="audioButton" Property="IsEnabled" Value="False"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding ActiveOptionsID}" Value="GameOptions">
                <Setter TargetName="gameplayButton" Property="IsEnabled" Value="False"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding ActiveOptionsID}" Value="ControllerOptions">
                <Setter TargetName="controllerButton" Property="IsEnabled" Value="False"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding ActiveOptionsID}" Value="KeybindingOptions">
                <Setter TargetName="keybindingButton" Property="IsEnabled" Value="False"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding ActiveOptionsID}" Value="VideoOptions">
                <Setter TargetName="videoButton" Property="IsEnabled" Value="False"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding ActiveOptionsID}" Value="InterfaceOptions">
                <Setter TargetName="interfaceButton" Property="IsEnabled" Value="False"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding ActiveOptionsID}" Value="AccessibilityOptions">
                <Setter TargetName="accessibilityButton" Property="IsEnabled" Value="False"/>
            </DataTrigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    
    <ControlTemplate x:Key="OptionBottomButtons">
        <ControlTemplate.Resources>
            <BitmapImage x:Key="BottomDecoration" UriSource="pack://application:,,,/Core;component/Assets/DMSettings/footer_centerDecors.png"/>
            <BitmapImage x:Key="SideDecorations" UriSource="pack://application:,,,/Core;component/Assets/DMSettings/footer_sideDecors.png"/>
        </ControlTemplate.Resources>
        <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,40">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Image Source="{StaticResource SideDecorations}" Stretch="None" VerticalAlignment="Center" Margin="-20,0"/>
                <ls:LSButton x:Name="CloseButtonBottom" HorizontalAlignment="Center" Style="{StaticResource BigBrownButtonStyle}" Content="{Binding Source='hfe928c0dgb014g4eb2g8c52g6ed43b026e20', Converter={StaticResource TranslatedStringConverter}}" SoundID="UI_HUD_Options_Close" Command="{Binding CustomEvent}" CommandParameter="CloseWidget">
                    <ls:LSButton.Resources>
                        <ImageBrush x:Key="IconNormal" ImageSource="pack://application:,,,/Core;component/Assets/Buttons/ico_btn_cross_d.png" Stretch="Fill"/>
                        <ImageBrush x:Key="IconPressed" ImageSource="pack://application:,,,/Core;component/Assets/Buttons/ico_btn_cross_d.png" Stretch="Fill"/>
                        <ImageBrush x:Key="IconHighlight" ImageSource="pack://application:,,,/Core;component/Assets/Buttons/ico_btn_cross_h.png" Stretch="Fill"/>
                        <ImageBrush x:Key="IconDisabled" ImageSource="pack://application:,,,/Core;component/Assets/Buttons/ico_btn_cross_d.png" Stretch="Fill"/>
                    </ls:LSButton.Resources>
                </ls:LSButton>
                <ls:LSButton x:Name="ResetButtonBottom" HorizontalAlignment="Center" Style="{StaticResource BigBrownButtonStyle}" Content="{Binding Source='h25d571f7g0b5fg4c8agb2f9g6b3aab5225ce', Converter={StaticResource TranslatedStringConverter}}" SoundID="UI_HUD_Options_Default" Command="{Binding ResetOptionsToDefaultCommand}" >
                    <ls:LSButton.Resources>
                        <ImageBrush x:Key="IconNormal" ImageSource="pack://application:,,,/Core;component/Assets/Buttons/ico_reset_d.png" Stretch="Fill"/>
                        <ImageBrush x:Key="IconPressed" ImageSource="pack://application:,,,/Core;component/Assets/Buttons/ico_reset_d.png" Stretch="Fill"/>
                        <ImageBrush x:Key="IconHighlight" ImageSource="pack://application:,,,/Core;component/Assets/Buttons/ico_reset_d.png" Stretch="Fill"/>
                        <ImageBrush x:Key="IconDisabled" ImageSource="pack://application:,,,/Core;component/Assets/Buttons/ico_reset_d.png" Stretch="Fill"/>
                    </ls:LSButton.Resources>
                </ls:LSButton>
                <Image Source="{StaticResource SideDecorations}" Stretch="None" VerticalAlignment="Center" Margin="-20,0" RenderTransformOrigin="0.5 0.5">
                    <Image.RenderTransform>
                        <ScaleTransform ScaleX="-1"/>
                    </Image.RenderTransform>
                </Image>
            </StackPanel>
            <Image Source="{StaticResource BottomDecoration}" Stretch="None" HorizontalAlignment="Center" Margin="0,-20,0,0"/>
        </StackPanel>
    </ControlTemplate>

    <ControlTemplate x:Key="PreviewOptionsTemplate">
        <ControlTemplate.Resources>
            <BitmapImage x:Key="Divider" UriSource="pack://application:,,,/Core;component/Assets/Lobby/divider_lobby.png"/>
            <BitmapImage x:Key="TopSeparator" UriSource="pack://application:,,,/Core;component/Assets/Shared/dialog_top.png"/>
        </ControlTemplate.Resources>

        <Grid Background="{StaticResource LS_PanelBackground}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <Image Grid.Row="0" Grid.RowSpan="3" Source="{StaticResource OptionsBackground}" Stretch="Fill"/>

            <Image Grid.Row="0" Source="{StaticResource TopSeparator}" Stretch="None" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,-180,0,0"/>
            <Control Grid.Row="0" Template="{StaticResource OptionTopButtons}" HorizontalAlignment="Stretch" VerticalAlignment="Top"/>

            <Viewbox Grid.Row="1"  StretchDirection="DownOnly">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="24,0">

                    <ListBox x:Name="Options" VerticalAlignment="Top" HorizontalAlignment="Center" AlternationCount="{Binding Options.Count}" ItemsSource="{Binding Options}" ItemContainerStyle="{StaticResource OptionStyle}" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <VirtualizingStackPanel Background="Transparent"/>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.Template>
                            <ControlTemplate>
                                <ScrollViewer HorizontalAlignment="Center" Padding="16,0" Height="1700" Width="Auto" Template="{StaticResource ScrollViewerTemplate}" Background="Transparent">
                                    <ScrollViewer.Resources>
                                        <Style TargetType="{x:Type ScrollBar}" BasedOn="{StaticResource BaseScrollBar}"/>
                                        <GridLength x:Key="Top">180</GridLength>
                                        <GridLength x:Key="Bottom">180</GridLength>
                                    </ScrollViewer.Resources>
                                    <ItemsPresenter/>
                                </ScrollViewer>
                            </ControlTemplate>
                        </ListBox.Template>
                    </ListBox>

                    <Image Source="{StaticResource Divider}" Stretch="None" VerticalAlignment="Center"/>

                    <StackPanel x:Name="Preview" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,60,50,0" Width="1350" Orientation="Vertical" DataContext="{Binding ElementName=Options, Path=SelectedItem}">

                        <TextBlock ls:TextBlockFormatter.SourceText="{Binding Text}" HorizontalAlignment="Center" FontSize="{StaticResource ScaledBigFontSize}" Foreground="{StaticResource LS_accent100TxtColor}" TextAlignment="Center"/>

                        <Control HorizontalAlignment="Center" Template="{StaticResource OptionPreview}"/>

                        <TextBlock ls:TextBlockFormatter.SourceText="{Binding Description}" HorizontalAlignment="Center" MaxWidth="1280" Foreground="{StaticResource LS_accent75TxtColor}" TextAlignment="Center" TextWrapping="Wrap" Margin="0,48,0,0"/>

                        <ListBox ItemsSource="{Binding ComboItems}" Visibility="{Binding ShowComboItemsDescriptions, Converter={StaticResource BoolToVisibleConverter}}">
                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Vertical" Width="1350"/>
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>

                            <ListBox.ItemTemplate>
                                <DataTemplate DataType="{x:Type ls:VMEnumData}">
                                    <DataTemplate.Resources>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="TextWrapping" Value="Wrap"/>
                                            <Setter Property="HorizontalAlignment" Value="Left"/>
                                        </Style>

                                        <ImageSource x:Key="SelectedOptionIcon">pack://application:,,,/Core;component/Assets/Shared_c/c_bullet_d.png</ImageSource>
                                    </DataTemplate.Resources>

                                    <StackPanel Orientation="Vertical" Width="1280">
                                        <StackPanel Orientation="Horizontal">
                                            <Image x:Name="SelectedIcon" Source="{StaticResource SelectedOptionIcon}" Stretch="None" Visibility="Collapsed" VerticalAlignment="Top" Margin="-60,-4,0,0"/>
                                            <TextBlock x:Name="Title" ls:TextBlockFormatter.SourceText="{Binding Name}" Foreground="{StaticResource LS_tint100}" FontSize="{StaticResource ScaledIntermediateFontSize}"/>
                                        </StackPanel>

                                        <TextBlock ls:TextBlockFormatter.SourceText="{Binding Description}" Foreground="{StaticResource LS_accent75TxtColor}" FontSize="{StaticResource ScaledDefaultFontSize}" Margin="0,0,0,40"/>
                                    </StackPanel>

                                    <DataTemplate.Triggers>
                                        <DataTrigger Value="True">
                                            <DataTrigger.Binding>
                                                <MultiBinding Converter="{StaticResource EqualConverter}">
                                                    <Binding Path=""/>
                                                    <Binding Path="DataContext.SelectedItem" RelativeSource="{RelativeSource AncestorType={x:Type ListBox}}" />
                                                </MultiBinding>
                                            </DataTrigger.Binding>
                                            <Setter TargetName="SelectedIcon" Property="Visibility" Value="Visible"/>
                                            <Setter TargetName="Title" Property="Foreground" Value="{StaticResource LS_extraspecialTxtColor}"/>
                                        </DataTrigger>
                                    </DataTemplate.Triggers>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>
                </StackPanel>   
            </Viewbox>

            <Control Grid.Row="2" Name="BottomButtons" Template="{StaticResource OptionBottomButtons}"/>
        </Grid>

        <ControlTemplate.Triggers>
            <DataTrigger Binding="{Binding SelectedItem.ListItemType, ElementName=Options}" Value="None">
                <Setter TargetName="Preview" Property="Visibility" Value="Hidden"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding SelectedItem.ListItemType, ElementName=Options}" Value="String">
                <Setter TargetName="Preview" Property="Visibility" Value="Hidden"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding SelectedIndex, ElementName=Options}" Value="-1">
                <Setter TargetName="Preview" Property="Visibility" Value="Hidden"/>
            </DataTrigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="KeybindingOptionsTemplate">
        <Grid>
            <Control Template="{StaticResource PreviewOptionsTemplate}"/>
            <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Width="3840" Height="2160" Visibility="{Binding InputBindingVisible, Converter={StaticResource BoolToVisibleConverter}}" Background="{StaticResource LS_PanelBackground}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Vertical">
                    <TextBlock Text="{Binding ActiveBindingKey}" TextAlignment="Center" Width="Auto" FontSize="{DynamicResource HugeFontSize}" Foreground="{StaticResource LS_tint100}" Background="Transparent" FontWeight="Bold" Margin="0,20,0,20"/>
                    <TextBlock Text="{Binding ActiveBindingName}" TextAlignment="Center" Width="Auto" FontSize="{DynamicResource HugeFontSize}" Foreground="{StaticResource LS_tint100}" Background="Transparent" FontWeight="Bold" Margin="0,20,0,20"/>
                    <StackPanel Orientation="Horizontal" Margin="10,0,10,0">
                        <ls:LSButton x:Name="KeybindClearBtn" Style="{StaticResource BrownButtonStyle}" Content="{Binding Source='hd58c82e5ga514g4c6ag9f1eg29134bf7e78b', Converter={StaticResource TranslatedStringConverter}}" Command="{Binding ClearSingleBinding}" OverwriteClickSound="UI_Shared_Apply"/>
                        <ls:LSButton x:Name="KeybindCancelBtn" Style="{StaticResource BrownButtonStyle}" Content="{Binding Source='h675a55aegd07cg4ab0g862dg0c564197d478', Converter={StaticResource TranslatedStringConverter}}" Command="{Binding CancelSingleBinding}" OverwriteClickSound="UI_Shared_Closed_Click"/>
                        <ls:LSButton x:Name="KeybindApplyBtn" Style="{DynamicResource BigBlueButtonStyle}" Content="{Binding Source='hd9ef0e80g254cg4b35gb1e7gadfd05fe9f25', Converter={StaticResource TranslatedStringConverter}}" Command="{Binding ApplySingleBinding}" OverwriteClickSound="UI_Shared_Apply"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Grid>
    </ControlTemplate>

</ResourceDictionary>