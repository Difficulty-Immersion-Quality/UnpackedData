<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                    xmlns:b="http://schemas.microsoft.com/xaml/behaviors"

                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:System="clr-namespace:System;assembly=mscorlib"
                    xmlns:ls="clr-namespace:ls;assembly=Code"
                    xmlns:noesis="clr-namespace:NoesisGUIExtensions;assembly=Noesis.GUI.Extensions"
                    xmlns:diag="clr-namespace:System.Diagnostics;assembly=WindowsBase"
                    mc:Ignorable="d">

    <!-- Constants -->
    <System:Double x:Key="TextOutlineThickness">4</System:Double>


    
    <Storyboard x:Key="DamageAnimationCommon">
        <!-- Animate in -->
        <DoubleAnimation From="0.1" To="1.0" Duration="0:0:0.1" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseOut"/>
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
        <DoubleAnimation From="0.1" To="1.0" Duration="0:0:0.1" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleY)">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseOut"/>
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>

        <!-- Fade out -->
        <DoubleAnimation To="0" BeginTime="0:0:1.8" Duration="0:0:0.2" Storyboard.TargetProperty="(TextBlock.Foreground).(Brush.Opacity)">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseIn"/>
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
    </Storyboard>

    <Storyboard x:Key="DamageAnimationX0">
        <!-- no sideways movement -->
    </Storyboard>
    <Storyboard x:Key="DamageAnimationX1">
        <!-- sideways movement -->
        <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[3].(TranslateTransform.X)" Duration="0:0:2">
            <EasingDoubleKeyFrame Value="-50" KeyTime="0:0:2">
                <EasingDoubleKeyFrame.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </EasingDoubleKeyFrame.EasingFunction>
            </EasingDoubleKeyFrame>
        </DoubleAnimationUsingKeyFrames>
    </Storyboard>
    <Storyboard x:Key="DamageAnimationX2">
        <!-- sideways movement -->
        <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[3].(TranslateTransform.X)" Duration="0:0:2">
            <EasingDoubleKeyFrame Value="50" KeyTime="0:0:2">
                <EasingDoubleKeyFrame.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </EasingDoubleKeyFrame.EasingFunction>
            </EasingDoubleKeyFrame>
        </DoubleAnimationUsingKeyFrames>
    </Storyboard>
    <Storyboard x:Key="DamageAnimationX3">
        <!-- sideways movement -->
        <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[3].(TranslateTransform.X)" Duration="0:0:2">
            <EasingDoubleKeyFrame Value="-35" KeyTime="0:0:2">
                <EasingDoubleKeyFrame.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </EasingDoubleKeyFrame.EasingFunction>
            </EasingDoubleKeyFrame>
        </DoubleAnimationUsingKeyFrames>
    </Storyboard>
    <Storyboard x:Key="DamageAnimationX4">
        <!-- sideways movement -->
        <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[3].(TranslateTransform.X)" Duration="0:0:2">
            <EasingDoubleKeyFrame Value="35" KeyTime="0:0:2">
                <EasingDoubleKeyFrame.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </EasingDoubleKeyFrame.EasingFunction>
            </EasingDoubleKeyFrame>
        </DoubleAnimationUsingKeyFrames>
    </Storyboard>

    <Storyboard x:Key="DamageAnimationY0">
        <!-- Animate up -->
        <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[3].(TranslateTransform.Y)" Duration="0:0:2">
            <EasingDoubleKeyFrame Value="-100.0" KeyTime="0:0:2">
                <EasingDoubleKeyFrame.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </EasingDoubleKeyFrame.EasingFunction>
            </EasingDoubleKeyFrame>
        </DoubleAnimationUsingKeyFrames>
    </Storyboard>
    <Storyboard x:Key="DamageAnimationY1">
        <!-- Animate up -->
        <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[3].(TranslateTransform.Y)" Duration="0:0:2">
            <EasingDoubleKeyFrame Value="-50.0" KeyTime="0:0:2">
                <EasingDoubleKeyFrame.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </EasingDoubleKeyFrame.EasingFunction>
            </EasingDoubleKeyFrame>
        </DoubleAnimationUsingKeyFrames>
    </Storyboard>

    <System:Double x:Key="OverheadOffset">1.0</System:Double>
    <System:Double x:Key="MidpointOffset">0.5</System:Double>

    <Style x:Key="OverheadPosition" TargetType="ContentPresenter">
        <Setter Property="Canvas.Left">
            <Setter.Value>
                <MultiBinding Converter="{StaticResource LinearInterpolationConverter}">
                    <Binding Path="BaseScreenPosition.X"/>
                    <Binding Path="TopScreenPosition.X"/>
                    <Binding Source="{StaticResource OverheadOffset}"/>
                </MultiBinding>
            </Setter.Value>
        </Setter>
        <Setter Property="Canvas.Top">
            <Setter.Value>
                <MultiBinding Converter="{StaticResource LinearInterpolationConverter}">
                    <Binding Path="BaseScreenPosition.Y"/>
                    <Binding Path="TopScreenPosition.Y"/>
                    <Binding Source="{StaticResource OverheadOffset}"/>
                </MultiBinding>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="MiddlePosition" TargetType="ContentPresenter">
        <Setter Property="Canvas.Left">
            <Setter.Value>
                <MultiBinding Converter="{StaticResource LinearInterpolationConverter}">
                    <Binding Path="BaseScreenPosition.X"/>
                    <Binding Path="TopScreenPosition.X"/>
                    <Binding Source="{StaticResource MidpointOffset}"/>
                </MultiBinding>
            </Setter.Value>
        </Setter>
        <Setter Property="Canvas.Top">
            <Setter.Value>
                <MultiBinding Converter="{StaticResource LinearInterpolationConverter}">
                    <Binding Path="BaseScreenPosition.Y"/>
                    <Binding Path="TopScreenPosition.Y"/>
                    <Binding Source="{StaticResource MidpointOffset}"/>
                </MultiBinding>
            </Setter.Value>
        </Setter>
    </Style>

    <ControlTemplate x:Key="WorldTextDisplay">
        <ControlTemplate.Resources>
            <!-- default to starting at overhead position -->
            <Style x:Key="WorldTextPositioning" BasedOn="{StaticResource OverheadPosition}"/>
        </ControlTemplate.Resources>

        <ItemsControl ItemsSource="{Binding CurrentPlayer.WorldText}" ItemContainerStyle="{DynamicResource WorldTextPositioning}" IsHitTestVisible="False">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock ls:TextBlockFormatter.SourceText="{Binding Text}" HorizontalAlignment="Center" VerticalAlignment="Bottom" RenderTransformOrigin="0.5,0.5"
                                   TextWrapping="Wrap" TextAlignment="Center" Foreground="{DynamicResource LS_PrimaryColor}" 
                                   noesis:Text.StrokeThickness="{StaticResource TextOutlineThickness}" >
                        <noesis:Text.Stroke>
                            <SolidColorBrush Color="{DynamicResource TextOutlineColor}" Opacity="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=TextBlock}, Path=Foreground.Opacity}"/>
                        </noesis:Text.Stroke>
                        <TextBlock.RenderTransform>
                            <!-- for animating position -->
                            <TransformGroup>
                                <ScaleTransform/>
                                <SkewTransform/>
                                <RotateTransform/>
                                <TranslateTransform X="0" Y="0"/>
                            </TransformGroup>
                        </TextBlock.RenderTransform>

                        <TextBlock.Effect>
                            <DropShadowEffect Direction="270" Color="Black" ShadowDepth="3.0" BlurRadius="0.0"/>
                        </TextBlock.Effect>

                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontSize" Value="{StaticResource TitleFontSize}"  />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding AnimationType}" Value="Anim0">
                                        <DataTrigger.EnterActions>
                                            <BeginStoryboard Storyboard="{StaticResource DamageAnimationCommon}"/>
                                            <BeginStoryboard Storyboard="{StaticResource DamageAnimationX0}"/>
                                            <BeginStoryboard Storyboard="{StaticResource DamageAnimationY0}"/>
                                        </DataTrigger.EnterActions>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding AnimationType}" Value="Anim1">
                                        <DataTrigger.EnterActions>
                                            <BeginStoryboard Storyboard="{StaticResource DamageAnimationCommon}"/>
                                            <BeginStoryboard Storyboard="{StaticResource DamageAnimationX1}"/>
                                            <BeginStoryboard Storyboard="{StaticResource DamageAnimationY0}"/>
                                        </DataTrigger.EnterActions>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding AnimationType}" Value="Anim2">
                                        <DataTrigger.EnterActions>
                                            <BeginStoryboard Storyboard="{StaticResource DamageAnimationCommon}"/>
                                            <BeginStoryboard Storyboard="{StaticResource DamageAnimationX2}"/>
                                            <BeginStoryboard Storyboard="{StaticResource DamageAnimationY0}"/>
                                        </DataTrigger.EnterActions>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding AnimationType}" Value="Anim3">
                                        <DataTrigger.EnterActions>
                                            <BeginStoryboard Storyboard="{StaticResource DamageAnimationCommon}"/>
                                            <BeginStoryboard Storyboard="{StaticResource DamageAnimationX3}"/>
                                            <BeginStoryboard Storyboard="{StaticResource DamageAnimationY1}"/>
                                        </DataTrigger.EnterActions>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding AnimationType}" Value="Anim4">
                                        <DataTrigger.EnterActions>
                                            <BeginStoryboard Storyboard="{StaticResource DamageAnimationCommon}"/>
                                            <BeginStoryboard Storyboard="{StaticResource DamageAnimationX4}"/>
                                            <BeginStoryboard Storyboard="{StaticResource DamageAnimationY1}"/>
                                        </DataTrigger.EnterActions>
                                    </DataTrigger>

                                    <DataTrigger Binding="{Binding DataContext.CurrentPlayer.UIData.ShowHud,RelativeSource={RelativeSource AncestorType={x:Type ls:UIWidget}}}" Value="False">
                                        <Setter Property="FontSize" Value="{StaticResource HumongousFontSize}"  />
                                    </DataTrigger>

                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>

                    </TextBlock>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        
    </ControlTemplate>

</ResourceDictionary>