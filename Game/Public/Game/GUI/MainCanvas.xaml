<ls:UICanvas xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:ls="clr-namespace:ls;assembly=Code"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
    x:Name="CanvasRoot">
    <ls:UICanvas.Resources>
        <ResourceDictionary Source="WindowManager.xaml"/>
      </ls:UICanvas.Resources>
    <Viewbox x:Name="ViewboxRoot">
        <Grid x:Name="ContentRoot" Width="3840" Height="2160" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="LeftPlayer" Width="*"/>
                <ColumnDefinition x:Name="RightPlayer" Width="*"/>
            </Grid.ColumnDefinitions>
        </Grid>
    </Viewbox>
</ls:UICanvas>