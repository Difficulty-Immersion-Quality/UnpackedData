<ls:UIWidget x:Name="ScreenFade"
             ls:UIWidget.ContextName="ScreenFade"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:ls="clr-namespace:ls;assembly=Code"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:noesis="clr-namespace:NoesisGUIExtensions;assembly=Noesis.GUI.Extensions"
             xmlns:System="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance {x:Type ls:DCScreenFade}, IsDesignTimeCreatable=True}"
             d:DesignHeight="2160" d:DesignWidth="3840">
    <ls:UIWidget.Template>
        <ControlTemplate TargetType="ls:UIWidget">
            <ControlTemplate.Resources>
                <Storyboard x:Key="HideSpinnerAtTheStart">
                    <ObjectAnimationUsingKeyFrames FillBehavior="Stop" Duration="0:0:2" Storyboard.TargetProperty="Visibility" Storyboard.TargetName="Spinner">
                        <DiscreteObjectKeyFrame KeyTime="0:0:0" Value="{x:Static Visibility.Collapsed}"/>
                    </ObjectAnimationUsingKeyFrames>
                </Storyboard>
            </ControlTemplate.Resources>
            <Grid x:Name="Root" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Visibility="{Binding ScreenFadeVisibility}">
                <Border x:Name="Bg" Background="{StaticResource ScreenFade.Default}" Opacity="{Binding ScreenFadeOpacity}"/>
                <Control x:Name="Spinner" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,140,100" Template="{StaticResource BusyElement}" Visibility="{Binding ScreenFadeHasSpin, Converter={StaticResource BoolToVisibleConverter}}"/>
                <ls:LSInputBinding AnyClick="True" AnyKey="True" EatInput="True" IsEnabled="{Binding BlockAllInput, FallbackValue=False}"/>
            </Grid>
            <ControlTemplate.Triggers>
                <Trigger SourceName="Root" Property="Visibility" Value="Visible">
                    <Trigger.EnterActions>
                        <BeginStoryboard x:Name="BeginHideSpinnerAtTheStart" Storyboard="{StaticResource HideSpinnerAtTheStart}"/>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <StopStoryboard BeginStoryboardName="BeginHideSpinnerAtTheStart"/>
                    </Trigger.ExitActions>
                </Trigger>
                <DataTrigger Binding="{Binding Path=ScreenFadeStyle}" Value="Bright">
                    <Setter TargetName="Bg" Property="Background" Value="{StaticResource ScreenFade.Bright}"/>
                </DataTrigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
    </ls:UIWidget.Template>
</ls:UIWidget>
