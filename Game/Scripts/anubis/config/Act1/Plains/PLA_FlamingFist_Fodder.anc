game.configs.PLA_FlamingFist_Fodder = Config{
    params={
        loopingAnimation = {
            type = EParamType.String,
            help = [[The animation to loop until the state is interrupted.]],
            required = true,
        },
    },
    root=StateRef{game.roots.PLA_GithChokepoint_FlamingFist,
        idle = StateRef{game.states.GLO_LoopAnimation,
            loopingAnimation = params.loopingAnimation,
        },
    }
}
