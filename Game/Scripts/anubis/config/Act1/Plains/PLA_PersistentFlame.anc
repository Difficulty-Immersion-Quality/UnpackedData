game.configs.PLA_PersistentFlame = Config{
    root=StateRef{game.roots.DefaultItem,
        combat = StateRef{game.states.DefaultItemCombat},
        idle = StateRef{game.states.PLA_PersistentFlame,
            expandFire = true,
        },
    }
}
