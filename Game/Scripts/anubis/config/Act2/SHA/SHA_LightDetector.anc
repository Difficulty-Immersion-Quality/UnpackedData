game.configs.SHA_LightDetector = Config{
    params={
    },
    root=StateRef{game.roots.DefaultItem,
        combat = StateRef{game.states.SHA_LightDetector,
            activeCheck = ExpressionRef{game.expressions.GLO_DetectDarkness},
        },
        idle = StateRef{game.states.SHA_LightDetector,
            activeCheck = ExpressionRef{game.expressions.GLO_DetectDarkness},
        },
    }
}
