game.configs.SHA_OrthonCompanion = Config{
    params={
        LairPosTrigger = {
            type = EParamType.String,
            help = [[]],
            required = true,
        },
    },
    root=StateRef{game.roots.DefaultCharacter,
        genericBehaviours = StateRef{game.states.CrimesHumanoid},
        idle = StateRef{game.states.SHA_OrthonCompanion,
            LairPos = params.LairPosTrigger,
        },
    }
}
