game.configs.SCL_CursedCharacter = Config{
    root=StateRef{game.roots.DefaultCharacter,
        genericBehaviours = StateRef{game.states.CrimesHumanoid},
        idle = StateRef{game.states.SCL_CursedCharacter},
        partyMember = StateRef{game.states.SCL_CursedCharacter},
    }
}
