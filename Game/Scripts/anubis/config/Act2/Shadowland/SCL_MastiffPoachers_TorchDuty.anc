game.configs.SCL_MastiffPoachers_TorchDuty = Config{
    params={
    },
    root=StateRef{game.roots.DefaultCharacter,
        genericBehaviours = StateRef{game.states.CrimesHumanoid},
        idle = StateRef{game.states.SCL_MastiffPoachers_TorchDuty},
    }
}
