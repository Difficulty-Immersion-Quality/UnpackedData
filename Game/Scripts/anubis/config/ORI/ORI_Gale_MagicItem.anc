game.configs.ORI_Gale_MagicItem = Config{
    params={
        SpotDistance = {
            type = EParamType.Int,
            help = [[]],
            required = false,
            default = 10
        },
    },
    root=StateRef{game.roots.DefaultItem,
        idle = StateRef{game.states.ORI_Gale_MagicItem,
            maxDistance = params.SpotDistance,
        },
    }
}
