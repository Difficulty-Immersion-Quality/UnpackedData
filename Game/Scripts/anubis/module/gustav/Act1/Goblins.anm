local m = AnubisModule()

m.action.RepeatAnimationRandomTimes = function(animation, maxTimes, sleepBetween)
    for i = 1, math.random(1, maxTimes) do
        PlayAnimation(animation)
        if sleepBetween > 0.0 then
            Sleep(sleepBetween)
        end
    end
end

return m