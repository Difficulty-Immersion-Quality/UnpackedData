<ls:UIWidget x:Name="ModBrowserHome"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ls="clr-namespace:ls;assembly=Code"
             xmlns:System="clr-namespace:System;assembly=mscorlib"
             xmlns:noesis="clr-namespace:NoesisGUIExtensions;assembly=Noesis.GUI.Extensions"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             ls:UIWidget.ContextName="ModBrowserHome"
             Template="{StaticResource ModBrowserBackgroundTemplate}"
             d:DataContext="{d:DesignInstance Type=ls:DCModBrowser, IsDesignTimeCreatable=True}"
             d:DesignWidth="3840" d:DesignHeight="2160">
    <ls:UIWidget.ContentTemplate>
        <DataTemplate>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <b:Interaction.Triggers>
                    <b:EventTrigger EventName="Loaded">
                        <b:Interaction.Behaviors>
                            <b:ConditionBehavior>
                                <b:ConditionalExpression>
									<b:ComparisonCondition LeftOperand="{Binding ShowModManagerSplash}" Operator="Equal" RightOperand="True"/>
                                </b:ConditionalExpression>
                            </b:ConditionBehavior>
                        </b:Interaction.Behaviors>

                        <b:InvokeCommandAction Command="{Binding CustomEvent}" CommandParameter="ShowSplashScreen"/>
                        <b:ChangePropertyAction TargetObject="{Binding DataContext, ElementName=ModBrowserHome}" PropertyName="IsShowingSplash" Value="True"/>
                    </b:EventTrigger>
                </b:Interaction.Triggers>

                <TextBlock x:Name="ConnectionWarning" Grid.Row="1" Text="{Binding Source='h41be27a3gda6cg4992gb00eg5317b117fac7', Converter={StaticResource TranslatedStringConverter}}"
                           HorizontalAlignment="Center" Margin="0,600" FontSize="{StaticResource LargeFontSize}" FontWeight="DemiBold"/>
            </Grid>
        </DataTemplate>
    </ls:UIWidget.ContentTemplate>
</ls:UIWidget>