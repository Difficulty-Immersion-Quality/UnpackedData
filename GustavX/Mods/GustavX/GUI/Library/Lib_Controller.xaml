<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:ls="clr-namespace:ls;assembly=Code"
                    xmlns:System="clr-namespace:System;assembly=mscorlib"
                    xmlns:noesis="clr-namespace:NoesisGUIExtensions;assembly=Noesis.GUI.Extensions"
                    xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                    mc:Ignorable="d">

    <Style x:Key="ActionResourcesTemplateSelector" TargetType="ls:LSActionPointResources" BasedOn="{StaticResource ActionResourcesTemplateSelector}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding TypeId}" Value="CosmicOmen">
                <Setter Property="ActionPointTemplate" Value="{StaticResource ActionResources.ActionGroup.SpellSlot}"/>
                <Setter Property="MaxGroupActionPoints" Value="1"/>
                <Setter Property="ActionPointSize" Value="{DynamicResource ActionResources.ActionPointSize}" />
            </DataTrigger>

            <DataTrigger Binding="{Binding TypeId}" Value="StarMapPoint">
                <Setter Property="ActionPointTemplate" Value="{StaticResource ActionResources.ActionGroup.SpellSlot}"/>
                <Setter Property="MaxGroupActionPoints" Value="1"/>
                <Setter Property="ActionPointSize" Value="{DynamicResource ActionResources.ActionPointSize}" />
            </DataTrigger>
            
            <DataTrigger Binding="{Binding TypeId}" Value="Bladesong">
                <Setter Property="ActionPointTemplate" Value="{StaticResource ActionResources.ActionGroup.ArcaneRecoveryActionGroup}"/>
                <Setter Property="MaxGroupActionPoints" Value="1"/>
                <Setter Property="ActionPointSize" Value="{DynamicResource ActionResources.ActionPointSize}" />
            </DataTrigger>

            <DataTrigger Binding="{Binding TypeId}" Value="WrithingTidePoint">
                <Setter Property="ActionPointTemplate" Value="{StaticResource ActionResources.ActionGroup.WildShapeActionGroup}"/>
                <Setter Property="MaxGroupActionPoints" Value="1"/>
                <Setter Property="ActionPointSize" Value="{DynamicResource ActionResources.ActionPointSize}" />
            </DataTrigger>
            
        </Style.Triggers>
    </Style>

</ResourceDictionary>