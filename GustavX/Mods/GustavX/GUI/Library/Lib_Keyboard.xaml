<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:ls="clr-namespace:ls;assembly=Code"
                    xmlns:System="clr-namespace:System;assembly=mscorlib"
                    xmlns:noesis="clr-namespace:NoesisGUIExtensions;assembly=Noesis.GUI.Extensions"
                    xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                    mc:Ignorable="d">

    <ControlTemplate x:Key="ActionResources.ActionGroup.CosmicOmenGroup" TargetType="ls:LSActionPoint">
        <ContentPresenter ContentTemplate="{StaticResource ActionResources.ActionGroup.ActionPoint}"/>
    </ControlTemplate>

    <ControlTemplate x:Key="ActionResources.ActionGroup.StarMapGroup" TargetType="ls:LSActionPoint">
        <ContentPresenter ContentTemplate="{StaticResource ActionResources.ActionGroup.ActionPoint}"/>
    </ControlTemplate>

    <ControlTemplate x:Key="ActionResources.ActionGroup.BladeSongGroup" TargetType="ls:LSActionPoint">
        <ContentPresenter ContentTemplate="{StaticResource ActionResources.ActionGroup.ActionPoint}"/>
    </ControlTemplate>

    <ControlTemplate x:Key="ActionResources.ActionGroup.WrithingCurrent" TargetType="ls:LSActionPoint">
        <ContentPresenter ContentTemplate="{StaticResource ActionResources.ActionGroup.ActionPoint}"/>
    </ControlTemplate>

    <Style x:Key="ActionResourcesTemplateSelector" TargetType="ls:LSActionPointResources" BasedOn="{StaticResource ActionResourcesTemplateSelector}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding TypeId}" Value="CosmicOmen">
                <Setter Property="ActionPointTemplate" Value="{DynamicResource ActionResources.ActionGroup.CosmicOmenGroup}"/>
                <Setter Property="MaxGroupActionPoints" Value="1"/>
                <Setter Property="ActionPointSize" Value="{DynamicResource ActionResources.ActionPointSize}" />
                <Setter Property="Margin" Value="0,-20,0,0"/>
            </DataTrigger>

            <DataTrigger Binding="{Binding TypeId}" Value="StarMapPoint">
                <Setter Property="ActionPointTemplate" Value="{DynamicResource ActionResources.ActionGroup.StarMapGroup}"/>
                <Setter Property="MaxGroupActionPoints" Value="1"/>
                <Setter Property="ActionPointSize" Value="{DynamicResource ActionResources.ActionPointSize}" />
                <Setter Property="Margin" Value="0,-18,0,0"/>
            </DataTrigger>
            
            <DataTrigger Binding="{Binding TypeId}" Value="Bladesong">
                <Setter Property="ActionPointTemplate" Value="{DynamicResource ActionResources.ActionGroup.BladeSongGroup}"/>
                <Setter Property="MaxGroupActionPoints" Value="1"/>
                <Setter Property="ActionPointSize" Value="{DynamicResource ActionResources.ActionPointSize}" />
            </DataTrigger>

            <DataTrigger Binding="{Binding TypeId}" Value="WrithingTidePoint">
                <Setter Property="ActionPointTemplate" Value="{DynamicResource ActionResources.ActionGroup.WrithingCurrent}"/>
                <Setter Property="MaxGroupActionPoints" Value="1"/>
                <Setter Property="ActionPointSize" Value="{DynamicResource ActionResources.ActionPointSize}" />
                <Setter Property="Margin" Value="0,-10,0,0"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>