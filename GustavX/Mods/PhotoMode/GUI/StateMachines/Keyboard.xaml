<ls:StateMachine xmlns:ls="clr-namespace:ls;assembly=Code"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
	<ls:StateMachine.States>

        <ls:State Name = "PlayerUIsHolder" ModType="Extend">
			<ls:State.Events>
				<ls:StateEvent Name="OpenPhotoMode">
					<ls:StateEvent.Actions>
                        <ls:PushState Name="PhotoMode"/>
					</ls:StateEvent.Actions>
				</ls:StateEvent>
			</ls:State.Events>
		</ls:State>

        <ls:State Name = "PhotoMode" Layout = "Player" Owner = "Player" DisableStatesBelow = "True" HideStatesBelow = "True" IsModal="True" IsModdable="False">
            <ls:State.Widgets>
                <ls:StateWidget Filename="PhotoMode.xaml" Layer="Panels"/>
            </ls:State.Widgets>

            <ls:State.Events>
                <ls:StateEvent Name = "OpenPhotoMode"/>
                <ls:StateEvent Name = "ClosePhotoMode">
                    <ls:StateEvent.Actions>
                        <ls:RemoveState/>
                    </ls:StateEvent.Actions>
                </ls:StateEvent>
                <ls:StateEvent Name = "IE.ToggleInGameMenu">
                    <ls:StateEvent.Actions>
                        <ls:PushState Name="Paused"/>
                    </ls:StateEvent.Actions>
                </ls:StateEvent>
            </ls:State.Events>
        </ls:State>

        <ls:State Name = "JournalQuests" ModType="Extend">
            <ls:State.Events>
                <ls:StateEvent Name = "IE.UITogglePhotoMode">
                </ls:StateEvent>
            </ls:State.Events>
        </ls:State>

        <ls:State Name = "JournalInspiration" ModType="Extend">
            <ls:State.Events>
                <ls:StateEvent Name = "IE.UITogglePhotoMode">
                </ls:StateEvent>
            </ls:State.Events>
        </ls:State>

        <ls:State Name = "JournalMap" ModType="Extend">
            <ls:State.Events>
                <ls:StateEvent Name = "IE.UITogglePhotoMode">
                </ls:StateEvent>
            </ls:State.Events>
        </ls:State>

        <ls:State Name = "JournalDialogues" ModType="Extend">
            <ls:State.Events>
                <ls:StateEvent Name = "IE.UITogglePhotoMode">
                </ls:StateEvent>
            </ls:State.Events>
        </ls:State>

        <ls:State Name = "JournalTutorial" ModType="Extend">
            <ls:State.Events>
                <ls:StateEvent Name = "IE.UITogglePhotoMode">
                </ls:StateEvent>
            </ls:State.Events>
        </ls:State>
        
    </ls:StateMachine.States>
</ls:StateMachine>