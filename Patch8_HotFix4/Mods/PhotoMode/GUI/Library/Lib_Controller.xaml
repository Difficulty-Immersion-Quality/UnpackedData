<ResourceDictionary
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:ls="clr-namespace:ls;assembly=Code"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/PhotoMode;component/Library/Lib_Shared.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <Thickness x:Key="PhotoListPadding">0,0,0,12</Thickness>

    <Style x:Key="TitleLabelStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource LS_accent100TxtColor}"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="FontSize" Value="{StaticResource ScaledLargeFontSize}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Margin" Value="0,32,0,38"/>
    </Style>

    <Style x:Key="TitleTextOptionStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource LS_accent100TxtColor}"/>
        <Setter Property="FontSize" Value="{StaticResource ScaledIntermediateFontSize}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Margin" Value="0,10,0,0"/>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="True">
                <Setter Property="Foreground" Value="{StaticResource LS_tint100}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="LabelTextOptionStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource LS_accent75TxtColor}"/>
        <Setter Property="FontSize" Value="{StaticResource ScaledMediumFontSize}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Margin" Value="0,0,0,10"/>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="True">
                <Setter Property="Foreground" Value="{StaticResource LS_tint100}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="PlayIconStyle" TargetType="Path">
        <Setter Property="Data" Value="{StaticResource PlayIconGeometry}"/>
        <Setter Property="Height" Value="60"/>
        <Setter Property="Width" Value="60"/>
        <Setter Property="StrokeThickness" Value="0"/>
        <Setter Property="Fill" Value="{StaticResource LS_tint100}"/>
        <Setter Property="Stretch" Value="None"/>
    </Style>

    <Style x:Key="PauseIconStyle" TargetType="Path" BasedOn="{StaticResource PlayIconStyle}">
        <Setter Property="Data" Value="{StaticResource PauseIconGeometry}"/>
    </Style>

    <Storyboard x:Key="selectorStoryboard" RepeatBehavior="Forever" AutoReverse="True">
        <DoubleAnimation Storyboard.TargetName="img1" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:2"/>
        <DoubleAnimation Storyboard.TargetName="img2" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:2"/>
        <DoubleAnimation Storyboard.TargetName="img3" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:2"/>
        <DoubleAnimation Storyboard.TargetName="img4" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:2"/>
    </Storyboard>

    <ControlTemplate x:Key="FocusedListItemIndicatorTemplate">
        <Grid>
            <Grid.Resources>
                <Thickness x:Key="listItemSlices">60,60,60,60</Thickness>
            </Grid.Resources>

            <ls:LSNineSliceImage x:Name="img1" ImageSource="{StaticResource ListItemSelected1}" Slices="{StaticResource listItemSlices}" Width="{TemplateBinding Width}" Height="{TemplateBinding Height}" Opacity="1"/>
            <ls:LSNineSliceImage x:Name="img2" ImageSource="{StaticResource ListItemSelected1}" Slices="{StaticResource listItemSlices}" Width="{TemplateBinding Width}" Height="{TemplateBinding Height}" Opacity="1"/>
            <ls:LSNineSliceImage x:Name="img3" ImageSource="{StaticResource ListItemSelected2}" Slices="{StaticResource listItemSlices}" Width="{TemplateBinding Width}" Height="{TemplateBinding Height}" Opacity="0"/>
            <ls:LSNineSliceImage x:Name="img4" ImageSource="{StaticResource ListItemSelected2}" Slices="{StaticResource listItemSlices}" Width="{TemplateBinding Width}" Height="{TemplateBinding Height}" Opacity="0"/>
            <ls:LSNineSliceImage x:Name="img5" ImageSource="{StaticResource ListItemSelected3}" Slices="{StaticResource listItemSlices}" Width="{TemplateBinding Width}" Height="{TemplateBinding Height}" Opacity="1"/>

        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="Visibility" Value="Visible">
                <Trigger.EnterActions>
                    <BeginStoryboard x:Name="BeginSelectorStoryboard" Storyboard="{StaticResource selectorStoryboard}"/>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <StopStoryboard BeginStoryboardName="BeginSelectorStoryboard"/>
                </Trigger.ExitActions>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="PhotoModeTabItemStyle" TargetType="TabItem" BasedOn="{StaticResource {x:Type HeaderedContentControl}}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Grid x:Name="TabRoot" ToolTipService.Placement="Left" HorizontalAlignment="Center">
                        <Image x:Name="tabBG" Source="{StaticResource NavBackground}" Stretch="None"  Margin="0,0,0,-82" Visibility="Hidden"/>
                        <ContentPresenter ContentSource="Header"/>
                        <Image x:Name="tabSelector" Source="{StaticResource NavTabSelector}" VerticalAlignment="Bottom" Stretch="None" Margin="-30,0,-30,-34" Visibility="Hidden"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="tabBG" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="tabSelector" Property="Visibility" Value="Visible"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="FocusVisualStyle" Value="{StaticResource Style.Focus.Inner.Uniform}"/>
    </Style>

    <Style x:Key="PhotoModeTabPanelController" TargetType="TabControl">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="1">
                            <ContentPresenter ContentSource="SelectedContent"/>
                        </Grid>

                        <Image Source="{StaticResource NavDivider}" Stretch="None" VerticalAlignment="Bottom" />
                        <StackPanel Orientation="Horizontal" Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Bottom" >
                            <ls:LSButton Template="{StaticResource ControllerButton.ImageSwitcher}"
                         BoundEvent="UITabPrev"
                         DataContext="{Binding CurrentPlayer.UIData.InputEvents, ConverterParameter=UITabPrev, Converter={StaticResource FindInputEventConverter}}"
                         ls:LSButton.EatInput="False"
                         Width="{DynamicResource ScaledControllerHintSize}"
                         Height="{DynamicResource ScaledControllerHintSize}"
                         VerticalAlignment="Center">
                                <b:Interaction.Triggers>
                                    <b:EventTrigger EventName="Click">
                                        <ls:SelectNextListBoxItem TargetObject="{Binding RelativeSource={RelativeSource TemplatedParent}}" Reversed="True" ForceMode="Cycle"/>
                                    </b:EventTrigger>
                                </b:Interaction.Triggers>
                            </ls:LSButton>

                            <TabPanel x:Name="HeaderPanel" IsItemsHost="True" KeyboardNavigation.TabIndex="1" VerticalAlignment="Center"/>

                            <ls:LSButton Template="{StaticResource ControllerButton.ImageSwitcher}"
							 BoundEvent="UITabNext"
							 DataContext="{Binding CurrentPlayer.UIData.InputEvents, ConverterParameter=UITabNext, Converter={StaticResource FindInputEventConverter}}"
							 ls:LSButton.EatInput="False"
							 Width="{DynamicResource ScaledControllerHintSize}"
							 Height="{DynamicResource ScaledControllerHintSize}"
							 VerticalAlignment="Center">
                                <b:Interaction.Triggers>
                                    <b:EventTrigger EventName="Click">
                                        <ls:SelectNextListBoxItem TargetObject="{Binding RelativeSource={RelativeSource TemplatedParent}}" ForceMode="Cycle"/>
                                    </b:EventTrigger>
                                </b:Interaction.Triggers>
                            </ls:LSButton>
                        </StackPanel>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ItemContainerStyle" Value="{StaticResource PhotoModeTabItemStyle}"/>
    </Style>

    <Style x:Key="IconCarouselButtonStyle" TargetType="ButtonBase">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate>
                    <Image x:Name="arrow" Source="{StaticResource CarouselArrow_d}" Stretch="None"/>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="arrow" Property="Opacity" Value="0.2"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Margin" Value="20" />
    </Style>

    <ControlTemplate x:Key="ControllerPhotoModeOption" TargetType="ContentControl">
        <Grid x:Name="bg" Width="{TemplateBinding Width}">
            <Image x:Name="box" Source="{StaticResource ListButtonBg}" Stretch="Fill"/>
            <ContentPresenter x:Name="content" IsEnabled="False"/>
            <Control x:Name="frame" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="-20"/>
        </Grid>
        <ControlTemplate.Triggers>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="ls:MoveFocus.IsFocused" Value="True"/>
                    <Condition Property="IsVisible" Value="True"/>
                </MultiTrigger.Conditions>
                <Setter TargetName="box" Property="Source" Value="{StaticResource ListButtonBg_h}"/>
                <Setter TargetName="frame" Property="Template" Value="{StaticResource FocusedListItemIndicatorTemplate}"/>
                <Setter TargetName="content" Property="IsEnabled" Value="True"/>
            </MultiTrigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="ControllerPhotoModeOptionStyle" TargetType="ContentControl">
        <Setter Property="Template" Value="{StaticResource ControllerPhotoModeOption}"/>
        <Setter Property="ls:MoveFocus.Focusable" Value="True"/>
        <Setter Property="Focusable" Value="True"/>
        <Setter Property="Margin" Value="14,0,14,12"/>
        <Setter Property="Height" Value="180"/>
    </Style>

    <ControlTemplate x:Key="SliderTemplatePM" TargetType="Slider">
        <Grid x:Name="SliderBase" HorizontalAlignment="Center" Width="520" Height="46" Opacity="0.7">
            <b:Interaction.Triggers>
                <b:PropertyChangedTrigger Binding="{Binding Value, RelativeSource={RelativeSource TemplatedParent}}">
                    <ls:LSPlaySound Sound="UI_HUD_SplitItem_Step"/>
                </b:PropertyChangedTrigger>
            </b:Interaction.Triggers>
            <ls:LSRepeatButton Template="{x:Null}"
                           Command="Slider.DecreaseSmall"
                           Focusable="False"
                           BoundEvent="UILeft"
                           Interval="1"/>
            <ls:LSRepeatButton Template="{x:Null}"
                           Command="Slider.IncreaseSmall"
                           Focusable="False"
                           BoundEvent="UIRight"
                           Interval="1" />

            <Rectangle Opacity="0.6" Fill="{StaticResource LS_accent100TxtColor}" Height="8" HorizontalAlignment="Stretch" VerticalAlignment="Center" Width="Auto" Margin="16,0,16,0" />
            <Rectangle Opacity="0.6" VerticalAlignment="Center" HorizontalAlignment="Left" Fill="{StaticResource options.SliderEnds}" Height="42" Width="8" />
            <Rectangle Opacity="0.6" VerticalAlignment="Center" HorizontalAlignment="Right" Fill="{StaticResource options.SliderEnds}" Height="42" Width="8" />
            <Track Name="PART_Track" VerticalAlignment="Center" SnapsToDevicePixels="True" HorizontalAlignment="Stretch" Margin="16,0,16,0">
                <Track.Thumb>
                    <ls:LSThumb x:Name="thumb" Style="{StaticResource OptionSliderThumbStyle}" Width="20">
                    </ls:LSThumb>
                </Track.Thumb>
            </Track>
        </Grid>
        <ControlTemplate.Triggers>
            <DataTrigger Binding="{Binding Enabled}" Value="False">
                <Setter Property="Opacity" Value="{DynamicResource DisabledOpacity}" />
            </DataTrigger>
            <DataTrigger  Binding="{Binding Path=(Grid.IsEnabled), ElementName=content}" Value="True">
                <Setter TargetName="thumb" Property="Style" Value="{DynamicResource OptionSliderThumbStyleOn}" />
                <Setter TargetName="SliderBase" Property="Opacity" Value="1" />
            </DataTrigger>

        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="DashItemTemplate" TargetType="ListBoxItem">
        <Rectangle x:Name="dashLine" Height="8" Fill="{StaticResource LS_accent100TxtColor}" Width="60" Margin="3,0,3,0" Opacity="0.6" />
        <ControlTemplate.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter TargetName="dashLine" Property="Opacity" Value="1" />
                <Setter TargetName="dashLine" Property="Width" Value="80" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="ListBoxDashItemStyle" TargetType="ListBoxItem">
        <Setter Property="Template" Value="{StaticResource DashItemTemplate}"/>
    </Style>

    <ControlTemplate x:Key="DashItemDynamicTemplate" TargetType="ListBoxItem">
        <Grid x:Name="dashLine" Height="8" Opacity="0.3">
            <Grid.Width>
                <MultiBinding Converter="{StaticResource DivideMultiConverter}" ConverterParameter="Round" >
                    <Binding Path="ActualWidth" ElementName="DynamicSlider" Converter="{StaticResource AddConverter}" ConverterParameter="-80"/>
                    <Binding Path="ItemsSource.Count" RelativeSource="{RelativeSource AncestorType={x:Type ListBox}}" Converter="{StaticResource AddConverter}" ConverterParameter="-1"/>
                </MultiBinding>
            </Grid.Width>
            <Rectangle x:Name="dashLineFill" Fill="{StaticResource LS_accent100TxtColor}" Height="8" HorizontalAlignment="Stretch" Margin="2,0"/>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter TargetName="dashLine" Property="Opacity" Value="1" />
                <Setter TargetName="dashLine" Property="Width" Value="80" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="ListBoxDashDynamicItemStyle" TargetType="ListBoxItem">
        <Setter Property="Template" Value="{StaticResource DashItemDynamicTemplate}"/>
    </Style>

    <ControlTemplate x:Key="DashLineCheckbox">
        <StackPanel Orientation="Horizontal">
            <Rectangle x:Name="dashLineOff" Height="8" Fill="{StaticResource LS_accent100TxtColor}" Width="60" Margin="3,0,3,0" Opacity="0.6" />
            <Rectangle x:Name="dashLineOn" Height="8" Fill="{StaticResource LS_accent100TxtColor}" Width="60" Margin="3,0,3,0" Opacity="0.6" />
        </StackPanel>
        <ControlTemplate.Triggers>
            <DataTrigger Binding="{Binding }" Value="True">
                <Setter TargetName="dashLineOn" Property="Opacity" Value="1" />
                <Setter TargetName="dashLineOn" Property="Width" Value="80" />
            </DataTrigger>
            <DataTrigger Binding="{Binding }" Value="False">
                <Setter TargetName="dashLineOff" Property="Opacity" Value="1" />
                <Setter TargetName="dashLineOff" Property="Width" Value="80" />
            </DataTrigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
</ResourceDictionary>